# DISTILL — attack on `L-CYC-SEC` (is C5 a SECONDARY / TWISTED / EQUIVARIANT cyclic class of `Q_ℕ`?)

**Mode:** DISTILL
**Timestamp:** 1782111292
**Branch:** claude/epic-wozniak-53e47f
**Attacking:** `L-CYC-SEC` (candidate lemma from `work/1782110302-EXPLORE.md`, node A19) — the
disjunction that C5, being invisible to every PRIMARY (degree-0, gauge-invariant) cyclic cocycle
on Cuntz's `ax+b` algebra `Q_ℕ` (A18, `[AFF-2]`), is carried by **(a)** a non-periodic / secondary
(Massey-type) `HC^{2k}` class on `ker τ_primary`, **(b)** a σ-TWISTED (modular/KMS) `HC^*_σ` class,
or **(c)** an `S¹`-gauge-EQUIVARIANT class — **AND** that this class is an **INDEPENDENT HANDLE**
on C5 (an actual (co)closed cocycle + a structural evaluation tied to `S(x)` that is NOT
`S(x)/x`-by-definition and NOT a `K_*(Q_ℕ)` pairing).

**The single decisive question (the whole content is the rider):** is the secondary/twisted/
equivariant class an INDEPENDENT handle, or does it collapse to (i) the `τ_λ` trap (= C5 by
definition) or (ii) K-theory blindness — i.e. the `L-C3-DUOIDAL` "a name, not a handle" verdict?

> **Provenance (binding, inherited from A18).** arXiv PDFs and publisher pages return **HTTP-403**
> in this environment (math/0611541, 0906.4903, 0907.3760, 0801.4605, 1007.4661, plus ADS / Cambridge
> Core / ScienceDirect / ResearchGate abstract pages — all re-confirmed 403 this pass). Operator-
> algebra facts and the two new literature computations below are **search-surfaced from multiple
> independent secondary restatements**, primary PDFs **not read**; I OPEN-cap every such fact. The
> computational audits **are** reproducible and are pasted inline. Per `AGENTS.md`: I ran the steelman
> (STAGE 1) for real — I genuinely tried to build the higher cocycle the lemma needs — and I did
> **not** manufacture a handle. I do not label; verdict lines (NEEDS-REVIEW vs FALSIFIED) are EXPLORE's.

---

## Executive summary

- **STAGE 1 (steelman) FAILED to exhibit an independent handle.** I tried to build the one object the
  lemma's residual crack needs: a **higher-degree** (twisted / equivariant / secondary) cyclic cocycle
  pairing `A = M_λ u² M_λ u^{-2}` against **other** generators (`u`-powers, `s_p`/`M_g`, `e_p`), with a
  structurally-computable, non-`C5`, non-K-theory evaluation. Three concrete candidates (I: the canonical
  twisted 1-cocycle `φ(a₀ δ(a₁))` of derivation type; II: a higher 2-cocycle pairing `A` against `u²` and
  a multiplicative monomial via the bridge relation `s_p u = u^p s_p`; III: the `S¹`-equivariant
  χ-isotypic refinement) **each collapse**, and they collapse for the **same degree-independent reason**.
- **The collapse object (the decisive finding): a DEGREE-INDEPENDENT integrand dichotomy.** Any
  gauge-traceable evaluation built from `A` and the generators is, term-by-term on the diagonal,
  **exactly one of two things**, with **no third case**:
  - **(C-two)** the cyclic word **re-encloses the shift** (`u^{+2}…u^{-2}` bracketing the two `M_λ`
    factors) ⇒ the diagonal at `n` is `λ(n)λ(n+2)` (times a gauge/scaling weight) ⇒ value = a
    **(weighted) two-point Liouville sum = C5-class**; or
  - **(C-zero)** the word is gauge-degree-0 **without** enclosing the shift ⇒ one of the two
    `λ`-evaluations is dropped ⇒ value = a **one-point / constant** Liouville quantity (PNT-class:
    `⟨λ(n+2)·w(n)⟩`, or `⟨λ(n)²⟩=1`), which **carries no twin-prime (+2) content**.
  To *see* the `+2` sign-correlation you must enclose the shift, which **reproduces `A` = C5**; to be
  gauge-traceable **without** enclosing it you must **drop** a `λ`-evaluation, killing the correlation.
  This is degree-independent: raising the cocycle degree only adds more generators around the same
  bracket, never a third way to be both `+2`-sensitive and not-`A`.
- **Two NEW literature computations sharpen the cull (search-surfaced, OPEN-capped).** (1)
  **Gourdeau–White** (*The cyclic and simplicial cohomology of the Cuntz semigroup algebra*,
  arXiv:1007.4661, JMAA): for the `ℓ¹` Cuntz semigroup algebra `HC^n = 0` for `n` **odd** and is
  **1-dimensional for `n` even** (`n≥2`), Hochschild `H^n = 0` for `n≥1` — so the higher even cyclic
  classes are the **`S`-operator (periodicity) images of the degree-0 trace**, *not* independent of it
  (this is candidate (a)'s home, and it shows (a) is governed by the trace). (2) **Carey–Phillips–Rennie**
  (*Twisted cyclic theory and an index theory for the gauge invariant KMS state on Cuntz algebras*,
  arXiv:0801.4605, J. K-Theory 6 (2010) 339–380): the σ-twisted cyclic cocycle on Cuntz algebras twists
  the **gauge-invariant** (λ-blind) KMS state, requires a **modified `K_1`** to pair with, and the index
  pairing computes **spectral flow / Araki relative entropy** — an **index number on the λ-blind side**,
  *not* a λ-two-point correlation (this is candidate (b)'s home, and it shows (b) lands on the blind
  side, exactly as A18 `[AFF-4]`/attack 4 predicted).
- **NET: the wall HOLDS; no handle opened.** `L-CYC-SEC`'s INDEPENDENT-HANDLE clause is, on every
  construction attempted, the `L-C3-DUOIDAL` **"a name, not a handle"** outcome — a class definable only
  as (a weighted/restricted version of) C5, or as a λ-blind index. The result reduces to a **sharper,
  degree-independent WALL** (`[CYC-WALL]`, in the `L-AFF-GAUGE` family) plus the **falsified
  independent-handle clause** (`[CYC-HANDLE]`, collapse object exhibited). The periodic-inheritance
  OPEN cap is **narrowed but not closed** (`[CYC-PERIODIC]`). **NOT progress on C5, NOT terminal.**

---

## STAGE 1 — STEELMAN: genuinely try to exhibit the independent handle

The lemma's own audits (work/1782110302) already disposed of the *easy* sinks: the natural **additive**
secondary cochain trivializes (Fermat cochain `C_p` is a coboundary over ℚ, Audit A); every **degree-0**
secondary class is gauge-blind (Audit B); the **σ-twist is multiplicative-only** (trivial on `u`), so the
twisted *diagonal trace* of `A` is C5-class (Audit C). EXPLORE correctly isolated the surviving crack:
a genuinely **HIGHER-degree** cocycle pairing `A` against **other** elements. So I tried to build exactly
that. The code (run, output inline) tests three concrete candidates spanning (a)/(b)/(c).

```python
import numpy as np
N = 2_000_000
spf = np.zeros(N+5, dtype=np.int64)
for i in range(2, N+5):
    if spf[i]==0: spf[i::i] = np.where(spf[i::i]==0, i, spf[i::i])
lam = np.ones(N+5, dtype=np.int64)
for n in range(2, N+5): lam[n] = -lam[n//spf[n]]
lam[1]=1
def expvec(m):
    d={}
    while m>1:
        p=spf[m]; c=0
        while m%p==0: m//=p; c+=1
        d[p]=c
    return d
x = N-3

# CANDIDATE I. The canonical TWISTED 1-COCYCLE of derivation type (the CPR shape):
#   c(a0,a1) = phi( a0 * delta(a1) ),  phi = trace-per-unit-length, delta = gauge/number generator.
# delta multiplies a diagonal monomial by its gauge degree = big-Omega(n). KEY: delta(u)=0 (u is
# gauge-weight 0!), so the +2 partner u^2 is INVISIBLE to delta.
Omega = np.zeros(N+5, dtype=np.float64)
for n in range(2, N+5): Omega[n] = Omega[n//spf[n]] + 1.0
print("c(A,u^2): delta(u)=0 => c(A,u^2)=0 IDENTICALLY (the +2 partner is killed by the natural derivation)")
cI_Mlam = np.sum(lam[3:x+2]*Omega[1:x])/x   # phi(A . delta(M_lam)) = <lam(n)lam(n+2)*Omega(n)*lam(n)> = <lam(n+2)*Omega(n)>
print("c(A,M_lam) = <lam(n+2)*Omega(n)>_avg = %.6e  [a loglog-weighted ONE-POINT Liouville sum; the +2 correlation is GONE]" % cI_Mlam)

# CANDIDATE II. Higher 2-cocycle pairing A against u^2 and a completely-mult gauge weight chi (using
# s_p u = u^p s_p to couple +2 to the grading). Any u-balanced, gauge-traceable word enclosing the
# shift has diagonal chi(n)*chibar(n+2). Scan random completely-mult sign characters chi:
def chi_from_signs(signvec):
    arr = np.ones(N+5, dtype=np.int64)
    for nn in range(2, N+5): arr[nn] = signvec.get(spf[nn],1)*arr[nn//spf[nn]]
    arr[1]=1; return arr
import random; random.seed(0)
primes_small=[p for p in range(2,60) if spf[p]==p]
for trial in range(4):
    chi = chi_from_signs({p: random.choice([1,-1]) for p in primes_small})
    val = np.sum(chi[1:x]*chi[3:x+2])/x
    print("  chi(rand signs p<60): <chi(n)chi(n+2)>_avg = %.6e  [STILL a two-point corr of a completely-mult fn = C5-CLASS]" % val)

# CANDIDATE III. S^1-gauge-EQUIVARIANT chi-isotypic refinement. A's diagonal term at n has gauge degree
# Omega(n)+Omega(n+2) > 0 always (n>=2) => the gauge-INVARIANT (degree-0) part of A is EMPTY (=AFF-2's 0).
# The equivariant degree-k component = restricted two-point sum over n with Omega(n)+Omega(n+2)=k:
from collections import defaultdict
buckets=defaultdict(float); xs=min(x,400000)
for nn in range(2,xs): buckets[int(Omega[nn]+Omega[nn+2])] += lam[nn]*lam[nn+2]
print("equivariant degree-k buckets sum_{Omega(n)+Omega(n+2)=k} lam(n)lam(n+2):",
      {k: buckets.get(k,0.0) for k in range(2,8)}, "[each is a RESTRICTED two-point sum; they SUM to S(x)]")
```
```
c(A,u^2): delta(u)=0 => c(A,u^2)=0 IDENTICALLY (the +2 partner is killed by the natural derivation)
c(A,M_lam) = <lam(n+2)*Omega(n)>_avg = -3.612505e-03  [a loglog-weighted ONE-POINT Liouville sum; the +2 correlation is GONE]
  chi(rand signs p<60): <chi(n)chi(n+2)>_avg = 3.940006e-04  [STILL a two-point corr of a completely-mult fn = C5-CLASS]
  chi(rand signs p<60): <chi(n)chi(n+2)>_avg = 4.644607e-02  [STILL a two-point corr of a completely-mult fn = C5-CLASS]
  chi(rand signs p<60): <chi(n)chi(n+2)>_avg = -3.160005e-04  [STILL a two-point corr of a completely-mult fn = C5-CLASS]
  chi(rand signs p<60): <chi(n)chi(n+2)>_avg = -2.860004e-04  [STILL a two-point corr of a completely-mult fn = C5-CLASS]
equivariant degree-k buckets sum_{Omega(n)+Omega(n+2)=k} lam(n)lam(n+2): {2: 3804.0, 3: -19695.0, 4: 43001.0, 5: -55585.0, 6: 54957.0, 7: -53595.0} [each is a RESTRICTED two-point sum; they SUM to S(x)]
```

**Reads — why each candidate is NOT a handle.**
- **Candidate I (twisted 1-cocycle, the CPR shape).** The only canonical odd cocycle pairs the trace
  against the **gauge/number derivation** `δ`. But `δ(u)=0` because `u` has gauge-weight 0 — so the `+2`
  partner `u²` is **annihilated by `δ`**, and `c(A,u²)=0` identically. Choosing instead a multiplicative
  partner `M_λ` gives `c(A,M_λ)=⟨λ(n+2)·Ω(n)⟩`, a `loglog`-weighted **one-point** Liouville sum — its
  smallness is a **PNT-class** statement about `λ`, and it has **dropped the `+2` correlation entirely**.
  This is exactly the (C-zero) leg.
- **Candidate II (higher 2-cocycle via the bridge relation).** Forcing a `u`-balanced, gauge-traceable
  word that **retains** the `+2` adjacency forces the diagonal to be `χ(n)χ̄(n+2)` for a completely-
  multiplicative gauge character `χ`. For **every** such `χ` (random sign patterns scanned) the value is
  a **two-point correlation of a completely-multiplicative sign function** — the **same family as C5**,
  the same parity barrier. This is the (C-two) leg: re-enclosing the shift reproduces an `A`-type object.
- **Candidate III (equivariant refinement).** The χ-isotypic / gauge-degree-`k` decomposition does not
  produce a new structural number: the degree-0 (invariant) part of `A` is **empty** (recovering A18's
  `[AFF-2]` "= 0"), and the degree-`k` components are **restricted two-point sums** `Σ_{Ω(n)+Ω(n+2)=k}
  λ(n)λ(n+2)` that simply **partition `S(x)`** (they sum to it). The equivariant refinement *slices* C5;
  each slice is itself a (harder, restricted) two-point sum. No slice is a structurally-computable
  non-C5 invariant.

**STAGE 1 outcome.** No candidate is an independent handle. Every candidate that **retains** the `+2`
adjacency is a (weighted/twisted/restricted) **two-point correlation = C5-class**; every candidate that
is gauge-traceable **without** the adjacency (or that uses the natural derivation, which kills `u²`)
**drops the `+2` correlation** to a one-point/constant quantity. I did **not** find the object the lemma
needs, and the *reason* I did not is structural, not a failure of imagination — which STAGE 2 nails.

---

## STAGE 2 — NAIL THE COLLAPSE: the degree-independent dichotomy, made airtight

STAGE 1's three failures share one cause. I state it as a **degree-independent** dichotomy and verify the
two exhaustive integrands term-by-term, then pin the literature.

### The dichotomy (the load-bearing argument)

**Claim.** Let `φ` be ANY cyclic `n`-cochain on `Q_ℕ` (`n` arbitrary; plain, σ-twisted, or
`S¹`-equivariant) whose evaluation on a chain built from `A = M_λ u² M_λ u^{-2}` and the generators is
**gauge-invariant** — a *necessary* condition for it to descend to a well-defined pairing on the
gauge-fixed / KMS / equivariant theory, because `Q_ℕ`'s distinguished functional (the unique β=1 KMS
state, the host of all three theories) is gauge-invariant (A18 `[AFF-2]`; CPR confirm the twisted theory
is built on the **gauge-invariant** KMS state). Then on the diagonal model the value is a sum over `n` of
a gauge weight `Π_p z_p^{(exponents of the M_λ-touched arguments)}` evaluated at `z_p = λ(p)`, restricted
to the **gauge-degree-0 (traceable)** part. Exhaustively, two cases:

- **(C-two)** the word retains a `u^{+2} … u^{-2}` bracket enclosing the two `M_λ` factors ⇒ diagonal at
  `n` is `λ(n)λ(n+2)` (times a gauge/scaling weight) ⇒ value is a **(weighted) two-point Liouville sum
  = C5-class**;
- **(C-zero)** the word is gauge-degree-0 **without** that bracket ⇒ the second `λ`-evaluation is
  un-shifted or dropped ⇒ value is a **one-point/constant** Liouville quantity (`⟨λ(n+2)·w(n)⟩` or
  `⟨λ(n)²⟩=1`) — **no `+2` content**.

There is **no third case**: the `+2` sign-correlation is visible *only* through the enclosing bracket
(which **is** `A`, i.e. C5), and gauge-traceability without the bracket forces dropping a `λ`. Raising
the degree of `φ` adds generators *around* the bracket; it never creates a third way to be both
`+2`-sensitive and not-`A`. Hence the `L-AFF-GAUGE` dichotomy is **degree-independent** — it is **not**
escaped by going to secondary / higher / twisted / equivariant cocycles.

### Term-by-term verification of the two exhaustive integrands + reproduction of Audits A/B/C

```python
# (continues; lam, spf, expvec, x from STAGE 1)
print("(C-two) enclosing M_lam u^2 M_lam u^{-2}: <lam(n)lam(n+2)> = %.6e  [C5-class]" % (np.sum(lam[1:x]*lam[3:x+2])/x))
print("(C-zero) non-enclosing M_lam M_lam (shift cancels before 2nd lam): <lam(n)^2> = %.6e [const, NO +2]" % (np.sum(lam[1:x]*lam[1:x])/x))
print("(C-zero', twisted one-point) one surviving lam(n+2): <lam(n+2)> = %.6e [ONE-POINT = PNT-class, NOT C5]" % (np.sum(lam[3:x+2])/x))

from fractions import Fraction
def C(p,a,b): return Fraction(a**p+b**p-(a+b)**p, p)
primes=[2,3,5,7,11,13]
A1 = all(C(p,a,b).denominator==1 for p in primes for a in range(-6,7) for b in range(-6,7))
def dC(p,a,b,c): return C(p,b,c)-C(p,a+b,c)+C(p,a,b+c)-C(p,a,b)
A2 = all(dC(p,a,b,c)==0 for p in primes for a in range(-4,5) for b in range(-4,5) for c in range(-4,5))
p=2; f={0:Fraction(0),1:Fraction(0)}
for n in range(2,9): f[n]=f[n-1]+f[1]-C(p,n-1,1)
f[-1]=C(p,1,-1)-f[1]
for n in range(2,9): f[-n]=f[-n+1]+f[-1]-C(p,-(n-1),-1)
A4 = all(C(p,a,b)==f[a]+f[b]-f[a+b] for a in range(-4,5) for b in range(-4,5) if a in f and b in f and a+b in f)
print("AUDIT A: Fermat cochain integral=%s, 2-cocycle on (Z,+)=%s, coboundary over Q=%s" % (A1,A2,A4))
def deg0_unc(n):
    g1=expvec(n); g2=expvec(n+2); allp=set(g1)|set(g2)
    return all(g1.get(p,0)+g2.get(p,0)==0 for p in allp)
print("AUDIT B: gauge-deg-0 lam-seeing UNcompensated +2 terms in 1..4999 = %d" % sum(deg0_unc(n) for n in range(1,5000)))
def twisted(s):
    n=np.arange(1,x); w=n.astype(float)**(-s); return np.sum(w*lam[1:x]*lam[3:x+2])/np.sum(w)
print("AUDIT C: sigma-twisted diagonal traces of A:", {s: round(float(twisted(s)),6) for s in [0.0,0.3,1.0]})
```
```
(C-two) enclosing M_lam u^2 M_lam u^{-2}: <lam(n)lam(n+2)> = -4.690007e-04  [C5-class]
(C-zero) non-enclosing M_lam M_lam (shift cancels before 2nd lam): <lam(n)^2> = 9.999995e-01 [const, NO +2]
(C-zero', twisted one-point) one surviving lam(n+2): <lam(n+2)> = -6.160009e-04 [ONE-POINT = PNT-class, NOT C5]
AUDIT A: Fermat cochain integral=True, 2-cocycle on (Z,+)=True, coboundary over Q=True
AUDIT B: gauge-deg-0 lam-seeing UNcompensated +2 terms in 1..4999 = 0
AUDIT C: sigma-twisted diagonal traces of A: {0.0: -0.000469, 0.3: -0.000406, 1.0: -0.089949}
```

**Read.** The two legs are verified: (C-two) is the genuine small correlation (`≈ −4.7e-4` = C5-class);
(C-zero) is a constant (`λ(n)²=1`, no information) or a one-point PNT-class sum (`⟨λ(n+2)⟩ ≈ −6.2e-4`,
the smallness of which is the prime number theorem for `λ`, **not** a twin-prime statement). The
candidate's own Audits A/B/C **reproduce exactly** (Fermat cochain a coboundary over ℚ; degree-0
secondary blind, 0 of 4999; σ-twisted traces all C5-class). The dichotomy is airtight at the
term-by-term level: there is no integrand that is `+2`-sensitive and not of the (C-two) `A`-form.

### Literature: did anyone compute the relevant groups? (search-surfaced; PDFs 403)

I attacked the periodic-inheritance OPEN cap by searching for any computation of `HP*/HC*/HC*_σ` of
`Q_ℕ` / Bost–Connes / Cuntz / Hecke-type algebras. Two relevant computations surfaced; **both PDFs and
all abstract pages 403**, so these are **search-surfaced / OPEN-capped** (cross-confirmed across ≥3
independent secondary restatements each), in line with the A18 protocol. I did **not** read the bodies
and do **not** fabricate a computation of `HP*(Q_ℕ)`.

1. **Gourdeau–White, *The cyclic and simplicial cohomology of the Cuntz semigroup algebra*,
   arXiv:1007.4661 (J. Math. Anal. Appl.).** Search-surfaced result: for the `ℓ¹` Cuntz semigroup
   algebra, **`HC^n = 0` for `n` odd and `HC^n` is one-dimensional for `n` even (`n ≥ 2`)**; via the
   Connes–Tsygan sequence the simplicial (Hochschild) `H^n = 0` for `n ≥ 1`. **Bearing on candidate (a):**
   the only nonzero higher cyclic classes are the **even** ones, and a one-dimensional `HC^{2k}` arising
   from a one-dimensional `HC^0` (trace) is the **`S`-operator (periodicity) image of the trace** — i.e.
   the higher classes are **not independent of the degree-0 trace**; they are `S^k τ`. A "secondary
   class definable on `ker τ_primary`" therefore has **no room** to be a new handle in this algebra:
   the cyclic cohomology is exhausted (up to `S`-periodicity) by the trace, exactly the object A18
   showed is λ-blind/C5. *Caveat:* this is the **multiplicative** Cuntz semigroup `ℓ¹`-algebra, not
   `Q_ℕ` itself (which adds the unitary `u`); it is the **closest** computation in the literature and
   it points hard at the cull, but it is **not** `HP^*(Q_ℕ)`. OPEN cap stands; narrowed, not closed.

2. **Carey–Phillips–Rennie, *Twisted cyclic theory and an index theory for the gauge invariant KMS
   state on Cuntz algebras*, arXiv:0801.4605 (J. K-Theory 6 (2010) 339–380).** Search-surfaced result:
   they construct a **σ-twisted cyclic cocycle** where the twist is the **modular automorphism of the
   canonical gauge action**, twisting the **gauge-invariant KMS state**; it requires a **modified `K_1`
   group** to pair with, and the index pairing computes **spectral flow**, interpreted via **Araki's
   relative entropy**. **Bearing on candidate (b):** this is the **actual** σ-twisted cyclic theory for
   exactly this class of algebras, and it lands **on the gauge-invariant (λ-blind) side** and produces
   an **index/entropy number**, *not* a λ-two-point correlation — precisely A18 `[AFF-4]`/attack-4's
   prediction, now backed by the primary construction's stated content. The twisted host **exists**
   (the lemma's "existence" claim is correct) but it is built where C5 is invisible.

**Combined literature read.** The two computations cover the two analytic legs of the disjunction:
(a) higher/secondary cyclic = `S`-periodicity of the trace (no independent class), and (b) σ-twisted
cyclic = gauge-invariant/λ-blind index. Neither is an independent handle on C5. This is consistent with,
and independent corroboration of, the degree-independent dichotomy proven term-by-term above.

---

## What the attack found (deltas for EXPLORE to fold when labelling)

1. **STAGE 1 exhibited NO handle.** The genuine steelman (three concrete higher candidates spanning
   (a)/(b)/(c)) failed, each collapsing to (C-two) C5-class or (C-zero) one-point/0.
2. **Collapse object (the decisive output): the DEGREE-INDEPENDENT integrand dichotomy.** Any
   gauge-traceable cyclic evaluation of `A` is either a (weighted/restricted) two-point sum (= C5) or has
   dropped the `+2` adjacency (one-point/constant). No third case; raising degree does not help. This
   **promotes** `L-AFF-GAUGE`'s degree-0 dichotomy to **all degrees** and is the sharpened wall.
3. **Two new literature computations** (search-surfaced, OPEN-capped) corroborate, on the two analytic
   legs: Gourdeau–White (higher cyclic = `S`-image of the trace) and Carey–Phillips–Rennie (σ-twisted =
   gauge-invariant index). Neither escapes the dichotomy.
4. **Periodic-inheritance OPEN cap NARROWED, not closed.** The Gourdeau–White computation is for the
   multiplicative `ℓ¹` Cuntz semigroup algebra, not `Q_ℕ` with its unitary `u`; `HP^*(Q_ℕ)` is still
   **not** computed in this environment (PDFs 403). What would close it: a primary-source computation of
   `HP^*(Q_ℕ)` (or its `HC^*_σ` / `S¹`-equivariant theory) showing the higher/twisted/equivariant classes
   are exhausted by `S`-periodicity of the gauge-invariant trace — readable once PDF access exists.
5. **The independent-handle clause of `L-CYC-SEC` is the `L-C3-DUOIDAL` "a name, not a handle"
   outcome**, now with a **concrete, reproducible collapse object** (the integrand dichotomy + the two
   exhaustive averages), exactly as the lemma's decisive question feared.

---

## REDUCED CLAIM GRAPH

The result under attack is the INDEPENDENT-HANDLE clause of a **disposition** (`L-CYC-SEC`), not a
theorem toward TPC. Its smallest load-bearing decomposition is three claims. `[CYC-HANDLE]` is the
clause the whole lemma rests on (and the one the attack targets); `[CYC-WALL]` is the surviving
sharpened (degree-independent) wall it collapses to; `[CYC-PERIODIC]` is the narrowed periodic-
inheritance cap. **STATUS lines are pre-fills per the DISTILL protocol; the NEEDS-REVIEW-vs-FALSIFIED
verdict on not-yet-certified claims is EXPLORE's.**

---

`[CYC-HANDLE]`  **The independent-handle clause (the load-bearing rider of `L-CYC-SEC`).** There exists
a secondary (`HC^{2k}` on `ker τ_primary`), σ-twisted (`HC^*_σ`), or `S¹`-equivariant cyclic cocycle `φ`
on `Q_ℕ`, written on the generators, that is (co)closed in its named complex AND whose pairing with a
chain built from `A = M_λ u² M_λ u^{-2}` is an **independent structural evaluation** tied to `S(x)` —
**not** `S(x)/x`-by-definition (the `τ_λ` trap) and **not** a `K_*(Q_ℕ)` pairing (λ-blind).
   DEPENDS ON: `[CYC-WALL]` (it is the negation of the wall's "no third case"); A18 `[AFF-2]`,`[AFF-4]`.
   STATUS: **FALSIFIED** (pre-fill — concrete collapse object exhibited; EXPLORE confirms the verdict).
   No such `φ` was found; the steelman's three candidates and the degree-independent dichotomy show every
   gauge-traceable evaluation is either C5-class (encloses the shift) or has dropped the `+2` adjacency
   (one-point/0). The two literature computations corroborate on both analytic legs. This is the
   `L-C3-DUOIDAL` "a name, not a handle" outcome with a reproducible collapse object.
   AUDIT (<5 min): run the STAGE 1 + STAGE 2 Python (inline). Inspect the **two exhaustive averages**:
   the enclosing word gives `⟨λ(n)λ(n+2)⟩ ≈ −4.7e-4` (= C5-class), the non-enclosing word gives
   `⟨λ(n+2)⟩ ≈ −6.2e-4` (a one-point PNT-class sum, no `+2` content) or `⟨λ(n)²⟩ = 1` (constant). A human
   sees there is no third integrand. For the literature legs: read the search-surfaced abstracts of
   Gourdeau–White (arXiv:1007.4661 — higher cyclic = `S`-image of the trace) and Carey–Phillips–Rennie
   (arXiv:0801.4605 — σ-twisted = gauge-invariant index/entropy) once PDF access exists.
   ADVERSARIAL PASS: STAGE 1 (three concrete higher candidates I/II/III, all collapsed) + STAGE 2
   (degree-independent dichotomy, two integrands verified term-by-term; two literature computations).
   **Outcome: name, not a handle — FALSIFIED clause. Collapse object exhibited.**

`[CYC-WALL]`  **The sharpened, DEGREE-INDEPENDENT gauge dichotomy (the surviving wall).** Any cyclic
`n`-cochain on `Q_ℕ` (plain, σ-twisted, or `S¹`-equivariant; any degree) whose evaluation on `A` and the
generators is gauge-invariant returns, term-by-term on the diagonal, **exactly one of**: a
(weighted/restricted) **two-point Liouville correlation `⟨λ(n)λ(n+2)·w⟩` = C5-class** (if the word
encloses the `u^{+2}…u^{-2}` shift around the two `M_λ` factors), **or** a **one-point/constant**
Liouville quantity with no `+2` content (if it is gauge-degree-0 without enclosing the shift). No third
case. Hence A18's degree-0 dichotomy (`[AFF-2]`) holds in **all** degrees: the secondary/higher/twisted/
equivariant theories do not escape it.
   DEPENDS ON: A18 `[AFF-2]` (the degree-0 case), `[AFF-1]`; established mathematics (gauge-invariance of
   the canonical KMS state; conditional expectation onto the diagonal; complete-multiplicativity of `λ`).
   STATUS: NEEDS-REVIEW (pre-fill — reproducible term-by-term audit is the certificate; equal-hardness /
   no-handle WALL-STATEMENT in the `L-AFF-GAUGE` family; **not** a foundation, **not** a bound). EXPLORE
   assigns the final label.
   AUDIT (<5 min): run the inline Python. The two exhaustive integrands reproduce (`⟨λ(n)λ(n+2)⟩` vs
   `⟨λ(n+2)⟩`/`⟨λ(n)²⟩`); the equivariant buckets `Σ_{Ω(n)+Ω(n+2)=k} λ(n)λ(n+2)` partition `S(x)`
   (they sum to it), showing the equivariant refinement slices C5 rather than escaping it; the gauge
   degree `Ω(n)+Ω(n+2) > 0` for all `n≥2` (so the invariant part of `A` is empty, recovering `[AFF-2]`).
   The "no third case" step is a one-paragraph operator-algebra check a human does by hand: a `+2`
   sign-correlation requires the enclosing bracket, which is `A`.
   ADVERSARIAL PASS: STAGE 1 + STAGE 2. The wall **holds** and is **strengthened** from degree-0 to
   degree-independent. Crux it localizes: the **parity obstruction (C5)**, restated as a degree-
   independent cyclic-cohomological dichotomy — a **checkpoint, not terminal**. What would **break or
   escalate** it: a cyclic/twisted/equivariant cocycle whose evaluation is `+2`-sensitive **without**
   enclosing the shift (the negation of "no third case"), or a primary `HP^*(Q_ℕ)` computation exhibiting
   a class not of `S`-periodic-trace type — none found; producing one is the open target.

`[CYC-PERIODIC]`  **Periodic-inheritance (narrowed OPEN cap).** If `HP^*(Q_ℕ)` is governed by the
Chern–Connes character against `K_*(Q_ℕ)` (= exterior algebra on primes, no `+2` class — A18 `[AFF-4]`),
the **periodic** cyclic theory inherits A18's blindness; and the higher cyclic classes of the closely
related Cuntz semigroup algebra are `S`-periodicity images of the trace (Gourdeau–White) — so any
carrier of C5 would have to be a genuinely **non-periodic / twisted / equivariant** class not of
`S`-periodic-trace type.
   DEPENDS ON: A18 `[AFF-4]`; established mathematics (Chern–Connes character; `HP^* = lim_S HC^*`).
   STATUS: **OPEN** (pre-fill — `HP^*(Q_ℕ)` is **not** computed in this environment; PDFs 403). The
   Gourdeau–White computation is for the multiplicative `ℓ¹` Cuntz semigroup algebra, **not** `Q_ℕ` with
   its unitary `u`; it **narrows** the cap (corroborates `S`-periodicity-of-trace for the closest
   algebra) but does **not** close it. Pre-fill OPEN, **not** NEEDS-REVIEW: no finite checkable
   certificate of `HP^*(Q_ℕ)` exists here, and "no non-periodic carrier" is a non-existence claim.
   AUDIT (what would certify/refute in <5 min once accessible): a primary-source computation of
   `HP^*(Q_ℕ)` / `HC^*(Q_ℕ)` (or its `HC^*_σ` / equivariant theory) — e.g. via Cuntz–Quillen excision on
   the crossed-product extension defining `Q_ℕ` — showing whether the higher/twisted/equivariant classes
   are exhausted by `S`-periodicity of the gauge-invariant trace (closes the cull) or contain an
   independent class (opens a lead). Search-surfaced inputs: Gourdeau–White arXiv:1007.4661;
   Carey–Phillips–Rennie arXiv:0801.4605; Cuntz–Quillen excision (Brodzki, Soton eprint 363525).
   ADVERSARIAL PASS: STAGE 2 literature search. Narrowed (two relevant computations surfaced, both on
   the side of the cull) but **OPEN-capped** (neither is `HP^*(Q_ℕ)`; PDFs 403). What would break it: a
   `HP^*(Q_ℕ)` computation exhibiting an independent (non-`S`-trace) class — not found.

---

## Honest disposition (anti-deception, weighted first)

- I ran the steelman **for real** (STAGE 1: three concrete higher twisted/equivariant/secondary
  candidates, pairing `A` against `u`-powers, multiplicative monomials, and via the bridge relation).
  **No handle opened.** The most valuable output is the **collapse object**: a degree-independent
  integrand dichotomy with two reproducible exhaustive averages, plus two corroborating literature
  computations. This is the `L-C3-DUOIDAL` **"a name, not a handle"** verdict, made concrete.
- **The independent-handle clause `[CYC-HANDLE]` is FALSIFIED** (collapse object exhibited); the lemma
  reduces to the **sharper degree-independent WALL `[CYC-WALL]`** (`L-AFF-GAUGE` family, NEEDS-REVIEW
  pre-fill) plus the **narrowed periodic OPEN cap `[CYC-PERIODIC]`**. This is **not** progress on C5 and
  **not** terminal — `[CYC-WALL]` localizes the parity obstruction one degree-level deeper, a checkpoint
  with a stated escalation path (the negation of "no third case", or a `HP^*(Q_ℕ)` computation).
- **No bound on C5 certified. No GRH/EH/heuristic smuggled.** The only inputs are the (search-surfaced)
  operator-algebra facts and the (reproducible) finite computations; none is used to bound C5.
- **OPEN caps carried forward:** `HP^*(Q_ℕ)` / `HC^*(Q_ℕ)` / `HC^*_σ(Q_ℕ)` / equivariant groups are
  **not** computed from primary sources (PDFs 403); the two new computations (Gourdeau–White,
  Carey–Phillips–Rennie) are **search-surfaced / OPEN-capped** and concern closely related (not
  identical) algebras; the periodic-inheritance argument is narrowed but **OPEN**. I did **not** fabricate
  `HP^*(Q_ℕ)`.

**Artifact ready for EXPLORE to label** (`[CYC-HANDLE]` FALSIFIED-pre-fill with collapse object;
`[CYC-WALL]` NEEDS-REVIEW-pre-fill sharpened wall; `[CYC-PERIODIC]` OPEN narrowed cap). The wall holds;
STAGE 1 exhibited no handle; the degree-independent collapse object is the decisive finding.
