# EXPLORE — Label passes F–J, then Review; declare terminal state (3)

**Mode:** EXPLORE
**Timestamp:** 1781355529
**Branch:** claude/loving-turing-1r5pqz

A DISTILL pass (`work/1781354707-DISTILL.md`, passes **F–J**) has returned with its
nodes **unlabelled**. Per the protocol my **first act is labelling**, done
independently — I re-ran the two load-bearing computations (Pass F and Pass G) at
parameters *different* from DISTILL's and re-checked the three load-bearing literature
facts against primary sources before assigning any label. Then I take **one action:
Review** — a survey of the whole sheet for missed escaping angles and for the rigour of
the reduction itself — and render a terminal judgment.

---

## Part 1 — LABELLING (mandatory first act; each verdict independently verified)

### Verification V-F — re-run Pass F at an independent N (6·10⁶, DISTILL used 8·10⁶)

The decisive new claim is that the "bucket is invisible (worst ratio ≈ 0.0048)"
corroboration from the prior EXPLORE step (`work/1781354180-EXPLORE.md` §2.3) is a
**small-`d` artifact**: the worst `|bucket sum|/count` blows up as `d → D` while the
z-score `|sum|/√count` stays bounded. I reproduced it at `N=6·10⁶`:

```python
import math
N = 6_000_000
Om = [0]*(N+1); is_comp = [False]*(N+1)
for p in range(2, N+1):
    if not is_comp[p]:
        pk = p
        while pk <= N:
            for m in range(pk, N+1, pk): Om[m]+=1
            pk *= p
        for m in range(2*p, N+1, p): is_comp[m]=True
def lam(n): return 1 if Om[n]%2==0 else -1
x = N-2
def carrier(n): return lam(n)*lam(n+2)
S = sum(carrier(n) for n in range(1,x+1))
print(f"GLOBAL sum, n<=x={x}: {S}  ratio/x={S/x:+.6f}  (1/sqrt(x)={1/math.sqrt(x):.6f})")
for d in [2,3,7,13,210,2310,10000,30030,100000]:
    sums=[0]*d; cnts=[0]*d
    for n in range(1,x+1):
        r=n%d; sums[r]+=carrier(n); cnts[r]+=1
    wr = max(abs(sums[a])/cnts[a] for a in range(d) if cnts[a])
    wz = max(abs(sums[a])/math.sqrt(cnts[a]) for a in range(d) if cnts[a])
    print(f"{d:>7} {x/d:>11.1f} {wr:>15.5f} {wz:>22.3f}")
```

Output:
```
GLOBAL sum, n<=x=5999998: 552  ratio/x=+0.000092  (1/sqrt(x)=0.000408)

      d   count/bkt  worst|sum|/cnt   worst|sum|/sqrt(cnt)
      2   2999999.0         0.00053                  0.909
      3   1999999.3         0.00041                  0.583
      7    857142.6         0.00219                  2.025
     13    461538.3         0.00213                  1.445
    210     28571.4         0.01603                  2.710
   2310      2597.4         0.07047                  3.591
  10000       600.0         0.15333                  3.756
  30030       199.8         0.29648                  4.182
 100000        60.0         0.56667                  4.389
```

**Confirmed, independently of N.** Worst `|bucket sum|/count` blows up monotonically
(`0.0005 → 0.57` as `d → 10⁵`) while the z-score stays bounded (`0.6 → 4.4`). The
blow-up is the noise floor `√count/count = 1/√count`, not a parity anomaly. So the
"every Type-I bucket is invisible ⟹ every signed combination is invisible **uniformly in
`d`**" step is **not** witnessed by finite computation — it *is* the open conjecture. The
Pass F audit is sound.

### Verification V-G — re-run Pass G at a different fixed modulus (d=11, DISTILL used d=7)

```python
import math
def build(N):
    Om=[0]*(N+1); is_comp=[False]*(N+1)
    for p in range(2,N+1):
        if not is_comp[p]:
            pk=p
            while pk<=N:
                for m in range(pk,N+1,pk): Om[m]+=1
                pk*=p
            for m in range(2*p,N+1,p): is_comp[m]=True
    return Om
def carrier(Om,n): return (1 if Om[n]%2==0 else -1)*(1 if Om[n+2]%2==0 else -1)
d=11
for N in [250_000,500_000,1_000_000,2_000_000,4_000_000]:
    Om=build(N); x=N-2
    sums=[0]*d; cnts=[0]*d
    for n in range(1,x+1):
        r=n%d; sums[r]+=carrier(Om,n); cnts[r]+=1
    wr=max(abs(sums[a])/cnts[a] for a in range(d) if cnts[a])
    wz=max(abs(sums[a])/math.sqrt(cnts[a]) for a in range(d) if cnts[a])
    print(f"{x:>10} {x/d:>10.0f} {wr:>15.5f} {wz:>22.3f}")
```

Output:
```
    249998      22727         0.01052                  1.585
    499998      45454         0.01118                  2.383
    999998      90909         0.00624                  1.881
   1999998     181818         0.00477                  2.036
   3999998     363636         0.00426                  2.567
```

**Confirmed.** At fixed `d=11` the ratio shrinks (`0.0105 → 0.0043`) while the z-score
stays bounded (`~1.6–2.6`): genuine √-cancellation, no anomalous bias. So `o(count)`
holds *pointwise in `d`*; the **only** open part is **uniformity as `d → D`**. The Pass G
audit is sound, and it correctly isolates exactly which sub-statement is open.

### Verification V-lit — the three load-bearing literature facts (primary sources, this step)

1. **Unaveraged two-point Chowla is OPEN; only the log-averaged form is a theorem.**
   Confirmed against Tao, *The logarithmically averaged Chowla and Elliott conjectures
   for two-point correlations* (arXiv:1509.05422; Forum Math. Pi 4 (2016)) and Tao's blog
   (2015-09-18). The paper proves `∑_{x/ω<n≤x} λ(a₁n+b₁)λ(a₂n+b₂)/n = o(log ω)`
   (logarithmically averaged). The **unaveraged** statement `∑_{n≤x}
   λ(a₁n+b₁)λ(a₂n+b₂) = o(x)` (`a₁b₂−a₂b₁≠0`) — for which the shift-2 carrier
   `λ(n)λ(n+2)` is the case `(a₁,b₁,a₂,b₂)=(1,0,1,2)` — **"remains an open problem."**
   This is C5 sub-fact (i): C5 stays OPEN.

2. **Sawin–Shusterman (TPC over `F_q[t]`) is a geometric escape with no `Z`-transfer.**
   Confirmed against Sawin–Shusterman, *On the Chowla and twin primes conjectures over
   `F_q[T]`* (arXiv:1808.04001; Ann. of Math. 196 (2022)). They obtain a **level of
   distribution beyond 1/2** for irreducible polynomials via a **function-field variant
   of Fouvry–Michel exponential-sum / Möbius-cancellation results** — i.e. a Type-II /
   geometric (monodromy, Riemann-hypothesis-over-finite-fields) input, in the regime
   `q → ∞` at fixed degree. This input has **no `Z`-analogue**. So it is a genuine escape
   over `F_q[t]`, not a `Z`-theorem; its missing `Z`-side ingredient is exactly a
   Type-II shift-2 correlation bound — a sibling of, and subsumed by, C5 (see Review §1).

3. **Tao–Teräväinen / Matomäki–Radziwiłł yield almost-all-shifts or conditional
   results, NOT the unconditional fixed shift 2.** Confirmed: the Matomäki–Radziwiłł /
   Tao–Teräväinen program proves correlation structure "at almost all scales" and
   Hardy–Littlewood-type conclusions for *almost all* shifts; the *fixed*-shift
   Hardy–Littlewood–Chowla statement is obtained only under a **Siegel-zero** hypothesis
   (Tao–Teräväinen 2021, "in the presence of a Siegel zero" — an explicitly hypothetical
   "illusory universe"). No unconditional fixed-shift-2 theorem at TPC strength. This is
   C5 sub-fact (iii) / Pass I: the forward direction for `n(n+2)` is not a cited theorem.

### Labels assigned (independently, with the audit behind each)

| Node | Label | Why (verified) |
|---|---|---|
| **C1 — membership half** | **NEEDS-REVIEW** | Carries a real audit: the closure-under-subtraction **linear algebra** (Chen's split `= Σ_d c_d T_d`, mixed-sign `c_d`, all `d ≤ z₁z < D`; leading-order values form a vector space closed under subtraction; Maynard/Selberg/GPY weights inspected). This is genuine bookkeeping a human can certify. → CLAIM GRAPH. |
| **C1 — parity-blocking half** | **FALSIFIED-as-an-audit** → `[C1-parity-blocked-as-audited]` | The reading that C1's comprehensiveness *closes* the parity-blocking question. The bucket-ratio corroboration was a small-`d` artifact (V-F: `0.0005 → 0.57` as `d → 10⁵`); the "hence the class cannot succeed, uniformly in `d`" half is **not** independently established — its content is exactly C5. → GRAVEYARD, with pointer that its content is absorbed into C5. |
| **C2** (Bombieri/FI dim-1 parity) | **ESTABLISHED-IN-LITERATURE** | Real theorem; scope note **verified present and correct**: hypothesis is sieve dimension **exactly 1**; the converse "Type-II ⟹ π₂(n(n+2))→∞" is NOT among it (heuristic at dim 2). → CLAIM GRAPH. |
| **C3** (Type-II escapes: FI `a²+b⁴`, H-B `x³+2y³`; none for `n(n+2)`) | **ESTABLISHED-IN-LITERATURE** | Real theorems; scope note **verified**: these are dimension-1 / thin-sequence escapes; they do NOT prove "Type-II input ⟹ π₂(n(n+2))→∞" (Pass I). → CLAIM GRAPH. |
| **C4** (Polymath8b GEH barrier) | **ESTABLISHED-IN-LITERATURE (re-scoped)** | Method-/GEH-specific statement verified verbatim; no GRH/EH/GEH leak (Pass J). → CLAIM GRAPH. Absolute over-reading stays in GRAVEYARD. |
| **C5 — THE CRUX** | **OPEN** | No audit closes it (V-lit #1: unaveraged shift-2 Chowla is open). Now explicitly carries **both** directions — necessity (no signed Type-I functional certifies π₂→∞) and sufficiency (a shift-2 λ-correlation input yields π₂→∞) — **both unproven**, both reducing to the **same single object**. → CLAIM GRAPH as the named OPEN crux; may NOT be built upon. |

I explicitly resisted any temptation to upgrade C5 to NEEDS-REVIEW or to manufacture a
two-sided "closed" reduction. Per the anti-deception rule, a no-audit node is OPEN; the
session-ending pressure to "resolve" it is exactly the signal to keep it OPEN. C5 is
OPEN, by design, and that is the result.

---

## Part 2 — ACTION: **Review**

I re-survey the entire tracking sheet for missed angles and misclassifications before any
terminal judgment, addressing the three required checks head-on.

### Review §1 — Missed-angle audit: does ANY unconditional approach escape C1-membership *without* needing C5?

For each candidate family I state precisely whether it (i) **lands inside class C1**
(governed by C5), or (ii) is a **genuine escape** but currently lacks a theorem at TPC
strength (and whether its open input is C5 or a distinct sibling).

- **A1–A7 (Brun, Selberg, Chen, GPY, Zhang, Maynard–Tao, Polymath8):** all **inside
  C1-membership** (signed linear functionals of Type-I data; Chen via the audited
  closed-under-subtraction argument). Governed by C5. No escape. *(C1-membership audit.)*

- **A8 (circle method + `k`-tuple):** the singular series is the *source* of the
  conjectural constant, but the minor arcs are uncontrollable for a binary problem (same
  wall as binary Goldbach). It does not produce an unconditional lower bound; it is **not
  an escape** — it has no theorem at TPC strength and its missing input is, if anything,
  *stronger* than C5 (full HL). Not a sibling that bypasses C5.

- **A9 (EH / GEH):** Type-I inputs. **Inside C1**; C5 stays open even under GEH (Pass J).
  No escape.

- **A10 (Matomäki–Radziwiłł, Tao log-Chowla, Tao–Teräväinen) — the parity-breaking
  correlation family.** This is the one family **not in-principle parity-blocked** — it
  supplies the Type-II cancellation a sieve cannot. **Verdict: genuine escape in
  principle, but its unconditional fixed-shift-2 theorem does not exist (V-lit #3).** The
  proven results are *logarithmically averaged* (Tao 2015), *almost-all-shifts*
  (Tao–Teräväinen), or *conditional on a Siegel zero* (2021). The **specific missing
  input is the unaveraged two-point correlation at the fixed shift 2, uniform in APs —
  which is C5 itself** (its global, `d=1` case is C5 sub-fact (i); the uniform-in-`d`
  case is C5 in full). **So A10 does not bypass C5 — it is the home of C5.**

- **A11a (automorphic / spectral shifted-convolution).** These control shifted
  convolution sums `∑ a(n)b(n+h)` for automorphic coefficients and **strengthen Type-I
  inputs** (larger level of distribution, GL(2) spectral input); they do **not** supply a
  sign-sensitive bilinear estimate for `λ(n)λ(n+2)`. **Verdict: inside the input-side of
  C1** (they feed `T_d`), not an escape. No unconditional fixed-shift-2 λ-correlation
  emerges. Governed by C5.

- **A11b (additive combinatorics / Green–Tao ergodic).** Produce **long arithmetic
  progressions** of primes, not a **fixed gap**; the transference/`W`-trick machinery
  detects structure in dense or relative-dense sets but cannot pin the gap to 2.
  **Verdict: DEAD END for TPC**, not an escape; no relevant open input that is a C5
  sibling. (The Gowers-norm control of the primes is exactly the Type-I-side input the
  parity barrier is orthogonal to.)

- **A11c (function field, Sawin–Shusterman).** TPC is **proved over `F_q[t]`**. **Verdict:
  genuine escape *in that setting*, with NO theorem over `Z` at TPC strength (V-lit #2).**
  The proof's engine is a geometric/monodromy level-of-distribution-beyond-1/2 input
  (function-field Fouvry–Michel) with no `Z`-analogue. Its missing `Z`-side ingredient is
  a Type-II shift-2 correlation / level-beyond-1/2 cancellation — **the same class of
  object as C5** (a parity-breaking bilinear input for `n(n+2)`). It is **not a distinct
  sibling OPEN node**: over `Z` it reduces to "produce the parity-breaking shift-2 input,"
  i.e. C5. (Thread T4 — characterising the obstruction precisely by its *absence* in
  `F_q[t]` — remains a refinement one *could* pursue, but it does not surface a *new* open
  object; it re-describes C5 geometrically.)

**Conclusion of §1.** **No known unconditional approach escapes the pair
{C1-membership, C5}.** Every family either (a) lands inside C1-membership and is governed
by C5, or (b) is a genuine escape (A10 correlation family; A11c function field) whose
*missing* input over `Z` is the **same object as C5** — an unaveraged / level-beyond-1/2
parity-breaking shift-2 correlation. **No distinct sibling OPEN node exists.** C5 really
is the single crux.

### Review §2 — Completeness of the reduction: which implications are rigorous?

I state this plainly and do **not** overstate.

**Rigorous (proven) content of the graph:**
- C1-membership: the named methods are signed linear functionals of Type-I data; the
  class is closed under subtraction. *(Linear algebra; human-certifiable.)*
- C2: the **dimension-1** Bombieri/FI asymptotic-sieve parity dichotomy. *(Theorem.)*
- C3: the **dimension-1 / thin-sequence** Type-II escapes (FI `a²+b⁴`, H-B `x³+2y³`).
  *(Theorems.)*
- C4: the **method-/GEH-specific** Polymath8b barrier `H₁≤6`. *(Theorem, re-scoped.)*

**NOT rigorous (heuristic) — both directions of the reduction itself:**
- **Necessity** ("TPC-via-this-class is *impossible*, i.e. no signed Type-I functional
  certifies π₂→∞ for `n(n+2)`"): rests on statement (iii), which is neither the heuristic
  (i) (Möbius pseudorandomness *principle*) nor the dimension-1 theorem (ii)
  (Bombieri/FI). Its quantitative form **is** the uniform shift-2 cancellation = C5.
  **Open.**
- **Sufficiency** ("(established sieve machinery) + C5 ⟹ TPC"): **also not a cited
  theorem at dimension 2** (Pass I; V-lit #3). FI's "Type-II ⟹ primes" is dimension-1;
  `a²+b⁴` / `x³+2y³` have no `n(n+2)` analogue.

**Therefore: "TPC ⟸ (established sieve machinery) + C5" is NOT a proven reduction.** Even
the sufficiency direction is heuristic at dimension 2. The honest terminal claim is thus
**not** "C5 would finish the proof" but rather:

> **The difficulty of TPC *concentrates on* C5** — the unaveraged shift-2 two-point
> λ-correlation, uniform in APs. Both the claim that C5 is *necessary* (no Type-I
> functional can avoid it) and the claim that C5 is *sufficient* (it would, with standard
> sieving, yield TPC) are the standard expectation but are **themselves unproven** at
> dimension 2; each reduces to the *same* open arithmetic object as C5.

This is the maximally honest statement and is exactly what terminal state (3) is meant to
capture: the wall located precisely, with no overclaim that crossing it is *known* to
finish the proof.

### Review §3 — Sharpest form of C5 (single precise sentence, exact quantifiers)

> **C5 (the crux).** There exist `ε>0` and a function `δ(x)→0` such that
> `| ∑_{n≤x, n≡a (mod d)} λ(n)λ(n+2) | ≤ δ(x)·(x/d)` **for all** `d ≤ x^{1−ε}` and **all**
> residues `a (mod d)`, where `λ=(-1)^Ω` is the Liouville function — i.e. the **unaveraged
> two-point Chowla/Liouville correlation at shift 2, with `o(x/d)` cancellation uniform
> over arithmetic progressions to modulus `d ≤ x^{1−ε}`.** *(Only the logarithmically
> averaged form is a theorem — Tao 2015. The `d=1` special case
> `∑_{n≤x} λ(n)λ(n+2)=o(x)` is itself open.)*

The uniformity quantifier `∀ d ≤ x^{1−ε}` is the load-bearing part: V-F shows finite `x`
*always* has a `d` with ratio `Θ(1)`, so this statement is unwitnessable by computation —
it is genuinely the open conjecture, not a numerical gap.

### Review §4 — misclassification sweep

- L1-as-written: correctly FALSIFIED (concrete counterexample, Pass A; re-verified prior
  step). Stays GRAVEYARD.
- `[C4-absolute]`, `[dim-2-necessity-as-theorem]`: correctly in GRAVEYARD (over-readings;
  the latter re-confirmed by Pass H — `(ii)→(iii)` is not rigorous). No reclassification.
- `[C1-parity-blocked-as-audited]`: newly added this step (FALSIFIED-as-an-audit);
  correctly placed, content absorbed into C5.
- No node is misclassified as ESTABLISHED that is actually conditional/heuristic: C2/C3
  carry their dimension-1 / thin-sequence scope notes; C4 carries its method-/GEH-specific
  scope; the heuristic reduction directions are *inside C5* (OPEN), not asserted as
  theorems anywhere in the graph. No circularity: C1-membership depends on nothing; C2/C3
  on established mathematics; C4 on C2; C5 names C1/C2/C3 only as framing and is the sole
  OPEN node. No node depends on C5.

---

## Part 3 — TERMINAL JUDGMENT: terminal state (3) reached

After this Review I judge the graph **clean, minimal, and audited**:

- every node is **ESTABLISHED-IN-LITERATURE** (C2, C3, C4) or **NEEDS-REVIEW with a real
  audit** (C1-membership) **except the single OPEN crux C5**;
- **no circularity** (no node depends on C5; C4→C2 is the only internal edge and is
  sound);
- **no missed escaping angle** (Review §1: every approach is inside C1-membership and
  governed by C5, or is a genuine escape whose missing `Z`-input is the *same object* as
  C5; no distinct sibling OPEN node exists);
- the reduction's **rigour is stated honestly** (Review §2: both necessity and
  sufficiency are heuristic at dim 2; the honest claim is "the difficulty *concentrates
  on* C5," not "C5 finishes the proof");
- C5 is stated as **one precise sentence with exact quantifiers** (Review §3).

I therefore **declare terminal state (3)**. The complete final Claim Graph is the
deliverable below. I am **not** manufacturing a proof or a fake NEEDS-REVIEW: locating
this wall precisely *is* the success defined by `AGENTS.md`.

### The crux, in one sentence

> **The entire difficulty of the Twin Prime Conjecture, reached through every
> unconditional sieve- and correlation-theoretic route, concentrates on a single open
> arithmetic statement (C5): that `∑_{n≤x, n≡a (d)} λ(n)λ(n+2) = o(x/d)` holds *uniformly*
> over all moduli `d ≤ x^{1−ε}` and residues `a` — the unaveraged two-point Chowla/
> Liouville correlation at shift 2 in arithmetic progressions, of which only the
> logarithmically averaged form is presently a theorem (Tao 2015).**

### What was and was not achieved (closing statement)

**Achieved.** A minimal, audited claim graph that localises TPC to one sharp open object.
We established (with checkable audits) that the standard sieve toolkit — Brun, Selberg,
GPY, Zhang, Maynard–Tao, and **Chen via the closed-under-subtraction linear-algebra
argument** — consists of signed linear functionals of the same Type-I data
(C1-membership, NEEDS-REVIEW); that the genuine parity dichotomy is a **dimension-1**
theorem (C2) and the genuine Type-II escapes are **dimension-1 / thin-sequence** theorems
with no `n(n+2)` analogue (C3); and that the Polymath8b `H₁≤6` barrier is **method- and
GEH-specific** (C4). We then showed — and this is the decisive structural finding of the
run — that the *necessity* direction ("sieves can't do it") is **not** an established
theorem but the **same open object** as the missing Type-II input, removing a hidden
circular dependency and collapsing the graph onto a single OPEN crux, C5.

**Not achieved (and honestly marked).** We did **not** prove TPC, and we did **not**
prove the reduction "machinery + C5 ⟹ TPC": *both* the necessity and the sufficiency
directions across C5 are heuristic at dimension 2, so the rigorous claim is that the
difficulty **concentrates on** C5, not that C5 would **finish** the proof. C5 itself is
OPEN — only its logarithmically averaged shadow is a theorem — and no unconditional
approach surveyed (sieve, circle method, automorphic shifted-convolution, additive
combinatorics, the Matomäki–Radziwiłł/Tao–Teräväinen correlation program, or the
function-field proof of Sawin–Shusterman) escapes it: each either lands inside the Type-I
functional class governed by C5, or is a genuine escape whose missing input over `Z` is
the same shift-2 parity-breaking correlation. This is the wall, stated as precisely as the
literature allows.

---

## TRACKING SHEET (full snapshot as of this step)

```
APPROACHES ATTEMPTED:
- [A1] Classical combinatorial sieve (Brun) | DEAD END (route to full TPC) | upper bound
  pi_2(x) << x/(log x)^2; no lower bound; collapses at dimension kappa=2 past sqrt(x).
  Inside class C1 (membership). Governed by C5.
- [A2] Selberg Lambda^2 sieve + parity principle | PROMISING (as the obstruction to STATE,
  not a route) | sharp bounds + the parity phenomenon explaining why A1/A3 stall; the
  terminal OPEN node (C5), not a path. Inside C1.
- [A3] Chen (P_2) | DEAD END (full TPC) | infinitely many p with p+2 a P_2; residual
  P_2->P_1 gap IS parity. Chen's subtracted-upper-bound weighted sieve is a single
  mixed-sign linear functional of Type-I data => INSIDE C1 (MEMBERSHIP, audited; closure
  under subtraction). Membership does NOT by itself prove parity-blocked (that is C5).
- [A4] GPY | DEAD END (full TPC) / ACTIVE (bounded gaps) | liminf gap/log = 0; bounded
  gaps under EH; never the fixed gap 2. Inside C1.
- [A5] Zhang 2013 | DEAD END (full TPC) | unconditional liminf < 7e7 (smooth moduli + GPY);
  cannot reach gap 2 (parity). Inside C1.
- [A6] Maynard-Tao | DEAD END (full TPC) | liminf <= 600, all m; cannot say WHICH two
  entries are prime. Nonneg divisor-sum-squared, Type-I inputs => inside C1.
- [A7] Polymath8 (8a/8b) | DEAD END (full TPC) | <=246, <=12 (EH), <=6 (GEH). Barrier is
  METHOD-specific + GEH-specific (= C4, re-scoped ESTABLISHED). No GRH/EH leak (Pass J).
- [A8] Hardy-Littlewood circle method + prime k-tuple | DEAD END (full TPC, unconditional)
  | source of the conjectural constant; minor arcs uncontrollable for a binary problem.
  NOT an escape (no theorem; missing input is stronger than C5). Same wall as binary
  Goldbach.
- [A9] EH / GEH as conditional input | DEAD END (full TPC even if assumed) / ACTIVE
  (bounded gaps) | does NOT break parity; EH/GEH are Type-I. C5 stays OPEN even under GEH
  (Pass J). Inside C1.
- [A10] Chowla/Liouville-correlation, parity-breaking (Matomaki-Radziwill, Tao log-Chowla,
  Tao-Teravainen) | PROMISING / ACTIVE (frontier) | the ONLY family not in-principle
  parity-blocked; supplies the Type-II cancellation a sieve cannot. GENUINE ESCAPE IN
  PRINCIPLE, but its unconditional FIXED-shift-2 theorem does not exist: proven results
  are LOG-AVERAGED (Tao 2015, arXiv:1509.05422), ALMOST-ALL-shifts (Tao-Teravainen), or
  conditional on a Siegel zero (Tao-Teravainen 2021). Its missing input = C5 itself.
  HOME of crux C5; does NOT bypass it.
- [A11] Other escapes/non-escapes:
  (a) automorphic/spectral shifted-convolution -> STRENGTHEN Type-I inputs; inside the
      input-side of C1; no sign-sensitive bilinear for lambda(n)lambda(n+2). Governed by C5.
  (b) additive-combinatorial / Green-Tao ergodic -> long APs, NOT a fixed gap; DEAD END for
      TPC; no C5-sibling open input.
  (c) function field Sawin-Shusterman -> TPC PROVED over F_q[t] via a geometric/monodromy
      level-beyond-1/2 input (function-field Fouvry-Michel) with NO Z-analogue. GENUINE
      ESCAPE over F_q[t], no Z-theorem at TPC strength; its missing Z-input is a Type-II
      shift-2 correlation = same object as C5 (not a distinct sibling). (Thread T4 refines
      this geometrically but surfaces no new open object.)
  Friedlander-Iwaniec (a^2+b^4) & Heath-Brown (x^3+2y^3) are the Type-II ESCAPES counting a
  fixed prime form (DIMENSION 1 / thin sequences), no n(n+2) analogue = Claim C3.

CANDIDATE LEMMAS (pre-distill — not yet certified):
- (none) — passes F-J have been LABELLED this step; the graph is at terminal state (3).

OPEN THREADS:
- T2 [= the crux C5, OPEN — TERMINAL]: the unaveraged two-point shift-2 cancellation
  Sum_{n<=x, n==a(d)} lambda(n)lambda(n+2)=o(x/d) UNIFORM over d<=x^{1-eps} (= unaveraged
  two-point Chowla at shift 2 in APs). BOTH directions across it (necessity:
  no signed Type-I functional certifies pi_2->inf; sufficiency: this input + sieve =>
  pi_2->inf) are unproven at dimension 2. Only the log-averaged form is a theorem (Tao
  2015). This thread is the located wall, not a lead to chase.
- T4 [optional refinement, not required for terminal state]: characterize the obstruction
  by its ABSENCE in F_q[t] (Sawin-Shusterman's geometric/monodromy Type-II input).
  Review §1 confirms this re-describes C5 geometrically; it does NOT surface a new open
  object. May be pursued by a human but is not a gap in the graph.
- [T3 CLOSED] C1 Chen-subtraction MEMBERSHIP resolved: subtracted-upper-bound sieves are
  single mixed-sign linear functionals of Type-I data, closed under subtraction => inside
  C1's membership half. (Parity-blocked is a SEPARATE claim = C5.)
- [T5 CLOSED] "Is necessity an established theorem or the same open object as C5?" SETTLED:
  the SAME OPEN OBJECT as C5 (Pass F/G/H). (i) "sieves can't break parity" is a HEURISTIC
  (Mobius pseudorandomness); (ii) Bombieri's theorem is DIMENSION-1 only; the n(n+2)
  statement (iii) is neither, its quantitative form IS C5. Hidden circular dependency
  REMOVED by demoting necessity into C5.

CURRENT POSITION:
- TERMINAL STATE (3) DECLARED. Passes F-J labelled (independently re-verified: V-F re-ran
  Pass F at N=6e6 — worst bucket-ratio 0.0005->0.57 as d->1e5, z-score bounded; V-G re-ran
  Pass G at d=11 — genuine sqrt-cancellation pointwise, only uniformity open; V-lit
  confirmed the three load-bearing literature facts). C1 is SPLIT: membership half =>
  NEEDS-REVIEW (audited linear algebra); parity-blocking half => GRAVEYARD
  (FALSIFIED-as-an-audit), content absorbed into C5. C2/C3 ESTABLISHED with verified
  dimension-1/thin-sequence scope notes; C4 ESTABLISHED (re-scoped). C5 is the SINGLE OPEN
  crux carrying BOTH directions (necessity AND sufficiency-for-n(n+2)), BOTH unproven,
  BOTH reducing to one object: the unaveraged shift-2 two-point Chowla in APs uniform over
  d<=x^{1-eps}. Review confirmed: NO unconditional approach escapes {C1-membership, C5} —
  every family is inside C1 (governed by C5) or a genuine escape (A10, A11c) whose missing
  Z-input is the SAME object as C5; no sibling OPEN node. The reduction itself
  ("machinery + C5 => TPC") is NOT rigorous at dim 2 — honest claim: the difficulty
  CONCENTRATES ON C5, not that C5 finishes the proof.
- Legal foundations: C1-membership/C2/C3/C4 (NEEDS-REVIEW/ESTABLISHED). C5 may be NAMED as
  the obstruction, NOT built upon. GRAVEYARD nodes may NOT be built upon.

CLAIM GRAPH (post-distill, LABELLED; the ONLY nodes that may be built upon are
C1-membership/C2/C3/C4; C5 is the named OPEN crux and may NOT be built upon):
- [C1] CLASS DEFINITION + MEMBERSHIP (parity-blocking content REMOVED -> see C5 / GRAVEYARD).
  A "Type-I sieve scheme" for F(n)=n(n+2) is a lower bound pi_2(x) >= L(a), L a (possibly
  SIGNED) LINEAR FUNCTIONAL of the Type-I data {T_d = sum_{n<=x, d|F(n)} a_n : d<=D=x^{1-eps}},
  Type-I inputs only (BV/EH/GEH/Zhang). MEMBERSHIP (surviving checkable content): Brun,
  Selberg, GPY, Maynard-Tao (nonneg divisor-sum-squared) AND Chen / Buchstab-iterated
  SUBTRACTED-upper-bound sieves are ALL signed linear functionals of the T_d (Chen:
  S(A;z) - 1/2 sum_{z<=p<z_1} S(A_p;z) - rem = sum_d c_d T_d, mixed-sign c_d, all
  d<=z_1*z<D; leading-order values form a VECTOR SPACE closed under subtraction). Asserts
  ONLY membership; does NOT assert the class is parity-blocked (that is C5).
  DEPENDS ON: nothing (definition + membership bookkeeping).
  STATUS: NEEDS-REVIEW (membership half).
  AUDIT: (a) inspect each method's weight (Maynard, Ann. of Math. 181 (2015), Sec.4-5;
  Selberg); (b) Chen split = sum_d c_d T_d, all d<=z_1*z<D (Halberstam-Richert ch.11;
  arXiv:math/0412220); (c) closed-under-subtraction is sound LINEAR ALGEBRA — human
  certifies the pd<=D bookkeeping + closure. SCOPE NOTE: audits MEMBERSHIP only;
  "...therefore parity-blocked" requires each T_d perturbed by o(main) UNIFORMLY over
  d<=D under lambda(n)lambda(n+2) — NOT established here, it is C5.
  ADVERSARIAL PASS: Pass F/G/H. Membership linear algebra SURVIVES; the implicit "hence
  parity-blocked" did NOT (its input is the open shift-2 Chowla bound) and is merged into
  C5 / GRAVEYARD.
  source: work/1781354707-DISTILL.md (Pass F/G/H) + work/1781354180-EXPLORE.md (Part 2)
  + work/1781355529-EXPLORE.md (V-F/V-G, label).
- [C2] BOMBIERI ASYMPTOTIC SIEVE / PARITY (dimension EXACTLY 1). For a sequence of sieve
  dimension exactly 1 on a Type-I EH-level hypothesis, Type-I data determines Lambda_k
  (k>=2) but provably NOT Lambda_1 (primes) without a Type-II bilinear estimate; explicit
  density-matched two-sequence construction.
  DEPENDS ON: established mathematics. | STATUS: ESTABLISHED-IN-LITERATURE.
  AUDIT: Bombieri, "The asymptotic sieve," Rend. Accad. Naz. XL (5) 1/2 (1975/76);
  Friedlander-Iwaniec, "Asymptotic sieve for primes," Ann. of Math. 148 (1998) (adds a
  BILINEAR-FORM hypothesis to break parity; companion does a^2+b^4); Tao, "Notes on the
  Bombieri asymptotic sieve" (2016): H-product converges to a nonzero constant IFF sieve
  dimension = 1. SCOPE NOTE (verified present/correct): hypothesis is sieve dimension
  EXACTLY 1; the converse "Type-II input => pi_2(n(n+2))->inf" is NOT among it (heuristic
  at dimension 2). ADVERSARIAL PASS: Pass H — SURVIVES as a DIMENSION-1 theorem; does NOT
  extend to dim-2 n(n+2) ((ii)->(iii) is the "a fortiori dim 2" heuristic, GRAVEYARD).
  Supports C5's framing without establishing dim-2 necessity.
  source: work/1781353616-DISTILL.md (Pass C/E) + work/1781354707-DISTILL.md (Pass H).
- [C3] TYPE-II SUFFICES / IS THE KNOWN ESCAPE (dimension-1 / thin sequences). A
  sign-sensitive bilinear (Type-II) estimate breaks parity and counts a fixed prime form
  (FI a^2+b^4; Heath-Brown x^3+2y^3); none known for n(n+2). These sit OUTSIDE class C1.
  DEPENDS ON: established mathematics. | STATUS: ESTABLISHED-IN-LITERATURE.
  AUDIT: Friedlander-Iwaniec, "The polynomial X^2+Y^4 captures its primes," Ann. of Math.
  148 (1998); Heath-Brown, "Primes represented by x^3+2y^3," Acta Math. 186 (2001). The
  "none for n(n+2)" half is an absence-of-citation survey fact (human-verify). SCOPE NOTE
  (verified): these prove only that Type-II CAN count a fixed prime form for SPECIFIC thin
  sequences; they do NOT prove "Type-II => pi_2(n(n+2))->inf" (dimension 2, no analogue).
  ADVERSARIAL PASS: Pass I — SURVIVES as a SUFFICIENCY EXAMPLE; the forward direction of C5
  is therefore heuristic, not established.
  source: work/1781353616-DISTILL.md (Pass E) + work/1781354707-DISTILL.md (Pass I).
- [C4] POLYMATH8b GEH BARRIER (method-specific, GEH-specific). Under GEH the
  multidimensional first-moment (Maynard-Tao) sieve gives H_1<=6, and the Selberg parity
  obstruction prevents THIS method from H_1<6 under GEH; under plain EH not even excluded.
  DEPENDS ON: established mathematics; C2 (parity obstruction instantiated). | STATUS:
  ESTABLISHED-IN-LITERATURE (re-scoped). AUDIT: D.H.J. Polymath, Res. Math. Sci. 1 (2014)
  art.12 (arXiv:1407.4897) — verified verbatim: H_1<=6 under GEH is "best possible FROM
  SIEVE-THEORETIC METHODS thanks to the parity problem"; cannot improve "without going
  beyond sieve theory itself"; retrospective arXiv:1409.8361. ADVERSARIAL PASS: Pass J —
  no hidden GRH/EH leak; GEH does NOT break the barrier. Method-/GEH-specific reading
  SURVIVES; absolute reading stays in GRAVEYARD.
  source: work/1781353616-DISTILL.md (Pass D) + work/1781354707-DISTILL.md (Pass J).
- [C5] *** THE CRUX (single OPEN claim — may be NAMED as the obstruction, NOT built upon).***
  Absorbs the necessity content stripped from C1. The single missing object is an
  UNAVERAGED TWO-POINT shift-2 cancellation for lambda(F(n))=lambda(n)lambda(n+2):
    EXISTS eps>0, delta(x)->0 s.t. |sum_{n<=x, n==a (mod d)} lambda(n)lambda(n+2)| <=
    delta(x)*(x/d) FOR ALL d <= x^{1-eps} AND ALL residues a (mod d)
  (= unaveraged two-point Chowla/Liouville at shift 2, uniform in APs), o(main) strength.
  TWO directions cross it, BOTH OPEN for n(n+2):
    (Necessity) "No signed linear functional of Type-I data (class C1, incl. subtracted
      sieves) certifies pi_2(x)->inf." NOT an established theorem: rests on statement (iii),
      which is neither heuristic (i) (Mobius pseudorandomness PRINCIPLE) nor dim-1 theorem
      (ii) (Bombieri/FI); its quantitative form IS the uniform cancellation above. C1's
      "comprehensiveness proves the wall" reduces to THIS — same open object.
    (Sufficiency) "This input + standard sieve => pi_2(n(n+2))->inf." Also NOT cited for
      n(n+2) at dimension 2 (Pass I; Tao-Teravainen give almost-all/Siegel-conditional, not
      fixed shift 2).
  DEPENDS ON: C1 (membership — frames which functionals are in scope), C2 (dim-1 model), C3
  (known Type-II escape). NOT a proven equivalence to TPC-via-this-class in either direction.
  STATUS: OPEN. AUDIT: NONE EXISTS (the point). Rigorous sub-facts, all verified:
  (i) unaveraged two-point Chowla sum lambda(n)lambda(n+2)=o(x) is OPEN — only log-averaged
      proven (Tao arXiv:1509.05422, Forum Math. Pi 4 (2016) — "remains an open problem";
      Pilatte arXiv:2310.19357 only log-averaged). Numerics: global ratio ~9e-5..3.5e-4 . x
      (< 1/sqrt(x)) at N=6-8e6; per-bucket sqrt-cancellation confirmed (Pass G / V-G) but
      worst bucket-ratio BLOWS UP 0.0005->0.57 as d->1e5 (Pass F / V-F), so the UNIFORM-in-d
      statement is unwitnessable by finite x = the open conjecture.
  (ii) the parity obstruction for n(n+2) is a HEURISTIC principle, not a theorem (Mobius
      pseudorandomness, Tao 2007/2014); Bombieri's actual THEOREM is dimension-1 only (Tao
      2016); n(n+2) is dimension 2.
  (iii) the forward/sufficiency implication for n(n+2) is not a cited theorem (Pass I;
      Tao-Teravainen almost-all/Siegel-conditional only). To upgrade C5: EITHER prove the
      uniform unaveraged shift-2 cancellation (major theorem, plausibly yielding TPC) OR a
      formal dim-2 impossibility theorem (NOT in the literature).
  ADVERSARIAL PASS: Pass F/G/H/I/J — every attack converged here; necessity is the SAME open
  object as C5 (hidden circularity removed); robust to GRH/EH/GEH (Pass J). STAYS OPEN with
  BOTH directions unproven.
  source: work/1781354707-DISTILL.md (all passes) + work/1781353616-DISTILL.md (all passes)
  + work/1781354180-EXPLORE.md (V-B/V-C) + work/1781355529-EXPLORE.md (V-F/V-G/V-lit, Review).

GRAVEYARD (may NOT be built upon):
- [L1-as-written] clause-1's literal involution a_n->(1±lambda(n))a_n/2 with lambda(n)=
  (-1)^Omega(n) ON n is Type-I invariant. FALSIFIED (Pass A): mass ratio 0.66-0.79 (not 1);
  EVERY true twin (n>3) has lambda(n)=-1 => all in b_minus, none in b_plus.
- [C4-absolute] the ABSOLUTE over-reading "no Type-I/no method whatever beats gap 6."
  FALSIFIED-as-stated: literature ties the barrier to "purely SIEVE-THEORETIC methods" and
  says plain EH does NOT exclude better; absolute claim is OPEN (= the open parity question).
- [dim-2-necessity-as-theorem] Type-II PROVABLY NECESSARY for n(n+2) ("a fortiori dim 2").
  OPEN, not a theorem: FI necessity is dimension EXACTLY 1; dim-2 transfer is heuristic.
  CONFIRMED buried (Pass H: (ii)->(iii) not rigorous). Honest content in C5.
- [C1-parity-blocked-as-audited] The reading that C1's comprehensiveness audit CLOSES the
  parity-blocking / necessity question. FALSIFIED-as-an-audit (Pass F/G/H + V-F/V-G): only
  the MEMBERSHIP half is audited; the "hence the class cannot succeed UNIFORMLY in d" half
  is the open object C5 (bucket-ratio corroboration was a small-d artifact, blows up
  0.0005->0.57 as d->1e5). Content ABSORBED INTO C5. Not a legal foundation.
  source: work/1781354707-DISTILL.md (Pass F/G/H) + work/1781355529-EXPLORE.md (V-F/V-G).
```

---

## End-of-step status

- **Labels assigned (independently verified):** C1-membership -> **NEEDS-REVIEW**
  (closed-under-subtraction linear algebra is a real audit); C1-parity-blocking ->
  **FALSIFIED-as-an-audit** -> GRAVEYARD `[C1-parity-blocked-as-audited]`, content
  absorbed into C5; C2, C3 -> **ESTABLISHED-IN-LITERATURE** (dimension-1 / thin-sequence
  scope notes verified present and correct, converse not among them); C4 ->
  **ESTABLISHED-IN-LITERATURE (re-scoped)**; **C5 -> OPEN**, now carrying BOTH directions
  (necessity AND sufficiency), BOTH unproven, BOTH reducing to one object.
- **Action taken:** **Review**. Missed-angle audit (no escape from {C1-membership, C5};
  A10 and the Sawin-Shusterman function-field proof are genuine escapes whose missing
  Z-input is the SAME object as C5; no sibling OPEN node). Completeness: the reduction
  "machinery + C5 => TPC" is NOT rigorous at dim 2 — honest claim is the difficulty
  CONCENTRATES ON C5. Sharpest form of C5 stated with exact quantifiers.
- **Terminal state:** **TERMINAL STATE (3) DECLARED.** The graph is clean, minimal,
  audited, non-circular, with a single OPEN crux C5 and no missed escaping angle. The wall
  is located precisely; per AGENTS.md this is the success, and no proof or fake
  NEEDS-REVIEW is manufactured.
```
