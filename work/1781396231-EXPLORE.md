# EXPLORE — LABEL the L-QUB nodes, then Review (Cluster-5 verdict + the why-L²-fails sharpening + next escalation)

**Mode:** EXPLORE
**Timestamp:** 1781396231
**Branch:** claude/loving-turing-1r5pqz
**Action this step:** first **LABELLING** (mandatory, of the returned DISTILL attack on
candidate lemma L-QUB in `work/1781395447-DISTILL.md`), then **Review** (re-survey the
tracking sheet, state the Cluster-5 verdict plainly, crystallize the sharpened
obstruction, recommend the next escalation). No new mathematics is introduced as a
candidate; this is a labelling + Review step.

**Framework (revised `AGENTS.md`, weighted first).** Exits are exactly
proof / disproof / independence. An obstruction is a **checkpoint to break or
escalate**, not an exit. A rename/relocation is an **escalation to record honestly**,
not progress and not a termination. No-fabrication binds: no checkable audit ⇒ **OPEN**,
never NEEDS-REVIEW; I invent no citations; I do not relabel an unbroken obstruction a
proof. I declare **no terminal state** this step.

---

## PART 0 — Independent re-verification of the decisive audits (I do not rubber-stamp)

The labelling instruction is explicit: re-check the decisive audits myself, do not
rubber-stamp DISTILL. I re-ran the four load-bearing checks with fresh code (different
seeds, independent λ sieve rebuild). All confirm.

### V1 — L-QUB-i: the factorization is the trivial `‖·‖∞` identity (no arithmetic).

Independent run (seed 20260613, arbitrary REAL arrays + arbitrary mass, NOT arithmetic):

```python
import math, random
random.seed(20260613)
allok = True
for trial in range(8):
    d  = random.randint(2, 60)
    S  = [random.uniform(-500, 500) for _ in range(d)]   # arbitrary reals
    xd = random.uniform(0.01, 5000)                       # arbitrary mass x/d
    l2 = math.sqrt(sum(s*s for s in S)/d)                 # ||v||_2 / sqrt(d)
    linf = max(abs(s) for s in S)
    U, A2, R = linf/xd, l2/xd, linf/l2
    ok = math.isclose(U, A2*R, rel_tol=1e-13); allok &= ok
    print(f" d={d:2d} U={U:.8e} A2*R={A2*R:.8e} equal={ok} (R={R:.3f})")
print("ALL hold to 1e-13:", allok)
```
Output (excerpt): every trial `equal=True`; `ALL hold to 1e-13: True`. Examples:
`d=2 U=1.048e-01=A2*R`, `d=51 U=2.692e+01=A2*R`, `d=43 U=9.958e-02=A2*R`.

**Confirmed.** `U_d = A^(2)_d·R_d` is the algebraic identity
`‖v‖_∞ = (‖v‖_2/√d)·(‖v‖_∞/(‖v‖_2/√d))` — the `‖v‖_2/√d` cancels. Holds for *any*
nonzero real vector with *any* mass; no property of λ or arithmetic is used. The "iff"
`C5 ⟺ sup_d A^(2)_d·R_d → 0` is therefore `sup_d U_d → 0 ⟺ sup_d U_d → 0`. The
Grothendieck/Kan/Beck–Chevalley dress is `‖v‖_∞ = ‖v‖_∞` in costume — TRUE but vacuous.

### V2 — L-QUB-ii: the L² factor is the FREE trivial diagonal.

Independent λ-sieve rebuild, `x = 2·10^6`. First the engine: `t(n)² = 1` for all n
(λ never vanishes) ⇒ the diagonal `Σ_n t(n)² = x` is exact.

```python
bad = sum(1 for n in range(1, x+1) if t(n)*t(n) != 1)   # -> 0
```
`#n≤x with t(n)²≠1 : 0`. Then the variance split `Σ_a S_{a,d}² = x + off-diagonal`:

```
      d |    Sum_a S^2 |  diag(=x) |   offdiag |  off/diag | A2_actual | sqrt(d/x) | reldiff
      2 |      1255888 |   2000000 |   -744112 |   -0.3721 |   0.00079 |   0.00100 |  20.76%
      6 |      1709424 |   2000000 |   -290576 |   -0.1453 |   0.00160 |   0.00173 |   7.55%
     30 |      1909792 |   2000000 |    -90208 |   -0.0451 |   0.00378 |   0.00387 |   2.28%
    210 |      2129100 |   2000000 |    129100 |   +0.0645 |   0.01057 |   0.01025 |   3.18%
   2310 |      2040592 |   2000000 |     40592 |   +0.0203 |   0.03433 |   0.03399 |   1.01%
  30030 |      2009152 |   2000000 |      9152 |   +0.0046 |   0.12282 |   0.12254 |   0.23%
```

**Confirmed**, and I add a sharpening DISTILL did not stress: the off-diagonal is sizable
and *negative* at small d (−37% at d=2), so `A^(2)_d` is not even uniformly ≥ √(d/x)
there. But in the regime C5 actually lives — d large, toward the edge x^{1−ε} — the
off-diagonal → 0 (off/diag = +0.46%, +0.23% at d=2310, 30030) and `A^(2)_d ≈ √(d/x)`
to within a fraction of a percent. So in the regime that matters, the L² factor is
exactly the free diagonal `√(d/x)`, which → 0 trivially for `d ≤ x^{1−ε}`. This is
**not** a cancellation theorem (it would hold verbatim for ±1-random `t`); it requires
no MRT/Tao/entropy/arithmetic. The verdict is *cleaner*, not weaker.

### V3 — the structural finding (non-multiplicativity + variance regeneration + L² blindness).

```python
# 3a: t non-multiplicative
coprime pairs tested: 12160; t(mn) != t(m)t(n) in 5992 of them (49.3%).
lambda(mn)==lambda(m)lambda(n) violations: 0     # lambda IS completely multiplicative
```
**Confirmed:** t fails multiplicativity on ~half of coprime pairs, while λ itself is
completely multiplicative. The +2 shift is exactly what destroys it.

```python
# 3b: the variance off-diagonal is itself shifted correlations of t
d=2310: var = 2040592; diagonal k=0 = x = 2000000; off-diagonal = 40592
  C(1*2310=2310)  = sum_n t(n)t(n+2310) = 1384   (a shift-2310 FOUR-fold Liouville corr.)
  C(2*2310=4620)  = sum_n t(n)t(n+4620) =  456
  C(3*2310=6930)  = sum_n t(n)t(n+6930) = 1774
```
**Confirmed:** `Σ_a S_{a,d}² = Σ_k C(kd)`, and each off-diagonal piece
`C(h) = Σ_n λ(n)λ(n+2)λ(n+h)λ(n+h+2)` is a four-fold Liouville correlation — the **same
hard family**. With no character orthogonality (t non-multiplicative), the variance
expansion **regenerates** these correlations instead of collapsing them to a negligible
error. This is the structural reason the large sieve has no purchase.

```python
# 3c: L2 statistics cannot see the difficulty
     d |  A2(t=corr)  A2(lambda) | sqrt(d/x)
    30 |     0.00378     0.00651 | 0.00387
   210 |     0.01057     0.01314 | 0.01025
  2310 |     0.03433     0.03580 | 0.03399
```
**Confirmed:** A^(2)_d is near-identical for the large-sieve-tractable multiplicative λ
and the intractable non-multiplicative t (both diagonals are x). The L² factor is *blind*
to non-multiplicativity. The entire difference — λ is large-sieve-tractable, t is not —
lives in the L∞ / `R_d` sup, not in L².

### V4 — Cauchy–Schwarz is divergent and useless at the edge.

```
 d=    210: CS bound U_d <=   0.153   (d/sqrt(x)=  0.148)
 d=   2310: CS bound U_d <=   1.650   (d/sqrt(x)=  1.633)
 d=  30030: CS bound U_d <=  21.283   (d/sqrt(x)= 21.234)
 d= 200000: CS bound U_d <= 141.252   (d/sqrt(x)=141.421)
```
**Confirmed:** `U_d ≤ √(var)/(x/d) ≈ √d·√(d/x) = d/√x`. At the edge `d = x^{1−ε}` this is
`x^{1/2−ε} → ∞`. Second-moment / L² control alone gives a **divergent** bound on the sup
exactly in the regime C5 needs.

### V5 — literature (MRT/Tao averaging-cannot-be-removed), source-level.

arXiv direct fetch is HTTP 403 here (a known constraint in this project; prior steps
cite C5-adjacent literature at the open-problem/quoted level for the same reason). The
search-surfaced source content is decisive and matches the audit: Tao 2015
(arXiv:1509.05422, Forum Math. Pi 2016) proves the **logarithmically averaged** two-point
Chowla/Elliott; **removing the logarithmic averaging "requires one to prove expansion
properties of a certain random graph,"** and the entropy decrement argument is the
workaround used **because** that expansion could not be established (the edges of the
relevant graph are "not random enough or structured enough" for standard methods). The
averaged→unaveraged gap is therefore real and currently unbridged, and is **not** a
packaging artifact removable by reorganization.
Source (open-problem/quoted level): T. Tao, "The logarithmically averaged Chowla and
Elliott conjectures for two-point correlations," Forum Math. Pi 4 (2016) e8,
arXiv:1509.05422; and Tao's exposition (terrytao.wordpress.com, 2015-09-18).

**All four decisive audits re-verified independently. I now label.**

---

## PART 1 — LABELLING (the returned DISTILL attack on L-QUB)

Per `AGENTS.md`: FALSIFIED requires a concrete counterexample or a real flaw; otherwise
NEEDS-REVIEW with the reason the attack fails; OPEN if there is no checkable audit and the
node is neither certified nor refuted. I assign:

- **L-QUB-i** (the categorical iff *as leverage*) → **FALSIFIED-as-leverage** → GRAVEYARD.
  Concrete object: the algebraic identity `‖v‖_∞ = (‖v‖_2/√d)·(‖v‖_∞/(‖v‖_2/√d))`,
  re-verified myself (V1) on arbitrary real arrays with arbitrary mass to 1e-13, no
  arithmetic. The iff is **TRUE but tautological**; the Grothendieck/Kan/Beck–Chevalley
  apparatus provides **no bound and no easier target**. Note for the record: the identity
  *itself* is trivially true — what is falsified is the claim that it constitutes
  *leverage* / a categorical characterization with content. Moves to GRAVEYARD as a
  leverage claim.

- **L-QUB-ii** (content-locating clause "all difficulty beyond averaged L² is in
  `sup_d R_d`") → **FALSIFIED-as-stated (mislocates)** → GRAVEYARD. Concrete object: the
  diagonal identity `Σ_a S_{a,d}² = Σ_n t(n)² + off-diag = x + off-diag`, with `t(n)²=1`
  exactly (V2: 0 exceptions), so the L² factor `A^(2)_d ≈ √(d/x)` is the **FREE diagonal**,
  not a substantive waypoint. The *locating* half ("the difficulty is all in `R_d`") is
  true, but the **"beyond averaged L²"** framing is false: there is no nontrivial
  "averaged L² control" being achieved and then supplemented — the L² factor is won for
  free. The equivocation is **pinned**: averaged-over-`d`/over-scales (the MRT/Tao theorem,
  deep) versus averaged-over-`a` at fixed `d` (the trivial diagonal, what `A^(2)_d`
  actually is). FALSIFIED as a leverage/content-locating claim; its only true residue is
  the bare rename L-QUB-reform. Moves to GRAVEYARD.

- **L-QUB-reform** (`C5 ⟺ sup_{d≤x^{1−ε}} R_d ≤ x^{o(1)}`, given the free diagonal
  `A^(2)_d ≈ √(d/x)`) → **OPEN** → recorded as a **RENAME** (a faithful reformulation
  whose difficulty is **equal to C5**), **NOT a legal foundation**. The audit (V2/V4/V3c
  + V5) certifies *why it is a rename* (the L² factor is free; CS is divergent at the edge;
  variance/large-sieve is multiplicativity-driven and t is non-multiplicative; MRT/Tao
  reach t only via averaging that cannot currently be removed). It does **not** certify the
  bound itself — none exists. Therefore STATUS = **OPEN**, never NEEDS-REVIEW. It lives
  alongside C5 (it *is* C5 in L² clothing), not as a foundation to build on.

- **C5** — unchanged **OPEN crux**, checkpoint under active escalation. Cluster 5 renames
  it; the rename does not lower the difficulty.

- **A13** (the Cluster-5 quantitative-uniformity bridge approach) → **ACTIVE-but-renamed**:
  it produced a faithful coordinate change on C5 (and a genuine sharpening of *why* C5 is
  hard), but no easier target and no bound. Recorded honestly as a relocation/rename, not
  progress toward TPC.

This matches DISTILL's pre-filled verdicts; I have independently confirmed each concrete
object, so the FALSIFIED labels are earned, not rubber-stamped, and the OPEN label is
correct (no audit for the bound ⇒ OPEN).

---

## PART 2 — ACTION: Review

A Review action: re-survey the tracking sheet for missed angles / misclassifications,
fold in the labelling, and state the Cluster-5 verdict, the sharpening, and the next
escalation. No new mathematics is introduced as a candidate.

### 2.1 — THE CLUSTER-5 VERDICT (plainly)

**The quantitative-uniformity bridge RENAMES C5 (it dresses C5 in L²/variance clothing);
it does NOT provide a target.** Concretely: the factorization `U_d = A^(2)_d·R_d` is a
trivial algebraic identity (L-QUB-i, GRAVEYARD); the L² factor `A^(2)_d ≈ √(d/x)` is the
free diagonal `Σ_n t(n)² = x`, not a waypoint (L-QUB-ii, GRAVEYARD); so the only survivor
is `C5 ⟺ sup_{d≤x^{1−ε}} R_d ≤ x^{o(1)}` (L-QUB-reform, OPEN), whose difficulty is **equal
to C5** with no slack. Cluster 5 is a faithful coordinate change on C5 — an honest
**escalation to record**, **not** progress toward TPC and **not** a terminal state.

### 2.2 — THE SHARPENING (the genuine yield of Cluster 5): *why L² fails here*.

Cluster 5 did produce something real, even though it provides no target: a crisp,
audit-able statement of **why** C5 resists the one route that usually makes hard
correlation problems tractable. I crystallize it as a **sharpened-obstruction note**
(OPEN as an obstruction; explicitly **not** progress toward TPC):

> **Sharpened obstruction (SO-L²): the shift by 2 destroys multiplicativity, so the
> L²/variance/large-sieve route — which beats L∞ for multiplicative objects — gives no
> advantage for `t(n)=λ(n)λ(n+2)`.** Precisely:
> (i) For a *multiplicative* `f`, the large sieve controls `Σ_{n≡a(d)} f(n)` in L²
>     **unconditionally** via Dirichlet-character orthogonality (the variance collapses to
>     a small diagonal because `Σ_n f(n)χ(n)` is an L-function-type sum with its own
>     cancellation). This is exactly why, for multiplicative objects, **L² is easier than
>     L∞**.
> (ii) `t` is **non-multiplicative** (V3a: `t(mn)≠t(m)t(n)` on ~49% of coprime pairs),
>      because the `+2` shift breaks `λ(mn)=λ(m)λ(n)`. There is **no character
>      orthogonality for `t` itself**.
> (iii) Consequently, expanding the variance `Σ_a S_{a,d}²` does **not** collapse: its
>       diagonal is the free triviality `Σ_n t(n)²=x` (since `t(n)²=1`), and its
>       off-diagonal **regenerates the same hard family** — shift-`kd` four-fold Liouville
>       correlations `Σ_n λ(n)λ(n+2)λ(n+kd)λ(n+kd+2)` (V3b). The L² statistics are *blind*
>       to the obstruction (V3c: A^(2)_d near-identical for tractable λ and intractable t).
> (iv) Hence **L² is no easier than L∞** here: the L² is all trivial diagonal and the sup
>      is all C5. Second-moment control alone (Cauchy–Schwarz) gives the divergent,
>      useless bound `U_d ≤ d/√x = x^{1/2−ε} → ∞` at the edge (V4).
> (v) The only known way to reach `t` is the MRT/Tao **scale/modulus averaging** (Tao 2015,
>     arXiv:1509.05422), and Tao states the averaging **cannot currently be removed**:
>     doing so "requires one to prove expansion properties of a certain random graph"
>     whose edges are "not random enough or structured enough" for standard methods (V5).
>     The averaging is precisely the L∞-in-`d` (single large `d`) information that `R_d` at
>     the edge demands and that the method provably does not supply.
>
> **Status: OPEN (a sharpened obstruction, NOT progress toward TPC).** This does not
> reduce C5; it explains a wall. **Break/escalate condition** (a real PROVIDES, were it
> achieved): either (1) a variance/second-moment bound for the non-multiplicative `t` that
> **survives the shift** — some substitute for character orthogonality controlling
> `Σ_{n≡a(d)} t(n)` in L² beyond the trivial diagonal; or (2) **removing the MRT/Tao
> scale-averaging** — i.e. establishing Tao's open random-graph expansion. Either turns
> the rename into a genuine reduction.

I judge SO-L² **worth crystallizing**: it is concrete, fully audited by the inline
computations and the source-level Tao quote, and it is the most precise statement yet of
why C5 is hard *along the analytic-uniformity axis*. It is recorded as an obstruction, not
a foundation and not a result.

### 2.3 — Review of the rest of the sheet (missed angles / misclassifications).

I re-surveyed APPROACHES, OPEN THREADS, and the CLAIM GRAPH. Findings:

- **No misclassification found.** The legal foundations (C1-membership NEEDS-REVIEW;
  C2/C3/C4/C6 ESTABLISHED) are untouched by this step and remain correctly labelled. C5
  remains the OPEN crux. The OPEN-CHECKPOINTS (C7, C-COH-MON, C-inf, L-COH') remain OPEN
  and non-foundational. The GRAVEYARD gains L-QUB-i and L-QUB-ii.
- **Two independent relocations now on record, both re-expressing C5.** Cluster 1
  (geometry / cohomological transport, A12) relocated C5 into {C7 broader, C-COH-MON =
  cohomological C5, C-inf archimedean} — none easier. Cluster 5 (analytic uniformity, A13)
  renamed C5 as `sup_d R_d ≤ x^{o(1)}` — equal difficulty. Two cross-domain reframings,
  two re-expressions; neither lowered the difficulty.
- **The one un-exhausted alternate is T11 / Cluster 3** (additive×multiplicative /
  parity-structural: Gowers norms, nilsequences, inverse theorems, Tao–Teräväinen
  higher-order Fourier). It is on a genuinely different axis from geometry (Cluster 1) and
  analytic uniformity (Cluster 5).

### 2.4 — NEXT-ESCALATION RECOMMENDATION (candid; no manufactured optimism).

**The honest reading.** Two independent cross-domain reframings (geometry; analytic
uniformity) have now each *re-expressed* C5 rather than reduced it. That is real
**evidence** — not proof, but evidence — that C5 is the irreducible analytic core of TPC,
and that a third reframing has a substantial prior probability of re-expressing it again.
SO-L² makes the mechanism precise on the analytic-uniformity axis: the difficulty is the
**uniform-in-`d` edge** `d ≈ x^{1−ε}`, where only scale-averaged methods reach the object
and the averaging is exactly what cannot currently be removed. I will not manufacture
optimism that Cluster 3 will break this.

**However**, Cluster 3 is **not** a third copy of the same axis, and there is a specific,
non-vacuous reason it could behave differently rather than merely rename:

- Clusters 1 and 5 both attack `t` as an *analytic/geometric correlation object* and run
  straight into the same wall (non-multiplicativity / no orthogonality / no purity over ℤ).
- Cluster 3 attacks the **parity/structure** directly: the inverse theory for the
  Gowers/uniformity norms (Green–Tao–Ziegler; Tao–Teräväinen) asks whether `t`, or the
  von Mangoldt/Liouville pair entering it, **correlates with a nilsequence**. This is the
  one framework in which the *parity obstruction itself* is the named quantity (a U²/U³
  inverse-theorem statement), rather than an unmultiplicative correlation to be bounded
  by orthogonality. SO-L² says nothing about whether `t` is *structured* (nilsequence-
  correlated) or *Gowers-uniform*; it only says the L²/orthogonality route is blind.
- So Cluster 3 is the one queued axis whose **failure mode would be different** (it would
  fail by the inverse theorem returning "correlates with the parity character," i.e. by
  *naming the parity obstruction as a structural object*), and that distinct failure mode
  is itself worth extracting even if it, too, does not reduce C5.

**Recommendation: try Cluster 3 (T11) next — once — as a deliberately bounded probe, with
a pre-registered decider, not as an open-ended hope.** Concretely, the next EXPLORE step
should be an **Apply** (import the Gowers-norm / inverse-theorem framework to `t`) whose
*pre-specified decider* is: *does the inverse theory yield a statement about `t` /
`λ(n)λ(n+2)` that is **either** a genuinely-easier-or-known target **or** a sharper named
form of the parity obstruction — or does it, like Clusters 1 and 5, reduce to a third
re-expression of C5 with no slack?* If the decider returns "third re-expression," then we
will have **three** independent cross-domain reframings (geometry, analytic uniformity,
additive-combinatorial/parity-structural) all re-expressing C5, which I would record as
**strong, explicitly-stated evidence that C5 is the irreducible crux** — and at that point
the rational next move shifts from "find another reframing" to either (a) attacking one of
the two concrete break/escalate conditions of SO-L² head-on (a shift-surviving variance
bound, or Tao's random-graph expansion), or (b) the **independence** exit-direction
(is uniform-in-`d` two-point Chowla provably out of reach of a specified formal
system/method class?), which is a legitimate terminal direction under `AGENTS.md` and
which two-or-three failed reframings would motivate.

I am **not** stepping back yet: one queued axis (Cluster 3) remains genuinely
un-probed and has a distinct, non-vacuous failure mode. But I am stating candidly, in
advance, the threshold at which continued reframing stops being the rational move:
**a third re-expression closes the "just reframe again" strategy** and should redirect
the campaign to the SO-L² break conditions or to the independence direction. No optimism
is manufactured; the prior is that Cluster 3 most likely re-expresses C5, and the value of
running it is (i) the small chance it does not, and (ii) the distinct, extractable form of
its failure if it does.

**No terminal state is declared. No NEEDS-REVIEW or fake progress is manufactured.**

---

## TRACKING SHEET SNAPSHOT (as of 1781396231)

```
APPROACHES ATTEMPTED:
- [A1] Classical combinatorial sieve (Brun) | DEAD END (route to full TPC) | upper bound
  pi_2(x) << x/(log x)^2; no lower bound; collapses at dimension kappa=2 past sqrt(x).
  Inside class C1 (membership). Governed by C5.
- [A2] Selberg Lambda^2 sieve + parity principle | PROMISING (obstruction to STATE, not a
  route) | sharp bounds + the parity phenomenon; lives at C5. Inside C1.
- [A3] Chen (P_2) | DEAD END (full TPC) | residual P_2->P_1 gap IS parity; Chen's subtracted
  upper-bound sieve is a signed linear functional of Type-I data => INSIDE C1 (membership).
- [A4] GPY | DEAD END (full TPC) / ACTIVE (bounded gaps) | liminf gap/log=0; never fixed gap 2.
- [A5] Zhang 2013 | DEAD END (full TPC) | unconditional liminf<7e7; cannot reach gap 2 (parity).
- [A6] Maynard-Tao | DEAD END (full TPC) | liminf<=600; cannot say WHICH two entries are prime.
- [A7] Polymath8 | DEAD END (full TPC) | <=246, <=12 (EH), <=6 (GEH); barrier method-/GEH-
  specific = C4.
- [A8] Hardy-Littlewood circle method | DEAD END (full TPC, uncond.) | minor arcs = Fourier
  reformulation of the binary problem = SAME object as C5 (Attack M).
- [A9] EH / GEH as conditional input | DEAD END (full TPC) / ACTIVE (bounded gaps) | Type-I;
  does NOT break parity; C5 stays OPEN even under GEH.
- [A10] Chowla/Liouville-correlation (Matomaki-Radziwill, Tao log-Chowla, Tao-Teravainen) |
  PROMISING / ACTIVE | the only family not in-principle parity-blocked; unconditional FIXED-
  shift-2 theorem absent (only log-averaged Tao 2015, almost-all-shift, or Siegel-conditional).
  HOME of C5. Cluster 5 (A13) executed HERE: attacked U(x) directly => RENAME (see A13).
- [A11] Other escapes/non-escapes:
  (a) automorphic/spectral shifted-convolution -> strengthen Type-I inputs; inside C1; no
      sign-sensitive bilinear for lambda(n)lambda(n+2). Governed by C5.
  (b) additive-combinatorial / Green-Tao -> long APs, not a fixed gap; DEAD END for TPC AS A
      ROUTE, but the additive x multiplicative / Gowers-norm parity-structural angle is the
      Cluster-3 alternate escalation (T11) -- NOW THE RECOMMENDED next probe (distinct axis).
  (c) function field Sawin-Shusterman -> TPC PROVED over F_q[t] via Frobenius/etale-cohomology
      + Deligne purity + big monodromy (function-field Fouvry-Michel), level beyond 1/2. Genuine
      escape over F_q[t]; no Z-theorem. MECHANISM dissected in A12; DISTILL confirmed purity is
      NECESSARY-NOT-SUFFICIENT even there. Missing Z-input split & LABELLED into C7/C-COH-MON/C-inf.
  (d) pair-correlation / spectral (Montgomery; Bogomolny-Keating) -> HL twin-prime ~ two-point
      correlation of zeta zeros; zero-side formula itself conjectural; input >= full HL > C5.
      Governed by / implies C5. (Attack K.)
  (e) Siegel-zero / Heath-Brown 1983 dichotomy -> inf many Siegel zeros => inf many twin primes;
      hypothesis BELIEVED FALSE; a Siegel zero = NEGATION of C5 (same lambda-in-APs axis,
      opposite polarity). => node C6 (ESTABLISHED-IN-LITERATURE). Not a sibling.
  Friedlander-Iwaniec (a^2+b^4) & Heath-Brown (x^3+2y^3) = Type-II escapes for dimension-1 /
  thin sequences; no n(n+2) analogue = Claim C3.
- [A12] CATEGORICAL / COHOMOLOGICAL TRANSPORT (Cluster 1 — the cancellation engine) | ACTIVE
  (relocated, not broken; downstream nodes parked OPEN) | TRANSPORT the F_q[t] twin-prime proof
  to Z. 0 of 4 clauses is a Z-theorem. DISTILL + LABEL: L-COH-as-proof = FALSIFIED (GRAVEYARD);
  PURITY-ONLY = FALSIFIED (GRAVEYARD); surviving content = relocation L-COH' (OPEN) into C7 (NEW,
  broader than TPC) + C-COH-MON (= C5 in disguise) + C-inf (new archimedean input). VERDICT:
  Cluster 1 RELOCATES C5, does NOT break the wall. (Reframing #1 of 2 that re-expressed C5.)
- [A13] QUANTITATIVE-UNIFORMITY BRIDGE (Cluster 5) | ACTIVE-BUT-RENAMED (this step's label) |
  Reframe over Z: fibration of {S_{a,d}} over the moduli/divisibility poset; L-inf pushforward =
  U_d (= C5), L1/L2 pushforward = averaged A^(2)_d, gap = Beck-Chevalley defect R_d, giving the
  EXACT factorization U_d = A^(2)_d * R_d. DISTILLED (work/1781395447) + LABELLED (this step):
  the factorization is the TRIVIAL ALGEBRAIC IDENTITY (L-QUB-i, GRAVEYARD); the L2 factor
  A^(2)_d ~ sqrt(d/x) is the FREE diagonal Sum_n t(n)^2=x (L-QUB-ii, GRAVEYARD); survivor =
  C5 <=> sup_d R_d <= x^{o(1)} (L-QUB-reform, OPEN, RENAME, difficulty EQUAL to C5). VERDICT:
  Cluster 5 RENAMES C5, does NOT provide a target. YIELD: the sharpened obstruction SO-L²
  (why L² fails: shift by 2 kills multiplicativity => L² no easier than L-inf). Reframing #2 of 2
  that re-expressed C5. Recorded honestly as relocation/rename, NOT progress toward TPC.

CANDIDATE LEMMAS (pre-distill — not yet certified):
- (none active) — L-QUB has been DISTILLED and LABELLED this step; its nodes are now in the
  CLAIM GRAPH (L-QUB-reform OPEN) / GRAVEYARD (L-QUB-i, L-QUB-ii). No new candidate introduced.

OPEN THREADS:
- T2 [= the crux C5]: the unaveraged two-point shift-2 cancellation sum_{n<=x, n==a(d)}
  lambda(n)lambda(n+2)=o(x/d) UNIFORM over d<=x^{1-eps}. *** CHECKPOINT UNDER ACTIVE ATTACK
  (not terminal). *** Cluster-1 (geometry) RELOCATED it (C7/C-COH-MON/C-inf, none easier);
  Cluster-5 (analytic uniformity) RENAMED it (sup_d R_d <= x^{o(1)}, equal difficulty).
- T10 [CLOSED — Cluster 5 labelled this step]: quantitative-uniformity bridge. VERDICT RENAME.
  L-QUB collapsed to L-QUB-i (trivial identity, GRAVEYARD) / L-QUB-ii (mislocates, GRAVEYARD) /
  L-QUB-reform (= C5 in L2 clothing, OPEN). YIELD = sharpened obstruction SO-L² (below).
- T12 [CLOSED — DECIDED = RENAME, labelled this step]: (a) sup_d R_d ~ sqrt(2 ln d) = x^{o(1)}
  (benign). (b) L2 factor A^(2)_d ~ sqrt(d/x) is the TRIVIAL DIAGONAL (t(n)^2=1), FREE, not
  C5-hard (re-verified V2: 0 exceptions). (c) all content in R_d; sup_d R_d <= x^{o(1)} <=> C5,
  no slack. (d) WHY no variance handle: large-sieve/BV is multiplicativity-driven; +2 shift makes
  t non-multiplicative (V3a: ~49% of coprime pairs); variance off-diagonal regenerates shift-kd
  four-fold Liouville correlations (V3b); MRT/Tao reach t only via averaging Tao cannot currently
  remove (V5). NOT strictly weaker than C5.
- SO-L² [OPEN — sharpened obstruction, NOT progress; crystallized this step]: the shift by 2
  destroys multiplicativity, so the L²/variance/large-sieve route (which beats L-inf for
  multiplicative objects) gives NO advantage for t=lambda(n)lambda(n+2). The difficulty is exactly
  the uniform-in-d edge d~x^{1-eps}, where only scale-averaged methods (MRT/Tao) reach t and the
  averaging is exactly what cannot currently be removed (Tao's open random-graph expansion).
  Fully audited (V1-V5). BREAK/ESCALATE condition: (1) a shift-surviving variance bound for the
  non-mult t, OR (2) removing MRT/Tao's scale-averaging (Tao's random-graph expansion). Recorded
  as an OBSTRUCTION, not a foundation, not a result.
- T11 [ACTIVE — RECOMMENDED NEXT PROBE]: Cluster 3, additive x multiplicative / parity-structural
  (Gowers norms / nilsequences / inverse theorems; Tao-Teravainen higher-order Fourier). Distinct
  axis from Cluster 1 (geometry) and Cluster 5 (analytic uniformity). Distinct failure mode: would
  fail by the inverse theorem NAMING the parity obstruction as a structural (nilsequence-
  correlation) object, not by lack of orthogonality. Next EXPLORE = Apply, with PRE-REGISTERED
  decider: easier/known target, OR sharper named parity obstruction, OR a third re-expression of
  C5 (no slack). If "third re-expression" => strong stated evidence C5 is the irreducible crux =>
  redirect to SO-L² break conditions or the INDEPENDENCE direction.
- [T7/T8/T9 RESOLVED-BY-LABELLING] cohomological relocation -> C7 (broader) / C-COH-MON
  (= C5 in disguise) / C-inf (archimedean). Live downstream link C7 parked OPEN.
- [T3/T4/T5/T6 CLOSED] C1-membership resolved; F_q[t]-absence subsumed into A12; necessity =
  same object as C5 (circularity removed); C6 folded in & labelled.

CURRENT POSITION:
- NOT at a terminal state. This step: (1) re-verified all four decisive audits of the L-QUB attack
  independently (V1-V5: the trivial identity; the free diagonal with 0 exceptions to t(n)^2=1;
  non-multiplicativity at ~49% of coprime pairs; variance off-diagonal = shift-kd four-fold
  Liouville correlations; CS divergent at the edge; MRT/Tao averaging not removable at source
  level). (2) LABELLED: L-QUB-i FALSIFIED-as-leverage (GRAVEYARD); L-QUB-ii FALSIFIED-as-stated /
  mislocates (GRAVEYARD); L-QUB-reform OPEN (RENAME, difficulty equal to C5, NOT a foundation);
  C5 unchanged OPEN; A13 ACTIVE-but-renamed. (3) Action = Review.
- CLUSTER-5 VERDICT (plainly): the quantitative-uniformity bridge RENAMES C5 (L² clothing), it
  does NOT provide a target. An honest escalation to record; NOT progress toward TPC; NO terminal
  state.
- SHARPENING (genuine yield): SO-L² -- the shift by 2 destroys multiplicativity, so L² is no easier
  than L-inf here; the difficulty is exactly the uniform-in-d edge, where only scale-averaged
  methods reach t and the averaging cannot currently be removed. Crystallized as an OPEN
  obstruction (not progress).
- TWO independent reframings (geometry: Cluster 1; analytic uniformity: Cluster 5) have each
  re-expressed C5 without reducing it. This is EVIDENCE (not proof) that C5 is the irreducible crux.
- NEXT-ESCALATION RECOMMENDATION (candid): run Cluster 3 (T11) ONCE as a bounded probe with a
  pre-registered decider -- it is the one un-probed axis with a DISTINCT failure mode (inverse
  theorem naming the parity obstruction). If it too returns a third re-expression of C5, that closes
  the "just reframe again" strategy and redirects the campaign to the SO-L² break conditions or to
  the INDEPENDENCE direction. No manufactured optimism: the prior is that Cluster 3 most likely
  re-expresses C5; its value is the small chance it does not + the distinct extractable failure mode.
- Legal foundations UNCHANGED: C1-membership (NEEDS-REVIEW); C2/C3/C4/C6 (ESTABLISHED). C5 may be
  NAMED, NOT built upon. C7/C-COH-MON/C-inf/L-COH'/L-QUB-reform are OPEN => NOT legal foundations.
  SO-L² is an OBSTRUCTION => NOT a foundation. GRAVEYARD nodes (incl. L-QUB-i, L-QUB-ii) may NOT be
  built upon.

CLAIM GRAPH
(LEGAL foundations = C1-membership/C2/C3/C4/C6. C5 = named OPEN crux, checkpoint under active
attack, NOT built upon. OPEN-CHECKPOINTS = C7/C-COH-MON/C-inf/L-COH'/L-QUB-reform, all OPEN, NOT
legal foundations. SO-L² = OPEN obstruction, NOT a foundation.):
- [C1] CLASS DEFINITION + MEMBERSHIP. A "Type-I sieve scheme" for F(n)=n(n+2) is a lower bound
  pi_2(x) >= L(a), L a (possibly SIGNED) linear functional of Type-I data {T_d = sum_{n<=x,
  d|F(n)} a_n : d<=D=x^{1-eps}}, Type-I inputs only (BV/EH/GEH/Zhang). MEMBERSHIP: Brun, Selberg,
  GPY, Maynard-Tao AND Chen/Buchstab-iterated subtracted-upper-bound sieves are ALL signed linear
  functionals of the T_d (closed under subtraction). Asserts ONLY membership; NOT parity-blocked
  (that is C5).
  DEPENDS ON: nothing. STATUS: NEEDS-REVIEW (membership half).
  AUDIT: (a) inspect each method's weight (Maynard, Ann. of Math. 181 (2015), Sec.4-5; Selberg);
  (b) Chen split = sum_d c_d T_d, all d<=z_1*z<D (Halberstam-Richert ch.11); (c) closed-under-
  subtraction is sound linear algebra. SCOPE NOTE: membership only; "therefore parity-blocked"
  requires each T_d perturbed by o(main) UNIFORMLY over d<=D under lambda(n)lambda(n+2) = C5.
  ADVERSARIAL PASS: Pass F/G/H + Attack N. SURVIVES as NEEDS-REVIEW.
  source: work/1781354707-DISTILL.md (F/G/H) + work/1781354180-EXPLORE.md (Part 2)
  + work/1781355529-EXPLORE.md (V-F/V-G) + work/1781356292-DISTILL.md (Attack N).
- [C2] BOMBIERI ASYMPTOTIC SIEVE / PARITY (dimension EXACTLY 1). For sieve dimension exactly 1 on
  a Type-I EH-level hypothesis, Type-I data determines Lambda_k (k>=2) but provably NOT Lambda_1
  without a Type-II bilinear estimate.
  DEPENDS ON: established mathematics. STATUS: ESTABLISHED-IN-LITERATURE.
  AUDIT: Bombieri, "The asymptotic sieve," Rend. Accad. Naz. XL (5) 1/2 (1975/76); Friedlander-
  Iwaniec, "Asymptotic sieve for primes," Ann. of Math. 148 (1998), 1041-1065; Tao, "Notes on the
  Bombieri asymptotic sieve" (2016). SCOPE NOTE: dimension EXACTLY 1; converse "Type-II =>
  pi_2(n(n+2))->inf" NOT included (heuristic at dim 2).
  ADVERSARIAL PASS: Pass H + Attack O.2. SURVIVES.
  source: work/1781353616-DISTILL.md (C/E) + work/1781354707-DISTILL.md (H)
  + work/1781356292-DISTILL.md (Attack O.2).
- [C3] TYPE-II SUFFICES / IS THE KNOWN ESCAPE (dimension-1 / thin sequences). A sign-sensitive
  bilinear (Type-II) estimate breaks parity and counts a fixed prime form (FI a^2+b^4; Heath-Brown
  x^3+2y^3); none known for n(n+2). Outside class C1.
  DEPENDS ON: established mathematics. STATUS: ESTABLISHED-IN-LITERATURE.
  AUDIT: Friedlander-Iwaniec, "The polynomial X^2+Y^4 captures its primes," Ann. of Math. 148
  (1998), 945-1040; Heath-Brown, "Primes represented by x^3+2y^3," Acta Math. 186 (2001). "None
  for n(n+2)" is an absence-of-citation survey fact. SCOPE NOTE: Type-II CAN count a fixed prime
  form for SPECIFIC thin sequences; does NOT prove "Type-II => pi_2(n(n+2))->inf".
  ADVERSARIAL PASS: Pass I + Attack O.2. SURVIVES.
  source: work/1781353616-DISTILL.md (E) + work/1781354707-DISTILL.md (I)
  + work/1781356292-DISTILL.md (Attack O.2).
- [C4] POLYMATH8b GEH BARRIER (method-specific, GEH-specific). Under GEH the Maynard-Tao sieve
  gives H_1<=6, and the Selberg parity obstruction prevents THIS method from H_1<6 under GEH;
  under plain EH not even excluded.
  DEPENDS ON: established mathematics; C2. STATUS: ESTABLISHED-IN-LITERATURE (re-scoped).
  AUDIT: D.H.J. Polymath, Res. Math. Sci. 1 (2014) art.12 (arXiv:1407.4897) -- "best possible
  FROM SIEVE-THEORETIC METHODS thanks to the parity problem"; retrospective arXiv:1409.8361.
  ADVERSARIAL PASS: Pass J + Attacks N, O.1. Method-/GEH-specific reading SURVIVES; absolute
  reading stays GRAVEYARD.
  source: work/1781353616-DISTILL.md (D) + work/1781354707-DISTILL.md (J)
  + work/1781356292-DISTILL.md (Attacks N, O.1).
- [C6] SIEGEL-ZERO / HEATH-BROWN ROUTE = CONDITIONAL DICHOTOMY, NOT A DISTINCT SIBLING. Heath-
  Brown's dichotomy: (inf many twin primes) OR (no Siegel zeros). Hypothesis BELIEVED FALSE (GRH
  precludes); a Siegel zero = failure of Mobius/Liouville pseudorandomness = NEGATION of C5.
  => NOT an unconditional escape and introduces NO new open object (same axis, opposite polarity).
  DEPENDS ON: established mathematics (framing input to C5). STATUS: ESTABLISHED-IN-LITERATURE.
  AUDIT: Heath-Brown, "Prime Twins and Siegel Zeros," Proc. LMS (3) 47 (1983) 193-224; Tao,
  "Heath-Brown's theorem on prime twins and Siegel zeroes" (2015-08-26); Tao-Teravainen arXiv:
  2111.14054, arXiv:2112.11412. SCOPE NOTE: conditional theorem on a believed-false hypothesis;
  CONFIRMS the missed-angle audit.
  ADVERSARIAL PASS: Attack L; independently re-verified at primary-source/quoted level.
  source: work/1781356292-DISTILL.md (Attack L) + work/1781356804-EXPLORE.md (labelling).
- [C5] *** THE CRUX (single OPEN claim — NAMED, NOT built upon; CHECKPOINT UNDER ACTIVE
  ESCALATION, NOT terminal). *** Single missing object: UNAVERAGED TWO-POINT shift-2 cancellation
  for lambda(n)lambda(n+2):
    EXISTS eps>0, delta(x)->0 s.t. |sum_{n<=x, n==a (mod d)} lambda(n)lambda(n+2)| <=
    delta(x)*(x/d) FOR ALL d <= x^{1-eps} AND ALL residues a (mod d)
  (= unaveraged two-point Chowla/Liouville at shift 2, uniform in APs), o(main) strength.
  Both directions (necessity; sufficiency) open at dimension 2. Only log-averaged proven (Tao
  2015). Chowla-strength: NOT implied by RH/GRH.
  LOCALIZATION (A13/Cluster 5): C5 <=> U(x):=max_{d<=x^{1-eps}} max_a |S_{a,d}|/(x/d) -> 0;
  factorization U_d = A^(2)_d * R_d. DISTILL+LABEL (work/1781395447 + this step): this is an
  ALGEBRAIC IDENTITY (L-QUB-i, GRAVEYARD); the L2 factor A^(2)_d ~ sqrt(d/x) is the TRIVIAL
  DIAGONAL (free; t(n)^2=1, re-verified 0 exceptions), so C5 <=> sup_{d<=x^{1-eps}} R_d <= x^{o(1)}
  (L-QUB-reform, OPEN) -- a faithful RENAME, difficulty EQUAL to C5; variance/large-sieve gives no
  handle (t non-multiplicative). Why-L²-fails crystallized as obstruction SO-L².
  DEPENDS ON: C1 (membership scope), C2 (dim-1 model), C3 (known Type-II escape), C6 (Siegel-zero
  anti-cancellation) -- all FRAMING; NOT a proven equivalence to TPC.
  STATUS: OPEN (checkpoint under active cross-domain attack). AUDIT: NONE EXISTS (the point).
  Rigorous sub-facts: (i) unaveraged two-point Chowla =o(x) is OPEN, only log-averaged proven
  (Tao arXiv:1509.05422; Pilatte arXiv:2310.19357); numerics (N up to 4e6): global |S(x)|/x ~
  x^{-1/2} but worst-bucket max_a |S_{a,d}|/(x/d) GROWS toward 1 with d; refined: U_d=A^(2)_d R_d,
  A^(2)_d~sqrt(d/x) (free diagonal), R_d~sqrt(2 ln d) (extremal, benign), worst bucket sqrt-cancels
  => U_d->1 is the log factor not a main term; fixed-d U_d->0; difficulty = the edge d~x^{1-eps}.
  (ii) parity obstruction for n(n+2) is HEURISTIC; Bombieri's THEOREM is dim-1 only. (iii)
  sufficiency for n(n+2) not cited.
  COHOMOLOGICAL RE-EXPRESSION (A12/L-COH, DISTILLED + LABELLED): C5's content disperses into C7
  (broader cohomological-existence conjecture = Deninger/Connes-Consani) + C-COH-MON (= C5 in
  sheaf clothing) + C-inf (archimedean). None easier than C5; RELOCATES, does not reduce.
  ANALYTIC-UNIFORMITY RE-EXPRESSION (A13/Cluster 5, DISTILLED + LABELLED): C5 <=> sup_d R_d <=
  x^{o(1)} (L-QUB-reform); RENAME, equal difficulty. Sharpened obstruction SO-L² records WHY:
  shift kills multiplicativity => L² no easier than L-inf.
  ADVERSARIAL PASS: Pass F/G/H/I/J + Attacks K/L/M/O + Attacks P1-P4 + Cluster-5 decider (T12).
  STAYS OPEN. Two independent reframings (geometry, analytic uniformity) re-express it; neither
  reduces it.
  source: work/1781354707-DISTILL.md + work/1781353616-DISTILL.md + work/1781354180-EXPLORE.md
  + work/1781355529-EXPLORE.md + work/1781356292-DISTILL.md + work/1781356804-EXPLORE.md
  + work/1781388988-EXPLORE.md + work/1781389622-DISTILL.md + work/1781390160-EXPLORE.md
  + work/1781394759-EXPLORE.md + work/1781395447-DISTILL.md + work/1781396231-EXPLORE.md (labels).

  --- OPEN-CHECKPOINTS (POST-DISTILL, LABELLED OPEN; NOT legal foundations) ---
- [L-QUB-reform] (Cluster-5 survivor, labelled this step) C5 <=> sup_{d<=x^{1-eps}} R_d(x) <=
  x^{o(1)}, where R_d = ||S_{.,d}||_inf / (||S_{.,d}||_2/sqrt d), given the FREE diagonal
  A^(2)_d ~ sqrt(d/x).
  DEPENDS ON: C5 (it IS a re-expression of C5). STATUS: OPEN -- a RENAME, difficulty EQUAL to C5
  (no slack, since A^(2)_d is the free diagonal). NOT a legal foundation.
  AUDIT: (i) A^(2)_d matches diagonal sqrt(d/x) to <=3% for d>=30 and to <1% near the edge
  (re-verified V2; t(n)^2=1 with 0 exceptions); (ii) variance-only (Cauchy-Schwarz) gives
  U_d <= sqrt(d) A^(2)_d ~ d/sqrt(x) = x^{1/2-eps} -> infinity at the edge (re-verified V4 --
  divergent, useless); (iii) large-sieve/BV variance is multiplicativity-driven (Kowalski; GHS
  BV-for-mult-fns) and t is non-multiplicative (re-verified V3a: ~49% of coprime pairs) so the
  machinery does not apply; MRT/Tao reach t only via scale/modulus averaging (arXiv:1509.05422)
  Tao states cannot currently be removed (random-graph expansion, re-verified V5 at source level).
  NO AUDIT certifies the bound itself (none exists) => STATUS stays OPEN, never NEEDS-REVIEW.
  ADVERSARIAL PASS: Cluster-5 decider T12 (work/1781395447) + independent re-verification V1-V5
  (this step). Faithful reformulation = RENAME, OPEN.
  source: work/1781395447-DISTILL.md + work/1781396231-EXPLORE.md (label + re-verification).
- [C7] "A Weil-type cohomology + six functors + Poincare-Verdier duality + absolute Frobenius +
  Grothendieck-Lefschetz trace formula over Spec Z (= F_1 / Connes-Consani arithmetic site)
  realizing the counting/zeta equality as a Lefschetz formula."
  DEPENDS ON: established math as a STATED open problem; nothing as an input. STATUS: OPEN — a
  recognized MAJOR open conjecture, a NEW object DISTINCT from C5 and STRICTLY BROADER than TPC
  (Deninger / Connes-Consani; its full purity form would yield RH).
  AUDIT (open-problem citation, NOT a theorem relied upon): Connes arXiv:1509.05576; Connes-
  Consani "Geometry of the Arithmetic Site" / arXiv:1405.4527 name this as the MAIN OPEN
  question (primary-source wording verified work/1781388988; arXiv PDFs HTTP 403 to direct fetch,
  so cited at open-problem level, which suffices: C7 asserted OPEN, not true).
  ADVERSARIAL PASS: Attacks P1, P4. LABELLED OPEN (genuine escalation; harder/broader than C5).
  source: work/1781389622-DISTILL.md + work/1781390160-EXPLORE.md (label).
- [C-COH-MON] "The geometric monodromy of the Liouville sheaf L (x) sh_2^* L over Spec Z is big
  enough to kill the top-weight invariants (= L-COH clause (3))."
  DEPENDS ON: C7 (needs the cohomology to exist before its monodromy is defined). STATUS: OPEN —
  and it is C5 RE-EXPRESSED ("cohomological C5"): vanishing of the top-weight invariant subspace
  IS "the shift-2 correlation has no main term" = C5; proving it over Z is equivalent to / harder
  than C5.
  AUDIT: NONE independent of C5. The F_q[t] template (Katz GKM, Ann. Math. Studies 116; S-S
  Burgess/Fouvry-Michel monodromy computation, arXiv:1808.04001) is NOT an audit for the Z claim.
  No Z-object has known-large monodromy in the required sense.
  ADVERSARIAL PASS: Attack P3 (fabrication audit). LABELLED OPEN (= cohomological C5; circular).
  source: work/1781389622-DISTILL.md + work/1781390160-EXPLORE.md (label).
- [C-inf] "The archimedean place infinity of Spec Z (no function-field counterpart) can be
  incorporated into the trace/Lefschetz formalism (properness / compact support)."
  DEPENDS ON: C7. STATUS: OPEN — a THIRD DISTINCT open input (structural disanalogy; F_q[t] has
  no infinity). Neither identical to C5 nor to C7's bare existence statement.
  AUDIT (open-problem citation): Connes treats infinity separately, e.g. arXiv:2006.13771.
  ADVERSARIAL PASS: Attack P4. LABELLED OPEN (distinct from C5/C7).
  source: work/1781389622-DISTILL.md + work/1781390160-EXPLORE.md (label).
- [L-COH'] (the surviving RELOCATION) "C5 follows IF { C7 AND C-COH-MON AND C-inf }, by transported
  Grothendieck-Lefschetz + Deligne purity."
  DEPENDS ON: C7, C-COH-MON, C-inf (conjunction); C5 (target). STATUS: OPEN — implication SHAPE is
  the established F_q[t] proof pattern (S-S arXiv:1808.04001; Deligne Weil II IHES 52 (1980);
  Katz GKM), but ANTECEDENTS are OPEN over Z => a CONDITIONAL REDUCTION with open hypotheses, an
  ESCALATION linking TPC to the Deninger/Connes-Consani programs; NOT a proof of C5.
  AUDIT: the implication is the literal transport of the S-S/Katz/Deligne mechanism; the audit of
  the antecedents is the audit of C7/C-COH-MON/C-inf.
  ADVERSARIAL PASS: Attacks P1, P4. LABELLED OPEN (survives ONLY as relocation, never as proof).
  source: work/1781389622-DISTILL.md + work/1781390160-EXPLORE.md (label).

  --- SHARPENED OBSTRUCTION (OPEN; NOT a foundation, NOT a result) ---
- [SO-L²] (crystallized this step) "The shift by 2 destroys multiplicativity, so the
  L²/variance/large-sieve route — which makes L² easier than L-inf for MULTIPLICATIVE objects —
  gives NO advantage for t(n)=lambda(n)lambda(n+2). The difficulty is exactly the uniform-in-d edge
  d~x^{1-eps}, where only scale-averaged methods (MRT/Tao) reach t and the averaging is exactly
  what cannot currently be removed."
  DEPENDS ON: nothing (it is an obstruction statement about C5, fully audited). STATUS: OPEN as an
  obstruction; NOT progress toward TPC, NOT a terminal state.
  AUDIT: V1 (factorization = trivial identity), V2 (diagonal Sum_a S^2=x; t(n)^2=1, 0 exceptions;
  A^(2)_d~sqrt(d/x) free), V3a (t non-multiplicative ~49% coprime pairs; lambda 0 violations),
  V3b (variance off-diagonal = shift-kd four-fold Liouville correlations, regenerates not
  collapses), V3c (L2 blind: A^(2)_d near-identical for tractable lambda and intractable t),
  V4 (Cauchy-Schwarz divergent at the edge: U_d ~ d/sqrt(x) = x^{1/2-eps} -> infinity),
  V5 (Tao arXiv:1509.05422: only LOG-averaged; removing averaging needs an unproven random-graph
  expansion; source-level confirmed). All inline in this file.
  BREAK/ESCALATE condition (a real PROVIDES, if achieved): (1) a shift-surviving variance/2nd-moment
  bound for the non-multiplicative t (a substitute for character orthogonality beyond the trivial
  diagonal); OR (2) removing the MRT/Tao scale-averaging (Tao's open random-graph expansion).
  source: work/1781396231-EXPLORE.md.

GRAVEYARD (may NOT be built upon):
- [L-QUB-i] (labelled this step) "The factorization U_d = A^(2)_d R_d, hence
  'C5 <=> sup_d A^(2)_d R_d -> 0', is a categorical/Beck-Chevalley CHARACTERIZATION with leverage."
  FALSIFIED-as-leverage. Concrete object: the algebraic identity ||v||_inf =
  (||v||_2/sqrt d)(||v||_inf/(||v||_2/sqrt d)) for any real vector v, re-verified independently
  (V1, seed 20260613, arbitrary reals + arbitrary mass, equal to 1e-13, NO arithmetic). The iff is
  TRUE but TAUTOLOGICAL; the Grothendieck/Kan/Beck-Chevalley apparatus provides NO bound and NO
  easier target. NOTE: the identity itself is trivially true; what is FALSIFIED is the LEVERAGE
  claim. source: work/1781395447-DISTILL.md (ATTACK 1) + work/1781396231-EXPLORE.md (V1 + label).
- [L-QUB-ii] (labelled this step) "All analytic difficulty BEYOND averaged (L²) control is carried
  by sup_d R_d." FALSIFIED-as-stated (mislocates). Concrete object: Sum_a S_{a,d}^2 = Sum_n t(n)^2
  + off-diag = x + off-diag with t(n)^2=1 exactly (V2, 0 exceptions), so the L² factor
  A^(2)_d ~ sqrt(d/x) is the FREE diagonal, NOT a substantive waypoint. The LOCATING half is true;
  the "beyond averaged L²" framing is false (no nontrivial L² control is achieved-then-supplemented).
  Equivocation PINNED: averaged-over-d (MRT/Tao, deep) vs averaged-over-a at fixed d (trivial
  diagonal). True residue = the bare rename L-QUB-reform.
  source: work/1781395447-DISTILL.md (ATTACKS 2b/3/4) + work/1781396231-EXPLORE.md (V2 + label).
- [L-COH-as-proof-of-C5] "(1)+(2)+(3) over Spec Z exist as available inputs and => C5, PROVING C5."
  FALSIFIED-as-stated (DISTILL Attacks P1+P2). 0 of its load-bearing hypotheses is a Z-theorem;
  clause (1)'s formalism is the explicitly OPEN central goal of Connes/Connes-Consani. Superseded
  by the relocation L-COH' (OPEN). source: work/1781389622-DISTILL.md + work/1781390160-EXPLORE.md.
- [PURITY-ONLY] "(1)+(2) => C5: purity / RH-strength alone yields the uniform two-point bound."
  FALSIFIED (DISTILL Attack P2). Concrete object: (a) RH/GRH =/=> the unaveraged fixed-shift two-
  point lambda-correlation (only LOG-AVERAGED known, Tao arXiv:1509.05422; Pilatte arXiv:2310.19357);
  (b) DECISIVE: in F_q[t] purity = Weil II is a THEOREM yet twin primes STILL need the SEPARATE
  Sawin-Shusterman big-monodromy / Burgess / Fouvry-Michel input (Annals 196 (2022),
  arXiv:1808.04001); (c) numerics: global |S(x)|/x ~ x^{-1/2} but worst-bucket max_a |S_{a,d}|/(x/d)
  GROWS toward 1 with d. source: work/1781389622-DISTILL.md + work/1781390160-EXPLORE.md.
- [L1-as-written] involution a_n->(1±lambda(n))a_n/2 (lambda on n) Type-I invariant. FALSIFIED
  (Pass A): mass ratio 0.66-0.79; every true twin (n>3) has lambda(n)=-1 => all in b_minus.
- [C4-absolute] absolute "no method whatever beats gap 6." FALSIFIED-as-stated (barrier tied to
  SIEVE-THEORETIC methods; plain EH does NOT exclude better).
- [dim-2-necessity-as-theorem] Type-II PROVABLY NECESSARY for n(n+2). OPEN, not a theorem (FI
  necessity is dim EXACTLY 1). Honest content in C5.
- [C1-parity-blocked-as-audited] reading that C1's comprehensiveness CLOSES necessity. FALSIFIED-
  as-an-audit (only MEMBERSHIP audited; uniform-in-d half is C5). Absorbed into C5.
```
