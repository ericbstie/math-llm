# DISTILL — step 1784988042

**MODE: DISTILL.** One attack-and-reduce pass. I do **not** assign final labels; each node
below carries a *recommended* verdict with its reason, for EXPLORE to adjudicate.

**TARGET.** The two candidate lemmas introduced by A22 in `work/1784986525-EXPLORE.md`
(commit `1f4dd16`): **[L-C5-CONST]** (an unconditional constant-factor bound on the crux
quantity) and **[L-EXP-REACH]** (the polylogarithmic confinement of the
Helfgott–Radziwiłł/Pilatte mechanism on the \(d\)-axis).

**HEADLINE OF THIS PASS — five things broke, and one thing got sharper.**

1. **The deduction in Prop A22.1 is valid only for *lower* density, and I built the
   concrete object that shows it.** A synthetic \(\pm1\) sequence in which every one of the
   eight length-3 patterns has upper density \(\ge 0.1210\) while
   \(\frac1x\sum_{n\le x}s(n)s(n+2)\ge 0.964\) at infinitely many scales. So the citation
   carries the whole weight, and I verified it at source: MRT's abstract says **"positive
   lower natural density"**. The deduction survives — but only because of that word.
2. **The "**\(d\mid h\)** iff" clause of [L-C5-CONST](c) is FALSE as worded.** Concrete
   counter-identity, verified to exact integer equality at five values of \(x\) up to \(10^7\):
   \(\sum_{n\le x,\,4\mid n}\lambda(n)\lambda(n+2)=-\sum_{m\le x/4}\lambda(m)\lambda(2m+1)\).
   A dilation identity exists for **every** \(d\); \(d\mid h\) is only the condition for the
   dilated object to be again a *shift* correlation. The conclusion "reachable moduli are
   exactly \(\{1,2\}\)" therefore loses its stated reason and becomes an **absence-of-construction
   claim ⇒ OPEN** (compare `[DDS-ENV]`).
3. **(R-i)'s "the matrix is the ZERO matrix on a class" is an artefact of testing the wrong
   shift set.** For Pilatte's actual \(\mathcal D_k=\{p_1\cdots p_k\}\), the shifts divisible by
   a given \(D\) are numerous and carry a **positive** fraction \(\asymp k/(DS)\) of the
   harmonic weight (computed below: \(V/\text{weight}=3.27\) at \(D=11\), matching \(DS/k\)).
   EXPLORE's Audit 5(a) used the **single-prime** set \(\mathcal P\) of Helfgott–Radziwiłł,
   where \(D\mid p\) forces \(D\in\{1,p\}\) — a strictly weaker case. And I exhibit the exact
   *class-restoring* dilation identity EXPLORE asked to be stress-tested for (its
   sub-question (i)): it exists, and I verified it at six parameter sets.
4. **The framing claim "the project was carrying a false premise" is a strawman.**
   `grep -ri` over the whole repository: the strings "nothing is known" / "no bound is known"
   occur **only inside A22's own files**. What the record actually said —
   *"unaveraged two-point Chowla \(=o(x)\) is OPEN … EVEN the Cesàro two-point Chowla is
   OPEN"* — is **true and remains true**. The real defect is narrower and still real: an
   **incomplete literature survey** (the MR/MRT constant-factor result was missing from
   the A10 line). "Incomplete survey", not "false premise".
5. **A genuine silent contradiction on the sheet, which A22 did not notice.** `SO-L²` reads
   *"…L²/variance/large-sieve/Halász/pretentious gives **NO advantage**"*. MR's own abstract
   (arXiv:1501.04585, fetched) says the mean value of \(\lambda(n)\lambda(n+1)\) *"is
   non-trivially bounded in absolute value by \(1-\delta\)"* — obtained by exactly that
   machinery. `SO-L²` must be re-scoped to "no \(o(1)\) advantage and no uniform-in-\(d\)
   advantage" or it is false as worded.
6. **The one thing that got sharper (in A22's favour).** [L-EXP-REACH]'s three ingredients
   collapse to **one** elementary inequality, and the imported *quotation* ("a power of log
   is the best achievable with current techniques") can be **replaced by a rigorous cap**:
   a shift set of distinct integers in \([1,x]\) confined to one residue class mod \(D\) has
   harmonic weight \(\le 1+\frac{\log x}{D}\), and the full-range weight is \(\le\log x+1\).
   Given only the *shape* of the method (saving \(=V^{-1/2+o(1)}\)), this yields
   \(D\ll(\log x)^{1/3}\) (direct restriction) and \(D\ll(\log x)^{1/2}\) (character twist) —
   **explicit exponents replacing A22's \((\log x)^{O(1)}\)**, with the residual dependence
   isolated in a single OPEN node.

**NOTHING HERE CHANGES C5's STATUS.** A constant-factor bound is not \(o(x/d)\); a reach of
\((\log x)^{1/2}\) is not \(x^{1-\varepsilon}\). Both remain a full power of \(x\) away.

---

## 0. Environment / reproducibility

All computations below were run once, ad hoc, with `python3` + `numpy` (no `sympy` in this
environment — the Liouville sieve is therefore cross-checked against an **independent
trial-division** implementation of \(\Omega\)). Code and captured output are inline. Scratch
scripts are not committed, per protocol.

Primary sources reached **this pass** (I re-verified rather than trusting A22's extraction):

| Source | Access achieved this pass | What it actually says |
|---|---|---|
| MRT, *Sign patterns of the Liouville and Möbius functions*, Forum Math. Sigma **4** (2016) e14 = arXiv:1509.01545 | **abs page fetched** | *"each of these eight sign patterns occur with **positive lower natural density**"* — for \((\lambda(n),\lambda(n+1),\lambda(n+2))\); and the nine patterns of \((\mu(n),\mu(n+1))\). |
| Tao, blog, *Sign patterns of the Möbius and Liouville functions* (2015-09-06) | **fetched** | Theorem 1: for \(k\le3\), *"each of the sign patterns in \(\{-1,+1\}^k\) is attained by the Liouville function for a set of natural numbers \(n\) of positive lower density."* **No** explicit constant; **no** discussion of effectivity. |
| Matomäki–Radziwiłł, *Multiplicative functions in short intervals*, arXiv:1501.04585 (Ann. of Math. **183** (2016)) | **abs page fetched** | abstract: *"the mean-value of \(\lambda(n)\lambda(n+1)\) … is non-trivially bounded in absolute value by \(1-\delta\) for some \(\delta>0\)"*. **This independently confirms A22's verbatim Tao quote, at the primary source.** |
| Helfgott–Radziwiłł, arXiv:2103.06853 | **abs page fetched** (A22 had abstract level only; same here) | eigenvalues \(O(\sqrt{\mathcal L})\), \(\mathcal L=\sum_{p}1/p\), *"optimal up to a constant factor"*; \(\frac1{\log x}\sum_{n\le x}\lambda(n)\lambda(n+1)/n=O(1/\sqrt{\log\log x})\); divisibility graph *"a strong local expander almost everywhere"*. **Log-averaged.** |
| Pilatte, arXiv:2310.19357 | **abs page fetched** (A22 had full text) | \(\sum_{n\le x}\frac1n\lambda(n)\lambda(n+1)\ll(\log x)^{1-c}\). **Log-averaged.** |

**Access limits, recorded honestly.** The MRT/MR *full texts* were not obtained this pass:
`https://www.math.mcgill.ca/radziwill/sign6.pdf` downloaded but the local PDF text
extractor is broken in this environment (`pypdf` → `ModuleNotFoundError: _cffi_backend`,
`cryptography` rust binding panics). So **every statement below about what is *inside* MR/MRT
(in particular effectivity) is at abstract/blog level and is marked as such.**

---

## ATTACK 1 — the citation is load-bearing at exactly one word: does MRT say *lower* density?

**Why this is the sharpest attack available.** Prop A22.1 uses superadditivity of
\(\liminf\) over four disjoint pattern classes. That step is legitimate for **lower**
densities and **illegitimate** for upper densities. If MRT proved only positive *upper*
density, Prop A22.1 — and therefore the whole of [L-C5-CONST](a) — would be **false**.

**Result: the claim survives.** arXiv:1509.01545 abstract, fetched this pass:
*"each of these eight sign patterns occur with **positive lower natural density**"*; Tao's
blog states Theorem 1 as *"positive lower density"* for \(k\le3\). Both "lower" and
"natural" (not logarithmic) are present. **ESTABLISHED at abstract level.**

**And here is the object that proves the attack was not idle.**

### Adversarial object: eight patterns of positive *upper* density, correlation \(\to1\)

Blocks \([\,r^{k-1},r^k)\), \(r=30\). Even-index blocks: \(s\equiv+1\). Odd-index blocks: the
cyclic de Bruijn word \(B(2,3)=00010111\) repeated (all eight length-3 patterns at density
exactly \(1/8\); \(\sum\varepsilon_1\varepsilon_3=0\) over the period).

```python
db = np.array([0,0,0,1,0,1,1,1], dtype=np.int8)   # cyclic de Bruijn of order 3
ratio, K = 30, 5
X = ratio**K
s = np.empty(X+4, dtype=np.int8); prev = 1
for k in range(1, K+1):
    xk = ratio**k
    idx = np.arange(prev, min(xk, X)+4)
    s[idx] = 1 if k % 2 == 0 else np.where(db[idx % 8] == 0, 1, -1)
    prev = xk
S = s.astype(np.int64)
for k in range(1, K+1):
    xk = ratio**k; n = np.arange(1, xk+1)
    corr = float(np.sum(S[n]*S[n+2]))/xk
    a,b,c = s[1:xk+1], s[2:xk+2], s[3:xk+3]
    cnt = {(e1,e2,e3): int(np.count_nonzero((a==e1)&(b==e2)&(c==e3)))
           for e1 in (1,-1) for e2 in (1,-1) for e3 in (1,-1)}
    print(xk, corr, min(cnt.values())/xk)
```

```
=== ATTACK 2: does the A22.1 deduction need LOWER density?  (synthetic object) ===
  toy sequence on [1,24300000], blocks of ratio 30
      x        corr/x     min pattern count / x
         30   +0.0667        0.1000
        900   +0.9644        0.0033
      27000   +0.0321        0.1210
     810000   +0.9677        0.0040
   24300000   +0.0323        0.1210
  => every pattern has UPPER density >= 0.1210 =: c_up,
     yet limsup corr/x >= 0.90  (achieved at the all-+1 block ends).
     The A22.1 conclusion '<= 1-8c' with c=c_up would read <= +0.0323 -- FALSE here.
     Conclusion: the deduction is valid ONLY with positive LOWER (natural) density.
```

**Verdict.** The deduction is *not* robust: with \(c_{\rm up}=0.1210\) the A22.1 conclusion
would assert \(\limsup\le0.0323\) while the truth is \(\ge0.964\). Prop A22.1 stands **only**
on MRT's word "lower". That word is present. **Attack fails; hypothesis is now pinned.**

---

## ATTACK 2 — is the deduction otherwise airtight (the \(O(1)\) boundary terms, the constant)?

\(P(x)+M(x)=\lfloor x\rfloor\) exactly; \(\sum_{n\le x}\lambda(n)\lambda(n+2)=P(x)-M(x)\);
\(\liminf M(x)/x\ge4c\) by superadditivity over the four patterns with
\(\varepsilon_1\varepsilon_3=-1\); hence
\(\limsup(P-M)/x=\limsup(x-2M+O(1))/x\le1-2\cdot4c\). Symmetrically for the other sign.
\(8c\le1\) because the eight lower densities sum to at most \(1\). **No gap.** The constant
\(1-8c\) is the best this argument can give (it uses only the minimum of the eight
densities). **Attack fails.**

---

## ATTACK 3 — effectivity: is \(\delta=8c\) an effective constant?

This was A22's own flagged worry (ii), and it is the one I could **not** settle.

* Neither the MRT abstract nor Tao's blog post states a numerical density, and neither
  discusses effectivity.
* I could not open MRT's or MR's full text (see §0), so I **cannot** certify the absence of
  an ineffective ingredient (e.g. a Siegel-zero-dependent Halász/large-sieve input, or an
  ineffective compactness step).
* **Scoping that matters, and that limits the damage:** ineffectivity would **not** falsify
  [L-C5-CONST](a). The statement *"there is an absolute \(\delta>0\) and an \(x_0\) with
  \(|\sum_{n\le x}\lambda(n)\lambda(n+2)|\le(1-\delta)x\) for \(x\ge x_0\)"* is unconditionally
  true either way; ineffectivity would only mean \(\delta,x_0\) are not computable. So the
  correct treatment is a **separate node** at OPEN, not a defect in the bound node.

**What would have to be true** for the effectivity node to become ESTABLISHED: a reader of
MR (Ann. of Math. 183 (2016) 1015–1056) and MRT (Forum Math. Sigma 4 (2016) e14) confirms
that every ingredient is effective and, ideally, extracts a numerical \(c\).
**Recommended verdict: OPEN.**

---

## ATTACK 4 — independent re-run of EXPLORE's Audit 1 (the exact even-class identity)

I re-derived and re-computed rather than trusting the pasted output, using my own sieve
**cross-checked against trial division**:

```python
def liouville(N):                      # prime-power sieve
    Om = np.zeros(N+1, dtype=np.int8)
    sieve = np.ones(N+1, dtype=bool); sieve[:2] = False
    for i in range(2, int(N**0.5)+1):
        if sieve[i]: sieve[i*i::i] = False
    for p in np.flatnonzero(sieve):
        p = int(p); pk = p
        while pk <= N:
            Om[pk::pk] += 1
            if pk > N//p: break
            pk *= p
    lam = np.where(Om & 1, -1, 1).astype(np.int8); lam[0] = 0
    return lam

def Omega_trial(n):                    # completely independent check
    c, p = 0, 2
    while p*p <= n:
        while n % p == 0: n //= p; c += 1
        p += 1
    return c + (1 if n > 1 else 0)

for x in [10,101,1000,12345,10**5,999999,4*10**6,9999997]:
    ns = np.arange(2, x+1, 2);  lhs = int(np.sum(L[ns]*L[ns+2]))
    ms = np.arange(1, x//2+1);  rhs = int(np.sum(L[ms]*L[ms+1]))
```

```
building lambda up to 10000000
[X] trial-division cross-check on 400 random n <= 10000000: mismatches = 0
[X] lam[2]=-1 lam[4]=1 lam[6]=1 lam[30]=-1

=== ATTACK 4: re-verify EXPLORE Audit 1 (exact even-class dilation identity) ===
  x=      10  LHS=     -3  RHS=     -3  equal=True
  x=     101  LHS=      6  RHS=      6  equal=True
  x=    1000  LHS=     12  RHS=     12  equal=True
  x=   12345  LHS=    122  RHS=    122  equal=True
  x=  100000  LHS=    146  RHS=    146  equal=True
  x=  999999  LHS=     37  RHS=     37  equal=True
  x= 4000000  LHS=   -392  RHS=   -392  equal=True
  x= 9999997  LHS=   -364  RHS=   -364  equal=True
  ALL EQUAL: True
```

Every value **reproduces A22's Audit 1 exactly, digit for digit** (−3, 6, 12, 122, 146, 37,
−392, −364). The proof is two lines (\(\lambda(2m)\lambda(2m+2)=\lambda(2)^2\lambda(m)\lambda(m+1)\)).
**Attack fails: the identity is correct.**

---

## ATTACK 5 — is the "**iff \(d\mid h\)**" clause exhaustive? **NO — concrete counter-identity.**

This is A22's flagged worry (iv), and the project's own precedent
(`[DDS-ENV]`, absence-of-construction) says an exhaustiveness claim over an open-ended
class of identities must be OPEN unless proved.

### 5a. A dilation identity for \(d=4\), \(h=2\) (so \(d\nmid h\))

\(\lambda(4m)\lambda(4m+2)=\lambda(4)\lambda(m)\cdot\lambda(2)\lambda(2m+1)
=-\lambda(m)\lambda(2m+1)\), hence **exactly, for every \(x\)**
\[
\sum_{n\le x,\ 4\mid n}\lambda(n)\lambda(n+2)\;=\;-\sum_{m\le\lfloor x/4\rfloor}\lambda(m)\lambda(2m+1).
\]

```
=== ATTACK 5a: counter-identity at d=4 (d does NOT divide h=2) ===
  x=    1000  sum_{4|n<=x} lam(n)lam(n+2) =      -6   -sum_{m<=x/4} lam(m)lam(2m+1) =      -6  equal=True
  x=   12345  sum_{4|n<=x} lam(n)lam(n+2) =      48   -sum_{m<=x/4} lam(m)lam(2m+1) =      48  equal=True
  x=  100000  sum_{4|n<=x} lam(n)lam(n+2) =     -82   -sum_{m<=x/4} lam(m)lam(2m+1) =     -82  equal=True
  x= 4000000  sum_{4|n<=x} lam(n)lam(n+2) =     298   -sum_{m<=x/4} lam(m)lam(2m+1) =     298  equal=True
  x= 9999997  sum_{4|n<=x} lam(n)lam(n+2) =    1693   -sum_{m<=x/4} lam(m)lam(2m+1) =    1693  equal=True
  ALL EQUAL: True
```

The same works for every \(d=2^k\): the class \(0\bmod 2^k\) is reduced **exactly** to
\(\pm\sum_{m\le x/2^k}\lambda(m)\lambda(2^{k-1}m+1)\).

**What this breaks and what it does not.**
* **BREAKS** the clause as worded: "the dilation identity … exists **iff** \(d\mid h\)".
  It exists for every \(d\). The correct scoped statement is: *the dilated object is again a
  **shift** correlation \(\sum\lambda(m)\lambda(m+h')\) iff \(d\mid h\)*; otherwise it is a
  correlation of two **distinct linear forms** \(\lambda(m)\lambda(am+b)\).
* **DOES NOT** by itself break the conclusion "reachable moduli \(=\{1,2\}\)". Reaching \(d=4\)
  would need an unconditional **Cesàro** bound (equivalently: both sign patterns of
  \((\lambda(m),\lambda(2m+1))\) with positive lower density). Tao arXiv:1509.05422 Thm 1.2
  covers \(\lambda(a_1n+b_1)\lambda(a_2n+b_2)\) with \(a_1b_2-a_2b_1\ne0\) — here \(=1\ne0\) —
  but **logarithmically averaged only**. I found no Cesàro sign-pattern theorem for
  \((\lambda(m),\lambda(2m+1))\), and I decline to assert one exists or does not.

**Verdict:** the *reason* is FALSIFIED (concrete object above); the *conclusion* is
downgraded to **OPEN**, contingent on a literature fact about \((\lambda(m),\lambda(2m+1))\).

### 5b. The class-restoring dilation identity — A22's [L-EXP-REACH] worry (i), answered YES

A22 asked: *"is there a variant whose shifts \(d\) are divisible by \(D\), restoring edges
inside a class?"* **Yes, and here it is, exactly.** For any \(D,a,d,h\) with \(D\mid d\):
\[
\sum_{n\le x,\ n\equiv a\,(D)}\lambda(n)\lambda(n+h)
\;=\;\sum_{M\le dx,\ M\equiv da\,(dD)}\lambda(M)\lambda(M+dh),
\]
and now **every edge has length \(dh\equiv0\ (\mathrm{mod}\ D)\) with both endpoints
\(\equiv0\ (\mathrm{mod}\ D)\)** — precisely the configuration (R-i) declared impossible.
Verified exactly:

```
=== ATTACK 5b: general D-divisible dilation identity (defeats R-i's zero-matrix) ===
  D=3 a=1 d=3 h=2 x=300000:  class-sum=     22   dilated (mod 9, shift 6)=     22  equal=True
  D=3 a=2 d=3 h=2 x=300000:  class-sum=    204   dilated (mod 9, shift 6)=    204  equal=True
  D=5 a=3 d=5 h=2 x=150000:  class-sum=    164   dilated (mod 25, shift 10)=    164  equal=True
  D=3 a=1 d=6 h=2 x=150000:  class-sum=    246   dilated (mod 18, shift 12)=    246  equal=True
  D=4 a=1 d=4 h=2 x=200000:  class-sum=    -74   dilated (mod 16, shift 8)=    -74  equal=True
  D=7 a=5 d=7 h=2 x=100000:  class-sum=      8   dilated (mod 49, shift 14)=      8  equal=True
  ALL EQUAL: True
```

The **cost** is visible in the identity itself: the modulus inflates \(D\mapsto dD\) and the
range inflates \(x\mapsto dx\). That is the real obstruction, and it is a *weight* loss, not
a *vanishing* — see ATTACK 6.

---

## ATTACK 6 — (R-i) on Pilatte's **own** shift set: the matrix is **not** zero on a class

A22's Audit 5(a) tested \(\mathcal P=\{\text{primes}\le50\}\), where \(D\mid p\Rightarrow D\in\{1,p\}\).
But the mechanism that actually holds the record (Pilatte) uses
\(\mathcal D_k=\{p_1\cdots p_k\}\), and there \(D\mid d\) has **many** solutions.

```python
H0, Hh, k = 10, 200, 3
ps = [p for p in range(H0+1, Hh+1) if all(p % q for q in range(2, int(p**0.5)+1))]
Dk = [math.prod(c) for c in combinations(ps, k)]
V  = sum(1.0/d for d in Dk)
for D in [11,13,17,101,199]:
    sub = [d for d in Dk if d % D == 0]
    print(D, len(sub), sum(1.0/d for d in sub), V/sum(1.0/d for d in sub))
```

```
=== ATTACK 6: does Pilatte's OWN shift set D_k have edges inside a class mod D? ===
  primes in (10,200]: 42;  |D_k|=11480 (k=3);  V = sum 1/d = 0.06595
   D=  11: #{d in D_k : D|d} =   820  weight = 0.020151  V/weight =     3.27  (D = 11)
   D=  13: #{d in D_k : D|d} =   820  weight = 0.017702  V/weight =     3.73  (D = 13)
   D=  17: #{d in D_k : D|d} =   820  weight = 0.014215  V/weight =     4.64  (D = 17)
   D= 101: #{d in D_k : D|d} =   820  weight = 0.002734  V/weight =    24.13  (D = 101)
   D= 199: #{d in D_k : D|d} =   820  weight = 0.001406  V/weight =    46.91  (D = 199)
```

**820 surviving shifts, not zero.** The exact structure:
\(\sum_{d\in\mathcal D_k,\,D\mid d}1/d=\frac1D\sum_{d'\in\mathcal D_{k-1}(\mathcal P\setminus\{D\})}1/d'\),
so the loss factor is \(V/\text{weight}\approx D\cdot S/k\) with \(S=\sum_{p\in\mathcal P}1/p\)
(here \(S\approx0.83\), \(k=3\): predicted \(11\cdot0.83/3=3.04\) vs observed \(3.27\) ✓).

**Verdict.** (R-i)'s *statement* ("zero unless \(D\mid d\) for some \(d\in\mathcal D\)") is true and
trivial. (R-i)'s *use* — as an obstruction showing the mechanism cannot see residue classes —
is **refuted for the state-of-the-art shift set**. The honest content is a **factor-\(D\)
harmonic-weight loss**, which is exactly what ATTACK 7 turns into the real confinement.

---

## ATTACK 7 — REDUCTION: (R-i)+(R-ii)+(R-iii) collapse to one elementary inequality, and the imported *quotation* can be discharged

A22's (R-iii) rests on Pilatte's sentence *"saving a fixed power of the logarithm is the
best that is achievable with current techniques"* — A22 itself flagged (worry (iv)) that
this is **a quoted expectation, not a theorem**. Under this project's rules a node resting on
it is OPEN. I therefore looked for a rigorous replacement, and there is one.

**Fact H (elementary, rigorous).** Let \(\mathcal D\subseteq[1,x]\cap\mathbb Z\) be any set of
**distinct** shifts. Then \(V:=\sum_{d\in\mathcal D}1/d\le\log x+1\). If moreover the bilinear
form is restricted to residue classes \(\alpha,\beta\ (\mathrm{mod}\ D_0)\) on the two sides,
only shifts \(d\equiv\pm(\alpha-\beta)\ (\mathrm{mod}\ D_0)\) contribute, and their weight is
\(\le 2\bigl(1+\frac{\log x}{D_0}\bigr)\).

```
=== ATTACK 6b: harmonic weight of a shift set confined to ONE class mod D0 ===
  V(x,D0,g) = sum_{d<=x, d = g mod D0} 1/d   vs   1 + log(x)/D0
   x=1e 6 D0=    2 g=   1: V=7.54294  1+logx/D0=7.90776  V<=bound: True
   x=1e 6 D0=   10 g=   1: V=2.19367  1+logx/D0=2.38155  V<=bound: True
   x=1e 6 D0=   10 g=   5: V=1.34764  1+logx/D0=2.38155  V<=bound: True
   x=1e 6 D0=  100 g=   1: V=1.09771  1+logx/D0=1.13816  V<=bound: True
   x=1e 6 D0=  100 g=  50: V=0.11174  1+logx/D0=1.13816  V<=bound: True
   x=1e 6 D0= 1000 g=   1: V=1.00748  1+logx/D0=1.01382  V<=bound: True
   x=1e 6 D0= 1000 g= 500: V=0.00887  1+logx/D0=1.01382  V<=bound: True
   x=1e 9 D0=    2 g=   1: V=11.30367  1+logx/D0=11.36163  V<=bound: True
   x=1e 9 D0=   10 g=   1: V=2.89979  1+logx/D0=3.07233  V<=bound: True
   x=1e 9 D0=   10 g=   5: V=2.09979  1+logx/D0=3.07233  V<=bound: True
   x=1e 9 D0=  100 g=   1: V=1.16695  1+logx/D0=1.20723  V<=bound: True
   x=1e 9 D0=  100 g=  50: V=0.18695  1+logx/D0=1.20723  V<=bound: True
   x=1e 9 D0= 1000 g=   1: V=1.01439  1+logx/D0=1.02072  V<=bound: True
   x=1e 9 D0= 1000 g= 500: V=0.01578  1+logx/D0=1.02072  V<=bound: True

  full-range cap:  sum_{d<=x} 1/d  vs  log x + 1
   x=1e 6: H_x ~ 14.39273  log x + 1 = 14.81551  cap holds: True
   x=1e12: H_x ~ 28.20824  log x + 1 = 28.63102  cap holds: True
   x=1e20: H_x ~ 46.62892  log x + 1 = 47.05170  cap holds: True

=== implied reach (given the method's saving is V^{-1/2+o(1)} and target o(x/D0)) ===
  route A (form restricted to the class): V <= 1+log x/D0, need V >= D0^2  =>  D0^3 <~ log x
  route B (character twist, full weight): V <= log x + 1,  need V >= D0^2  =>  D0^2 <~ log x
   log x =    10:  route A  D0 <~ 2.15   route B  D0 <~ 3.16
   log x =    30:  route A  D0 <~ 3.11   route B  D0 <~ 5.48
   log x =   100:  route A  D0 <~ 4.64   route B  D0 <~ 10.00
   log x =  1000:  route A  D0 <~ 10.00   route B  D0 <~ 31.62
```

**Combining Fact H with the method's shape.** The HR/Pilatte output on the class-\(a\)
correlation has size \(\asymp x\cdot V^{-1/2+o(1)}\) (Pilatte: the eigenvalues of
\(\widetilde A|_X\) are \((\sum_{d\in\mathcal D}1/d)^{1/2+o(1)}\); HR: this is *"optimal up to a
constant factor"*). C5 at modulus \(D_0\) demands \(o(x/D_0)\). Hence:

* **Route A (restrict the form to the class).** \(V\le2(1+\log x/D_0)\) and \(V\ge D_0^2\)
  \(\Rightarrow D_0^3\lesssim2\log x\), i.e. \(\boxed{D_0\ll(\log x)^{1/3}}\).
* **Route B (character twist, \(1_{n\equiv a}=\varphi(D_0)^{-1}\sum_\chi\bar\chi(a)\chi\)).** Full
  weight \(V\le\log x+1\), and the \(\varphi(D_0)\) twisted sums are each bounded by
  \(x V^{-1/2+o(1)}\), giving \(|S_{a,D_0}|\ll xV^{-1/2+o(1)}\); need \(\ll x/D_0\)
  \(\Rightarrow V\ge D_0^2\Rightarrow\boxed{D_0\ll(\log x)^{1/2}}\).

**This is strictly better than A22's [L-EXP-REACH].** It (a) gives **explicit exponents**
\(1/3\) and \(1/2\) instead of \((\log x)^{O(1)}\); (b) needs **no** numerics, **no**
Brun–Titchmarsh, and **no** \(V_D\) table; (c) makes (R-ii) and the numerical audits
*non-load-bearing*; and (d) reduces the entire dependence on Pilatte's quoted expectation to
one clearly isolated hypothesis, namely **the saving is \(V^{-1/2+o(1)}\)** — which is a
*characterization of the method*, not a theorem, and is therefore split off as an OPEN node.

**Remaining freedom the reduction does not close (stated so it is not hidden):** a variant
could in principle weight the shifts differently (the \(1/d\) weight is intrinsic to the
dilation \(n\mapsto dn\) shrinking the range by \(d\)), or could use a pairing other than an
operator-norm bound against a \(\lambda\)-blind matrix. That is exactly why the
*unconditional* confinement claim stays **OPEN** (absence-of-construction).

---

## ATTACK 8 — (R-ii) re-run, and an arithmetic correction

```
=== ATTACK 7: re-run V_D and check the Brun-Titchmarsh inference ===
  D=      1: #primes= 78498  V_D=2.88733  phi(D)=     1  V_D*phi(D)/loglogH=   1.100  USABLE
  D=      2: #primes= 78497  V_D=2.38733  phi(D)=     1  V_D*phi(D)/loglogH=   0.909  USABLE
  D=      3: #primes= 39231  V_D=0.95608  phi(D)=     2  V_D*phi(D)/loglogH=   0.728  no saving
  D=     10: #primes= 19617  V_D=0.44765  phi(D)=     4  V_D*phi(D)/loglogH=   0.682  no saving
  D=     30: #primes=  9807  V_D=0.17763  phi(D)=     8  V_D*phi(D)/loglogH=   0.541  no saving
  D=    100: #primes=  1964  V_D=0.03292  phi(D)=    40  V_D*phi(D)/loglogH=   0.502  no saving
  D=   1000: #primes=   195  V_D=0.00179  phi(D)=   400  V_D*phi(D)/loglogH=   0.272  no saving
  D=  10000: #primes=    20  V_D=0.00007  phi(D)=  4000  V_D*phi(D)/loglogH=   0.111  no saving
  D= 100000: #primes=     2  V_D=0.00000  phi(D)= 40000  V_D*phi(D)/loglogH=   0.039  no saving
```

* **\(V_3=0.95608\) at \(H=10^6\) reproduces A22's \(0.956\) exactly**, and \(V_{1000}=0.00179\)
  reproduces its \(0.0018\). \(V_D\varphi(D)/\log\log H\le1.10\) across the whole table,
  consistent with \(V_D\ll(\log\log H)/\varphi(D)\) (Brun–Titchmarsh + partial summation).
* **Arithmetic correction to A22.** A22 writes "\(\varphi(D)\ll\log\log H\), i.e.
  \(D=O(\log\log H)\)". That step is not valid as stated: \(\varphi(D)\ll Y\) only gives
  \(D\ll Y\log\log Y\). The correct inference is \(D=O(\log\log H\cdot\log\log\log\log H)\).
  Same order of magnitude; the sentence as written is a (minor) overstatement.
* **Scope correction.** (R-ii) is about **class-preserving dilations** \(p\equiv1\ (D)\).
  Under ATTACK 7's reduction this branch is subsumed and is no longer load-bearing; it is
  retained below only as a supporting fact, not as an ingredient of the confinement.

---

## ATTACK 9 — is the "Cesàro comment" error confined to the comment? **Yes — verified.**

A22 disclosed that its Audit-2 script printed *"Cesàro sides are NOT equal"* while the output
showed `ratio = 1.000`. I re-ran this to check whether the error contaminates a conclusion.

```
=== ATTACK 8: is the 'Cesaro sides' correction right? (containment check) ===
  p=   2: log |diff|=0.00e+00 | raw sums    -360 vs    -360 equal=True | densities -1.20e-04 vs -2.40e-04 ratio=2.000
  p=   3: log |diff|=1.67e-16 | raw sums   -1108 vs   -1108 equal=True | densities -3.69e-04 vs -1.11e-03 ratio=3.000
  p=   5: log |diff|=2.78e-17 | raw sums    -310 vs    -310 equal=True | densities -1.03e-04 vs -5.17e-04 ratio=5.000
  p=   7: log |diff|=5.55e-17 | raw sums    -337 vs    -337 equal=True | densities -1.12e-04 vs -7.86e-04 ratio=7.000
  p=  11: log |diff|=1.39e-17 | raw sums    -523 vs    -523 equal=True | densities -1.74e-04 vs -1.92e-03 ratio=11.000
  p= 101: log |diff|=3.47e-18 | raw sums     -18 vs     -18 equal=True | densities -6.00e-06 vs -6.06e-04 ratio=101.000
```

**Finding, and it sharpens A22's own correction.** The **raw sums** are equal (term-by-term
bijection \(n=pm\)) — so the printed comment was indeed wrong. But the **normalized
densities** differ by a factor of **exactly \(p\)** (column `ratio` = 2, 3, 5, 7, 11, 101), because
the two sides live over ranges \([1,x]\) and \([1,x/p]\). So A22's correction ("the log
weight's role is the RANGE, not the sides") is **right**, and the numbers make it precise:
the Cesàro *identity* survives, the Cesàro *normalization* does not, and it is exactly this
factor \(p\) that the \(dn/n\) measure absorbs. The A15 note (M1) on the sheet already states
the corrected version. **Error confined to the printed comment; no conclusion contaminated.**

---

## ATTACK 10 — the framing claim, and a contradiction A22 missed

**(a) "The project was carrying a false premise."** Checked against the record:

```
$ grep -rniE "nothing is (known|proven)|no bound is known|no unconditional bound" . 
TRACKING.md:231:  that the project's premise "nothing is known about the crux quantity" was FALSE.
work/1784986525-EXPLORE.md:83:being used by the project as if it meant "no bound is known", and that is **false**.
work/1784986525-EXPLORE.md:875: that the project's premise "nothing is known about the crux quantity" was FALSE.
```

**Every occurrence is A22's own.** What the sheet actually said (unchanged since
`work/1781472028-EXPLORE.md`) is:

> "unaveraged two-point Chowla \(=o(x)\) is OPEN, only log-averaged proven (Tao
> arXiv:1509.05422; Pilatte arXiv:2310.19357 = \((\log x)^{1-c}\)); … **EVEN the Cesàro
> two-point Chowla is OPEN.**"

That is **true**, and a \((1-\delta)x\) bound does not contradict it. **The "false premise"
framing is a strawman and should not be carried on the sheet.** The genuine, still valuable,
defect is narrower: **the A10 literature line was incomplete** — it listed the log-averaged
and almost-all-scales results and omitted the MR/MRT constant-factor result. Recommended
rewording: *"incomplete survey corrected"*, not *"false premise corrected"*.

**(b) A real contradiction, which A22 did not flag.** The sheet's `SO-L²` reads:
*"shift by 2 destroys multiplicativity ⇒ L²/variance/large-sieve/Halász/pretentious gives
**NO advantage**."* MR's abstract (fetched this pass) says their machinery bounds the mean
value of \(\lambda(n)\lambda(n+1)\) by \(1-\delta\) — **an advantage, from exactly that
machinery**. `SO-L²` as worded is therefore **false**; its true content is *"no \(o(1)\)
advantage, and no uniform-in-\(d\) advantage"*. EXPLORE must re-scope it when labelling.

**(c) Duplication check.** C5's AUDIT paragraph, `L-QUB-reform` (\(\sup_dR_d\le x^{o(1)}\)) and
`SO-L²` are all about **uniformity in \(d\)** at strength \(o(1)\); [L-C5-CONST] is about
**\(d\in\{1,2\}\)** at strength \(1-\delta\). No duplication; the only interaction is (b).

---

## ATTACK 11 — does any of this touch TPC, or smuggle GRH/EH/parity?

* **GRH/EH.** MR, MRT, HR and Pilatte are all unconditional; none assumes GRH or
  Elliott–Halberstam. The only conditionality risk is *ineffectivity* (ATTACK 3), isolated
  as its own node.
* **Parity.** A constant-factor bound \(|C5(x)|\le(1-\delta)x\) does **not** break the parity
  barrier in any sense relevant to TPC: it cannot distinguish primes from \(P_2\), and it is
  fully consistent with Sawin's dynamical models (arXiv:1809.03280 — which the sheet already
  cites, and which is precisely the obstruction paper for extending sign patterns beyond
  length 3). **Even \(|C5(x)|\le\varepsilon x\) for arbitrarily small fixed \(\varepsilon\) would
  give nothing for twin primes**; the sieve needs \(o(x/d)\) *uniformly* over
  \(d\le x^{1-\varepsilon}\). The GRAVEYARD entry `[ABSOLUTE-PARITY-BARRIER]` is unaffected,
  and A22's reinforcement of it is correctly scoped ("what is open is the STRENGTH and the
  UNIFORMITY, not the existence of any saving").
* **Uniformity.** The one place A22 could have slid — presenting a constant-factor
  \(d\in\{1,2\}\) bound as progress on an \(o(x/d)\)-uniform-in-\(d\) statement — it does not:
  it says so explicitly and repeatedly. **No slide found.**

---

# REDUCED CLAIM GRAPH

Smallest set whose joint truth carries whatever survives of A22. `[L-C5-CONST]` splits into
five nodes; `[L-EXP-REACH]` into five. **Recommended verdicts only — EXPLORE labels.**

## A. From [L-C5-CONST]

```
[C5C-MRT]  All eight sign patterns of (lambda(n),lambda(n+1),lambda(n+2)) in {-1,+1}^3
           occur with positive LOWER NATURAL density.
      DEPENDS ON: established mathematics.
      RECOMMENDED STATUS: ESTABLISHED-IN-LITERATURE.
      AUDIT (<5 min): arXiv:1509.01545 abstract, verbatim: "each of these eight sign
           patterns occur with positive lower natural density"; = Forum Math. Sigma 4
           (2016) e14. Cross-check: Tao, blog 2015-09-06, Theorem 1, "for k <= 3 ... a set
           of natural numbers n of positive lower density". Both fetched this pass.
      ADVERSARIAL PASS: ATTACK 1. I attacked precisely the word "lower" (an upper-density
           version would make [C5C-DED] false — see the synthetic object) and the word
           "natural" (a logarithmic version would kill the Cesaro claim). Both present at
           source. SURVIVES at abstract/blog level. Full text NOT obtained (PDF extractor
           broken in this environment) -- recorded, not papered over.

[C5C-DED]  IF each of the eight length-3 sign patterns has lower density >= c > 0, THEN
           limsup_x (1/x)|sum_{n<=x} lambda(n)lambda(n+2)| <= 1 - 8c.
      DEPENDS ON: nothing (pure elementary implication).
      RECOMMENDED STATUS: NEEDS-REVIEW.
      AUDIT (<5 min): P(x)+M(x)=floor(x); sum = P-M; M is the union of the four patterns
           with eps_1*eps_3 = -1, so liminf M/x >= 4c by superadditivity of liminf on
           disjoint unions; limsup (x-2M)/x <= 1-8c; symmetric for the other sign. Plus the
           rerunnable synthetic object of ATTACK 1 certifying that the hypothesis cannot be
           weakened to upper density.
      ADVERSARIAL PASS: ATTACKS 1-2. Attacked by substituting upper for lower density: a
           concrete de Bruijn/block sequence has all eight patterns at upper density
           >= 0.1210 while corr/x >= 0.964 infinitely often, so the conclusion "<= 0.0323"
           would be FALSE. With LOWER density the deduction is airtight (O(1) boundary
           terms harmless; 8c <= 1 automatic). SURVIVES, hypothesis now pinned.

[C5C-EFF]  The constant delta = 8c in [C5C-DED]+[C5C-MRT] is EFFECTIVE (computable), i.e.
           no ineffective (Siegel-zero-type) ingredient enters MR's or MRT's proof.
      DEPENDS ON: C5C-MRT.
      RECOMMENDED STATUS: OPEN -- no audit exists at the access level reached.
      AUDIT: NONE. Neither the MRT abstract nor Tao's blog states a numerical density or
           discusses effectivity; the full texts could not be opened this pass.
      WHAT WOULD HAVE TO BE TRUE: a reader of MR (Ann. of Math. 183 (2016) 1015-1056) and
           MRT (Forum Math. Sigma 4 (2016) e14) confirms every ingredient is effective, and
           ideally extracts a numerical c.
      ADVERSARIAL PASS: ATTACK 3. NOTE THE SCOPING: ineffectivity would NOT falsify
           [C5C-DED]+[C5C-MRT] -- "there exists an absolute delta>0 and an x_0 such that
           |sum| <= (1-delta)x for x >= x_0" is unconditionally true either way. It would
           only forbid quoting an explicit delta. Do not let this node's OPEN status be
           read as a defect in the bound.

[C5C-ID2]  EXACTLY, for every x: sum_{n<=x, 2|n} lambda(n)lambda(n+2)
                                  = sum_{m<=floor(x/2)} lambda(m)lambda(m+1).
      DEPENDS ON: nothing (complete multiplicativity).
      RECOMMENDED STATUS: NEEDS-REVIEW.
      AUDIT (<5 min): n=2m gives lambda(2m)lambda(2m+2) = lambda(2)^2 lambda(m)lambda(m+1)
           = lambda(m)lambda(m+1); m -> 2m is a bijection onto the even n <= x. Rerun the
           ATTACK-4 code: integer equality at x = 10, 101, 1000, 12345, 1e5, 999999, 4e6,
           9999997, reproducing -3, 6, 12, 122, 146, 37, -392, -364.
      ADVERSARIAL PASS: ATTACK 4. Independently recomputed with my own sieve, cross-checked
           against a trial-division Omega on 400 random n <= 1e7 (0 mismatches). Digit-for-
           digit agreement with A22's Audit 1. SURVIVES.

[C5C-SH1]  |sum_{n<=x} lambda(n)lambda(n+1)| <= (1-delta_1)x for some absolute delta_1>0
           and all large x.
      DEPENDS ON: established mathematics.
      RECOMMENDED STATUS: ESTABLISHED-IN-LITERATURE.
      AUDIT (<5 min): Matomaki-Radziwill, arXiv:1501.04585 ABSTRACT, verbatim: "the
           mean-value of lambda(n)lambda(n+1) ... is non-trivially bounded in absolute value
           by 1 - delta for some delta > 0" (= Ann. of Math. 183 (2016) 1015-1056).
           Independent cross-check: Tao arXiv:1509.05422v4 p.2, quoted verbatim in
           work/1784986525.
      ADVERSARIAL PASS: I attacked A22's chain of custody (it had only Tao's paraphrase of
           MR) by going to MR's own abstract. It says the same thing. SURVIVES.
      CONSEQUENCE (with C5C-ID2): |sum_{n<=x, 2|n} lambda(n)lambda(n+2)| <= (1-delta_1)x/2,
           a nontrivial bound at (a,d)=(0,2) -- constant-factor, NOT o(x/2).

[C5C-REACH2]  For shift h=2, the moduli d at which the dilation route yields a nontrivial
           bound are EXACTLY d in {1,2}; the odd class mod 2 gets nothing.
      DEPENDS ON: C5C-ID2, C5C-SH1, and an unresolved literature fact (below).
      RECOMMENDED STATUS: **OPEN**, with its stated REASON **FALSIFIED**.
      THE REASON IS FALSIFIED, concretely: A22 argues "the dilation identity exists iff
           d | h". It does not. For d=4, h=2, EXACTLY for every x:
              sum_{n<=x, 4|n} lambda(n)lambda(n+2) = - sum_{m<=x/4} lambda(m)lambda(2m+1),
           verified to integer equality at x = 1000, 12345, 1e5, 4e6, 9999997 (ATTACK 5a:
           -6, 48, -82, 298, 1693). d|h is only the condition for the DILATED OBJECT TO BE
           AGAIN A SHIFT CORRELATION; otherwise it is a correlation of two distinct linear
           forms. The same holds for every d = 2^k.
      WHY THE CONCLUSION IS NEVERTHELESS ONLY OPEN, NOT FALSIFIED: reaching d=4 needs an
           unconditional CESARO bound on sum_{m<=y} lambda(m)lambda(2m+1) (equivalently:
           both sign patterns of (lambda(m),lambda(2m+1)) with positive lower density). Tao
           arXiv:1509.05422 Thm 1.2 covers lambda(a1 n+b1)lambda(a2 n+b2) with
           a1b2-a2b1 = 1 != 0 but LOGARITHMICALLY AVERAGED only. I found no Cesaro
           sign-pattern theorem for (lambda(m),lambda(2m+1)) and I decline to assert either
           its existence or its absence.
      AUDIT: the ATTACK-5a identity is the inspectable concrete object; the reachability
           claim itself has NO audit -> OPEN (absence-of-construction, exactly the
           [DDS-ENV] precedent).
      ADVERSARIAL PASS: ATTACK 5.

[C5C-FRAME]  (framing, not mathematics) "The project was carrying the false premise that
           nothing is known about C5(x)."
      RECOMMENDED STATUS: **FALSIFIED as worded** -- replace with "the A10 literature line
           was INCOMPLETE".
      AUDIT (<5 min): grep -rniE "nothing is (known|proven)|no bound is known" over the
           repository returns THREE hits, all inside A22's own two files. The sheet's actual
           wording, unchanged since work/1781472028-EXPLORE.md, is "unaveraged two-point
           Chowla = o(x) is OPEN ... EVEN the Cesaro two-point Chowla is OPEN" -- true then,
           true now, and not contradicted by a (1-delta)x bound.
      ADVERSARIAL PASS: ATTACK 10(a).

[SO-L2-RESCOPE]  (correction forced on an EXISTING node) SO-L²'s sheet wording
           "L²/variance/large-sieve/Halasz/pretentious gives NO advantage" is FALSE as
           worded: MR obtain a (1-delta) advantage at shift 1 with exactly that machinery.
      RECOMMENDED ACTION FOR EXPLORE: re-scope SO-L² to "no o(1) advantage and no
           uniform-in-d advantage". Its role as an obstruction is unchanged; only the
           wording is wrong.
      AUDIT (<5 min): MR abstract (arXiv:1501.04585) vs the SO-L² line in TRACKING.md.
      ADVERSARIAL PASS: ATTACK 10(b).
```

## B. From [L-EXP-REACH]

```
[EXP-MECH]  Tao's multiplicative reduction expresses the log-averaged two-point correlation
           via E_{d in D} sum_n lambda(n)lambda(n+d) prod_{p|d}(1_{p|n} - 1/p) =
           lambda^T A~ lambda, and HR/Pilatte bound the eigenvalues of A~|_X by
           (sum_{d in D} 1/d)^{1/2+o(1)}, yielding (log x)/(log log x)^{1/2} (HR) and
           (log x)^{1-c} (Pilatte) -- both LOGARITHMICALLY AVERAGED.
      DEPENDS ON: established mathematics.
      RECOMMENDED STATUS: ESTABLISHED-IN-LITERATURE.
      AUDIT (<5 min): Pilatte arXiv:2310.19357 Sec 1.2 (verbatim block quoted in
           work/1784986525) + its abstract (fetched: "<< (log x)^{1-c}"); HR arXiv:2103.06853
           abstract (fetched: eigenvalues O(sqrt(L)), "optimal up to a constant factor",
           (1/log x) sum lambda(n)lambda(n+1)/n = O(1/sqrt(log log x))).
      ADVERSARIAL PASS: re-fetched both abstracts independently of A22's extraction; the
           statements match. SURVIVES.

[EXP-HARM]  Fact H. For any set D of DISTINCT shifts in [1,x]: sum_{d in D} 1/d <= log x + 1.
           If the bilinear form is restricted to residue classes alpha, beta mod D0 on the
           two sides, only shifts d = +-(alpha-beta) mod D0 contribute, and their harmonic
           weight is at most 2(1 + (log x)/D0).
      DEPENDS ON: nothing.
      RECOMMENDED STATUS: NEEDS-REVIEW.
      AUDIT (<5 min): harmonic series; and A_{mn} != 0 with m = alpha, n = beta (mod D0)
           forces m-n = alpha-beta (mod D0). Numerically rerunnable (ATTACK 7 table:
           V <= 1 + log x/D0 at every tested (x,D0,g); H_x <= log x + 1 at x = 1e6, 1e12,
           1e20).
      ADVERSARIAL PASS: ATTACK 7. This node is the REDUCTION: it makes (R-i), (R-ii) and the
           V_D numerics non-load-bearing. It is also what refutes the ZERO-MATRIX reading of
           (R-i) -- the weight is not 0, it is ~ V/D0.

[EXP-NOTZERO]  (R-i) as USED by A22 is refuted for the state-of-the-art shift set: for
           Pilatte's D_k = {p_1...p_k}, restricting to a residue class mod D leaves MANY
           shifts (not the zero matrix), retaining a fraction ~ k/(D*S) of the harmonic
           weight, S = sum_{p in P} 1/p. Moreover a class-RESTORING dilation exists:
           for D | d, EXACTLY
              sum_{n<=x, n=a (D)} lam(n)lam(n+h) = sum_{M<=dx, M = da (dD)} lam(M)lam(M+dh),
           in which every edge has length dh = 0 (mod D) with both endpoints = 0 (mod D).
      DEPENDS ON: nothing.
      RECOMMENDED STATUS: NEEDS-REVIEW (as a REFUTATION of an inference, not as a bound).
      AUDIT (<5 min): rerun ATTACK 6 (D=11: 820 of 11480 shifts survive, weight 0.020151 vs
           V = 0.06595, ratio 3.27, predicted D*S/k = 3.04) and ATTACK 5b (six exact
           integer identities: D=3,5,7 and d=3,4,5,6,7).
      ADVERSARIAL PASS: ATTACKS 5b, 6. This is the answer to A22's own stress-test question
           (i) for [L-EXP-REACH]: the variant it asked about EXISTS. The confinement does
           NOT come from vanishing; it comes from weight loss.

[EXP-SHAPE]  The saving delivered by this family of arguments is V^{-1/2+o(1)}, where V is
           the harmonic weight of the usable shift set -- and no better.
      DEPENDS ON: EXP-MECH.
      RECOMMENDED STATUS: **OPEN**.
      AUDIT: NONE that certifies the "and no better" half. The upper half is Pilatte's
           theorem; the lower half rests on quoted expectations -- HR "optimal up to a
           constant factor" (about their eigenvalue bound, not about all possible
           arguments) and Pilatte "saving a fixed power of the logarithm is the best that is
           achievable with current techniques". A22 itself flagged this as a quoted
           expectation, not a theorem.
      WHAT WOULD HAVE TO BE TRUE: (i) a proof that ||A~|_X||_op >= V^{1/2-o(1)}, and (ii) a
           proof that no variant escapes the 1/d weighting (which arises because dilating by
           d shrinks the range by d) or the operator-norm pairing itself.
      ADVERSARIAL PASS: ATTACK 7.

[EXP-CONF]  CONDITIONAL CONFINEMENT. Given [EXP-HARM] and [EXP-SHAPE], the mechanism can
           deliver C5-strength bounds (o(x/D0)) only for
              D0 << (log x)^{1/3}   [form restricted to the class],
              D0 << (log x)^{1/2}   [character-twist route, which costs a factor D0].
      DEPENDS ON: EXP-HARM (NEEDS-REVIEW), EXP-SHAPE (OPEN), EXP-MECH (ESTABLISHED).
      RECOMMENDED STATUS: NEEDS-REVIEW **as a conditional statement only**; the
           UNCONDITIONAL confinement (i.e. "no variant of the mechanism can do better") is
           **OPEN** -- absence-of-construction, the [DDS-ENV] precedent.
      AUDIT (<5 min): three lines. Need saving V^{-1/2} <= 1/D0, i.e. V >= D0^2. Route A:
           V <= 2(1+log x/D0) => D0^3 <~ 2 log x. Route B: V <= log x + 1 => D0^2 <~ log x.
           Numerically tabulated in ATTACK 7 (log x = 1000 gives D0 <~ 10 and <~ 31.6).
      ADVERSARIAL PASS: ATTACK 7. This SUPERSEDES A22's (R-i)+(R-ii)+(R-iii) with explicit
           exponents and without the numerics; it also STRENGTHENS the conclusion (1/2
           rather than an unspecified O(1)).
      DISTANCE TO C5 (unchanged in kind, sharper in size): C5 demands uniformity to
           d <= x^{1-eps}. (log x)^{1/2} vs x^{1-eps} is a FULL POWER OF x.

[EXP-VD]  (retained as a supporting fact only, NO LONGER an ingredient) Class-preserving
           dilations require primes p = 1 (mod D), all of which exceed D, and
           V_D = sum_{p<=H, p=1(D)} 1/p << (log log H)/phi(D).
      DEPENDS ON: established mathematics (Brun-Titchmarsh + partial summation).
      RECOMMENDED STATUS: NEEDS-REVIEW as a supporting fact, WITH AN ARITHMETIC CORRECTION.
      AUDIT (<5 min): rerun ATTACK 8: V_3 = 0.95608 and V_1000 = 0.00179 at H = 1e6
           (reproducing A22's 0.956 / 0.0018 exactly); V_D*phi(D)/log log H <= 1.10 across
           the table. p = 1+kD >= 1+D is immediate.
      CORRECTION: A22 infers "phi(D) << log log H, i.e. D = O(log log H)". Invalid as
           stated: phi(D) << Y gives only D << Y log log Y. Correct inference:
           D = O(log log H * log log log log H).
      ADVERSARIAL PASS: ATTACK 8.
```

## C. What is NOT claimed (kept explicit so the graph cannot be misread)

```
- NOTHING here bears on C5's status. C5 stays OPEN, unchanged, and is not built upon.
- A constant-factor bound is NOT a bound of C5's strength. C5 needs o(x/d) UNIFORMLY over
  all d <= x^{1-eps} and all residues a. [C5C-DED]+[C5C-MRT] give a fixed 1-8c at d=1 only,
  and [C5C-ID2]+[C5C-SH1] a fixed 1-delta_1 at (a,d)=(0,2) only. Even |C5(x)| <= eps x for
  arbitrarily small FIXED eps would yield NOTHING for twin primes.
- The odd class mod 2 still gets nothing: |Sigma_odd| <= (1-8c)x + (1-delta_1)x/2 exceeds
  the trivial x/2 unless 1-8c < delta_1/2, which the unspecified constants do not deliver.
- [EXP-CONF] is a ceiling on ONE mechanism, conditional on [EXP-SHAPE] (OPEN). It is not a
  parity-barrier theorem and not an impossibility proof.
- No node here is a legal foundation.
```

## D. The crux, restated after this pass (checkpoint, not exit)

C5 is unchanged: \(\sum_{n\le x,\,n\equiv a(d)}\lambda(n)\lambda(n+2)=o(x/d)\) uniformly for
\(d\le x^{1-\varepsilon}\). This pass measured the two axes of the deficit more precisely
than before:

* **Strength axis.** Best proven, all scales, Cesàro: \(1-\delta\) (constant), at \(d\in\{1,2\}\).
  Needed: \(o(1)\). Gap: infinite.
* **Uniformity axis.** Best proven mechanism reaches \(D_0\ll(\log x)^{1/2}\) (ATTACK 7,
  conditional on [EXP-SHAPE]). Needed: \(x^{1-\varepsilon}\). Gap: a full power of \(x\).

**What would break or escalate it — the two concrete openings this pass produced (both new):**

1. **An AP-restricted sign-pattern theorem.** If the eight length-3 patterns have positive
   lower density *inside* \(n\equiv a\ (\mathrm{mod}\ d)\), uniformly in \(d\), then
   [C5C-DED] transports verbatim and gives a constant-factor bound uniform in \(d\) — the
   first statement of the right *shape* for C5, still short by \(o(1)\) vs constant. This is
   A22's T18 sub-question (4); ATTACK 1's synthetic object shows exactly what such a theorem
   must supply (a **lower** density, uniformly).
2. **A Cesàro sign-pattern theorem for two distinct linear forms.** By ATTACK 5a, a positive
   lower density for both patterns of \((\lambda(m),\lambda(2m+1))\) immediately yields a
   nontrivial bound at \((a,d)=(0,4)\), and its \(2^k\) analogue at \((0,2^k)\) — the first
   moduli beyond \(\{1,2\}\). This is a *strictly easier-looking* target than C5 and it is
   currently unclassified in this project's record. Recommended as the next EXPLORE probe.

Neither is progress; both are named, checkable, and did not exist on the sheet before.
