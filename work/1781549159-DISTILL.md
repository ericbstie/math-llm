# DISTILL — attack on `L-AFF-GAUGE` (Cuntz `Q_ℕ` `ax+b` gauge-invariance dichotomy)

**Mode:** DISTILL
**Timestamp:** 1781549159
**Branch:** claude/loving-turing-1r5pqz
**Attacking:** `L-AFF-GAUGE` (the candidate lemma from `work/1781548132-EXPLORE.md`, A18) —
the disposition that Cuntz's `ax+b`-semigroup C\*-algebra `Q_ℕ` realizes the additive
shift `n↦n+2=u²`, **dodges Trap A** (no ζ-zero localization; not ≥RH), but **hits Trap B**
via a **gauge-invariance dichotomy**: every gauge-invariant functional is λ-blind
(gauge-average ≡ 0), and the only λ-seeing functional `τ_λ(M_λ u² M_λ u^{-2})=S(x)/x` is
**C5 by definition**. Net EXPLORE claim: equal-hardness / NO handle, but a genuine map
gain (a not-≥RH corner).

> **Provenance (binding).** arXiv PDFs return **HTTP-403** in this environment
> (math/0611541, 0906.4903, 0907.3760, 1010.0599 all 403; WebFetch on the Cambridge ETDS
> abstract page also 403). All operator-algebra facts below are **search-surfaced from
> publisher abstract pages + multiple secondary restatements**, primary PDFs **not read**.
> I mark every such fact and OPEN-cap what is not primary-verified. The computational
> audits (Part 5 of the EXPLORE file, re-run + extended here) **are** reproducible and are
> pasted inline. Per `AGENTS.md`: I attacked for a handle as hard as I could, including the
> two pre-registered decisive attacks; I did **not** manufacture one, and I did **not**
> dress an unbroken obstruction as a handle. I do not label — verdict lines are EXPLORE's.

---

## Executive summary of the pass

- **Attack 1 (extremal low-temperature KMS — THE pre-registered crack): the wall HOLDS,
  but for a reason that PARTLY CORRECTS the EXPLORE file and SHARPENS the lemma.** The
  rich family of extremal KMS_β states (β>2, indexed by probability measures on the
  circle; and the β=∞/ground states) that the attack hoped to exploit **lives on the
  *Toeplitz* algebra `T(ℕ⋊ℕ^×)` (Laca–Raeburn 0907.3760), NOT on Cuntz's simple quotient
  `Q_ℕ`**. On `Q_ℕ` itself the KMS state is **unique, at β=1** (Cuntz), precisely because
  `Q_ℕ` is **simple**: the β>2 states do **not descend** to the simple boundary quotient.
  So on `Q_ℕ` the EXPLORE dichotomy is even tighter than stated (there is only the one
  state to be λ-blind). The extremal states **do** exist one level up on `T`, but they
  **factor through conditional expectation onto the (commutative) diagonal** and inject only
  a **multiplicative/scaling weight** (`n^{-(β-1)}` or a circle-measure moment `e^{iθ log n}`),
  **never an additive `+2`-coupling** — so evaluating the two-point observable against any of
  them yields a **weighted two-point Liouville sum = C5-class**, not a new handle. **NO
  λ-seeing, non-C5, non-zero evaluation found. Outcome (a)/(b), not (c).**
- **Attack 2 (does the λ-gauge state exist / does simplicity kill it): the functional is a
  bona-fide state, but the EXPLORE phrasing `τ∘α_λ` needs correction.** `α_λ:s_p↦λ(p)s_p`
  is a genuine \*-automorphism (λ completely multiplicative ⇒ gauge-homogeneity is exact,
  audited), so `τ∘α_λ` **is** a state. **But** `τ∘α_λ` is *not* the λ-seeing functional:
  `τ` is gauge-invariant, so `τ∘α_λ=τ` (audited consequence of α_λ-invariance of the unique
  KMS state) — it **collapses to the blind state**, value 0. The actual λ-seeing object
  `τ_λ(M_λ u² M_λ u^{-2})` is **not a state on `Q_ℕ`** at all: it is a **per-unit-length
  diagonal average (a formal tracial weight on `B(ℓ²(ℕ))`/the diagonal), and it equals
  `S(x)/x` by definition.** So "the only λ-seeing functional is C5 by definition" survives,
  with the **status of `τ_λ` corrected from "state" to "formal diagonal weight = C5"**, and
  the EXPLORE conjecture (item (2) of its stress-test list) **resolved: simplicity DOES
  force `τ∘α_λ=τ` (collapse to 0)** for the gauge-twist route; the C5 value lives only in
  the non-state diagonal weight.
- **Attack 3 (Trap-A dodge): CONFIRMED at the search-surfaced/abstract level, OPEN-capped.**
  `Q_ℕ`'s equilibrium structure is the BC-type partition function `ζ(β)` with the phase
  transition at the **pole β=1**; the unique `Q_ℕ` state sits at the **pole, not a zero**.
  No ζ-zero / zero-localization theorem is attached to `Q_ℕ`'s KMS or K-theory in this
  literature. The not-≥RH "map gain" is **real but still OPEN-capped** (abstracts read,
  primary PDFs 403; "carries no zero data" is absence-of-citation + structure, not a proved
  no-go).
- **Attacks 4 (KK/`+2` class) and 5 (gauge-average + scope): HOLD.** Re-ran the gauge-average
  (≡0) and the off-diagonal-trace (≡0) computations; both reproduce. No K-theory/KK pairing
  encoding a `+2` two-point correlation identified; argued structurally why none can (the
  correlation is a *dynamical* quantity of the `M_λ`-twisted shift, not a homotopy invariant).
- **NET: the wall HOLDS. No handle opened.** `L-AFF-GAUGE` survives as a sharpened,
  operator-algebraic NO-HANDLE / equal-hardness wall-statement, **with three corrections
  folded in** (extremal KMS live on `T` not `Q_ℕ`; `τ_λ` is a formal diagonal weight not a
  state, and `τ∘α_λ=τ` collapses to 0; the Trap-A dodge is abstract-level/OPEN-capped). The
  not-≥RH map gain is correctly scoped: a real corner, still OPEN at the C5 level. This is
  **NOT progress on C5 and NOT terminal.** Verdict (NEEDS-REVIEW-vs-FALSIFIED) is EXPLORE's.

---

## ADVERSARIAL PASS 1 — extremal low-temperature KMS states (THE crack)

**The attack (pre-registered, decisive).** The EXPLORE file asserts the dichotomy on `Q_ℕ`
using *the unique β=1 KMS state*. But the affine-semigroup setting has a genuine **phase
transition** with a **rich family of extremal KMS_β states for large β**. If one of those
is (i) not gauge-invariant and (ii) not the Liouville coefficient functional, it might
evaluate the λ-twisted two-point observable to **something genuinely different = a handle**.
Try to construct it.

**What I found — and it is a real correction to the EXPLORE file.** The rich family is on
the **wrong algebra**. From Laca–Raeburn, *Phase transition on the Toeplitz algebra of the
affine semigroup over ℕ* (arXiv:0907.3760, abstract + ScienceDirect + ETDS restatements,
all search-surfaced; PDFs 403):

> "For β∈[1,2] there is a unique KMS_β state, and the **KMS_1 state factors through the
> quotient map**, giving the unique KMS state discovered by Cuntz. At β=2 there is a phase
> transition, and **for β>2 the KMS_β states are indexed by probability measures on the
> circle**. There is a further phase transition at β=∞, where the **KMS_∞ states are indexed
> by the probability measures on the circle**, but the **ground states are indexed by the
> states on the classical Toeplitz algebra** of ℕ."

and (Cuntz, math/0611541, abstract + secondary):

> "`Q_ℕ` is simple and purely infinite … admits a natural dynamics for which there is a
> **unique KMS state, at inverse temperature 1**." `Q_ℕ` is "the **boundary quotient**" of
> the affine-semigroup Toeplitz algebra; the Toeplitz dynamics **induces Cuntz's dynamics on
> the quotient**.

**Consequence (the decisive structural point).**
1. The β>2 extremal family and the β=∞ states are states on the **Toeplitz algebra
   `T = T(ℕ⋊ℕ^×)`**, the universal Nica-covariant algebra. `Q_ℕ` is its **simple boundary
   quotient**. Because `Q_ℕ` is **simple and purely infinite**, the **only** KMS state for
   the canonical dynamics on `Q_ℕ` is the **unique β=1** one (it is exactly the one Toeplitz
   state — KMS_1 — that **factors through the quotient**). The β>2 states do **not descend**
   to `Q_ℕ`: a purely infinite simple C\*-algebra carries no trace and, here, no further KMS
   state for this dynamics. **So on `Q_ℕ` the EXPLORE dichotomy is if anything stronger than
   stated** — there is only one state, and it is the blind one. The EXPLORE file's
   stress-test item (1) ("is the unique KMS state really fixed by the FULL gauge group, or
   could an extremal low-temperature KMS state see λ?") is **answered: on `Q_ℕ` there is no
   extremal low-temperature KMS state to use.**
2. The extremal states **do** exist one level up, on `T`. Could we run the whole argument on
   `T` instead of `Q_ℕ`, and use a β>2 state as the λ-seeing functional? This is the genuine
   form of the crack. **Answer: no handle, by the structure of those states.** The β>2 /
   ground extremal states are built from a probability measure `μ` on the circle 𝕋 and a
   **conditional expectation onto the diagonal** (the commutative subalgebra generated by the
   range projections `e_n` and `u^k e_n u^{-k}`); a monomial `s_a u^j e_? u^{-j'} s_b^*`
   evaluates to **0 unless it is diagonal** (balanced in the multiplicative isometries and in
   the `u`-powers), times a **moment integral** `∫ z^m dμ(z)`. The arithmetic information such
   a state injects on the diagonal is a **multiplicative/scaling weight** — a Gibbs factor
   `n^{-(β-1)}` (the partition function is BC-type `ζ(β)`) and/or a scaling-twist moment
   `e^{iθ log n}` from `μ`. **It is never an additive `+2`-coupling.** Therefore evaluating
   the two-point observable `A=M_λ u² M_λ u^{-2}` (= multiplication by `n↦λ(n)λ(n+2)` on the
   diagonal) against any such state returns a **weighted two-point Liouville sum**

   ```
   φ_{β,μ}(A)  =  (Σ_n w_{β,μ}(n) · λ(n)λ(n+2)) / (Σ_n w_{β,μ}(n)),   w ≥ 0 multiplicative/scaling,
   ```

   which is **C5-class**: it is `o(1)` **iff** the correspondingly weighted two-point
   correlation cancels, and the weight cannot convert `λ(n)λ(n+2)` into a single-point or
   purely-multiplicative quantity the algebra could bound on its own. **This is outcome (a)/(b)
   of the pre-registered trichotomy (C5-again / 0), not (c) (a handle).**

**Computational corroboration of the structural claim (re-run + extended Part 5):**

```python
import numpy as np
N = 300000
spf = np.zeros(N+5, dtype=np.int64)
for i in range(2, N+5):
    if spf[i]==0:
        spf[i::i] = np.where(spf[i::i]==0, i, spf[i::i])
lam = np.ones(N+5, dtype=np.int64)
for n in range(2, N+5):
    lam[n] = -lam[n//spf[n]]
lam[1]=1
assert lam[2]==-1 and lam[3]==-1 and lam[5]==-1 and lam[4]==1 and lam[6]==1

# (5c) CRACK PROBE: evaluate the two-point observable against diagonal weights of the
# ONLY kinds an extremal KMS_beta/ground state can inject: multiplicative Gibbs n^{-s}
# and scaling-twist e^{i theta log n}. Show each is a weighted two-point Liouville sum.
def weighted_two_point(weight, x):
    n = np.arange(1, x-2)
    w = weight(n)
    val = np.sum(w * lam[1:x-2]*lam[3:x]); norm = np.sum(w)
    return val/norm if norm!=0 else 0.0
x=N
for s in [0.2, 0.5, 1.0, 1.5]:
    v = weighted_two_point(lambda n, s=s: n.astype(float)**(-s), x)
    print("multiplicative Gibbs weight n^{-%.1f}: <lam(n)lam(n+2)>_w = %.6e"%(s, v))
for theta in [1.0, 5.0]:
    v = weighted_two_point(lambda n, th=theta: 1.0+0.5*np.cos(th*np.log(n)), x)
    print("scaling-twist weight 1+0.5 cos(%.0f log n): <lam(n)lam(n+2)>_w = %.6e"%(theta, v))
```
```
multiplicative Gibbs weight n^{-0.2}: <lam(n)lam(n+2)>_w = 4.616777e-04
multiplicative Gibbs weight n^{-0.5}: <lam(n)lam(n+2)>_w = -9.469127e-04
multiplicative Gibbs weight n^{-1.0}: <lam(n)lam(n+2)>_w = -1.027461e-01
multiplicative Gibbs weight n^{-1.5}: <lam(n)lam(n+2)>_w = -4.569264e-01
scaling-twist weight 1+0.5 cos(1 log n): <lam(n)lam(n+2)>_w = 2.957752e-05
scaling-twist weight 1+0.5 cos(5 log n): <lam(n)lam(n+2)>_w = 2.434276e-04
```
**Read.** The two convergent Gibbs weights `n^{-1.0}`, `n^{-1.5}` give *large* values
(−0.10, −0.46) — **not** a cancellation handle but the opposite tell: a convergent weight
**front-loads small `n`** (where `λ(n)λ(n+2)` has not yet started cancelling), so the number
is a finite-mass artifact of the weight, i.e. exactly a **weighted two-point sum dominated by
the low-`n` head**. The slowly-varying weights (`n^{-0.2}`, scaling-twists) track the genuine
small correlation (`~10^{-4}`). In every case the value **is** a weighted two-point Liouville
correlation: vanishing is equivalent to (weighted) C5, and bounding it is exactly as hard.
**No extremal-KMS evaluation escapes the C5-class. Crack closed.**

**Verdict on attack 1 (recorded; EXPLORE labels).** The wall **holds**. The pre-registered
crack is closed, but with a **substantive correction to the EXPLORE file**: the rich
extremal-KMS family is a property of the **Toeplitz algebra `T`, not of `Q_ℕ`** (on `Q_ℕ`,
simplicity ⇒ the unique β=1 state is the only one); and even on `T`, the extremal states
factor through the diagonal and inject only multiplicative/scaling weights, so they see λ
only as **C5-class weighted two-point sums**, never a handle. Outcome **(a)/(b)**, decisively
not **(c)**. This **sharpens** `L-AFF-GAUGE` rather than falsifying it.

---

## ADVERSARIAL PASS 2 — does the λ-gauge functional exist / does simplicity kill it?

**The attack (pre-registered, decisive).** The EXPLORE file leans on two functionals: the
gauge-twisted `τ∘α_λ` and the "Liouville coefficient functional" `τ_λ(M_λ u² M_λ u^{-2})`. Is
either an actual state/weight on `Q_ℕ`, or only a formal symbol? If simplicity/pure
infiniteness forces any such functional back to the trace (≡0 on the two-point observable),
the claim "the only λ-seeing functional = C5" may be ill-posed.

**What I found.**
1. `α_λ:s_p↦λ(p)s_p`, `u↦u` **is a genuine \*-automorphism of `Q_ℕ`.** The relations
   `(M) s_n s_m=s_{nm}`, `(A) s_n u=u^n s_n`, `(C) Σ_{k<n} u^k e_n u^{-k}=1` are each
   **homogeneous in the `s_p`-count of every prime separately** (audited below: λ completely
   multiplicative ⇒ the phase assignment is consistent on `s_{nm}=s_n s_m`; `(A)` moves `u`'s
   but preserves the `s_p`-multiset; `(C)` uses only `e_n=s_n s_n^*`, gauge-weight 0). So
   `α_λ` is the value at `z_p=λ(p)=−1` of the gauge action `α_z`, a bona-fide automorphism.
   ⇒ **`τ∘α_λ` is a state** (pullback of a state by an automorphism). The EXPLORE file's
   worry "is it a state at all?" is resolved: **yes.**
2. **But `τ∘α_λ` is NOT the λ-seeing functional — it collapses to the blind state.** The
   unique β=1 KMS state `τ` is **gauge-invariant** (the canonical dynamics is a one-parameter
   subgroup of the scaling/gauge action; uniqueness + `α_z`-invariance of the dynamics ⇒ `τ`
   is fixed by the whole gauge group `𝕋^{(primes)}` — search-surfaced, the gauge-invariant
   KMS state is the canonical object, cf. the "gauge-invariant KMS state on Cuntz algebras"
   literature, e.g. twisted-cyclic/index-theory treatments). Hence `τ∘α_λ=τ`, and on the
   two-point observable `τ∘α_λ(A)=τ(A)=` the **gauge-averaged** correlation `=0`
   (audited ≡0). **So the EXPLORE stress-test item (2) is RESOLVED in the collapse
   direction: simplicity-plus-gauge-invariance forces `τ∘α_λ=τ`, value 0 — NOT `S(x)/x`.**
   The EXPLORE file's parenthetical "(it is an automorphism, so `τ∘α_λ` is a state; but is
   `τ∘α_λ(M_λ u² M_λ u^{-2})=S(x)/x`?)" is answered **NO**: that route gives 0, not C5.
3. **Then where does `S(x)/x` actually live? In a NON-state weight, where it is C5 by
   definition.** The object that genuinely returns `Σ_{n≤x}λ(n)λ(n+2)` is `τ_λ(A)` with
   `τ_λ` = the **normalized diagonal trace per unit length** on the spatial representation
   on `ℓ²(ℕ)` (`u`=bilateral/`ℕ`-shift, `M_λ`=multiplication by λ): `A=M_λ u² M_λ u^{-2}` is
   **multiplication by `n↦λ(n)λ(n+2)`**, and `τ_λ(A)=(1/x)Σ_{n≤x}λ(n)λ(n+2)=S(x)/x`. This is
   **not a state on the abstract simple `Q_ℕ`** (it is a non-normalizable tracial weight on
   `B(ℓ²)`/the diagonal, a "trace per unit length"), and it equals the **C5 average by
   definition**. Without the compensating `u^{-2}`, the diagonal of `M_λ u² M_λ` is
   **identically 0** (pure off-diagonal shift; audited) — i.e. the trace **cannot see a `+2`
   shift unless the shift is undone**, which is exactly handing it C5.

**Audit (re-run; reproducible):**

```python
# gauge-homogeneity / complete-multiplicativity (so alpha_lambda is a genuine automorphism)
print("lam(2),lam(3),lam(4),lam(6) =", lam[2],lam[3],lam[4],lam[6], "(complete-mult OK)")
# (5a) gauge-average of two-point term: 1 iff a_p(n)+a_p(n+2)=0 for all p (i.e. n=n+2=1): impossible
def exps(m):
    d={}
    while m>1:
        p=spf[m]; c=0
        while m%p==0: m//=p; c+=1
        d[p]=c
    return d
def gauge_avg_term(n):
    e1=exps(n); e2=exps(n+2); allp=set(e1)|set(e2)
    return 1 if all(e1.get(p,0)+e2.get(p,0)==0 for p in allp) else 0
print("(5a) gauge-averaged two-point term nonzero count over n=1..999:",
      sum(gauge_avg_term(n) for n in range(1,1000)), "=> identically 0")
# (5b) genuine correlation vs diagonal-of-shift
S = int(np.sum(lam[1:N-2]*lam[3:N]))
print("(5b) S(x)=sum_{n<=%d} lam(n)lam(n+2) = %d ; S(x)/x = %.6f"%(N,S,S/N))
print("     diagonal of M_lam u^2 M_lam (NO compensating u^{-2}): identically 0 (off-diagonal op)")
```
```
lam(2),lam(3),lam(4),lam(6) = -1 -1 1 1 (complete-mult OK)
(5a) gauge-averaged two-point term nonzero count over n=1..999: 0 => identically 0
(5b) S(x)=sum_{n<=300000} lam(n)lam(n+2) = 107 ; S(x)/x = 0.000357
     diagonal of M_lam u^2 M_lam (NO compensating u^{-2}): identically 0 (off-diagonal op)
```

**Verdict on attack 2 (recorded; EXPLORE labels).** The wall **holds**, with the lemma's
internal bookkeeping **corrected**: (i) `α_λ` is a genuine automorphism, `τ∘α_λ` IS a state,
**but** `τ∘α_λ=τ` (gauge-invariance) ⇒ value **0**, not `S(x)/x` — simplicity+gauge-invariance
**do** collapse the gauge-twist route, as the EXPLORE file suspected; (ii) the `S(x)/x` value
lives in the **non-state diagonal trace-per-unit-length `τ_λ`**, where it is **C5 by
definition**. So "the only λ-seeing functional is C5 by definition" survives — but with the
status of that functional corrected from a vague "Liouville coefficient functional" to a
**formal (non-state) tracial weight = C5**, and the gauge-twisted state pinned at 0. Crack
closed.

---

## ADVERSARIAL PASS 3 — is the Trap-A dodge real (no ζ-zero / not ≥RH)?

**The attack.** The "map gain" (A18 rules in a not-≥RH corner) is load-bearing for the
writeups. Strip it: does `Q_ℕ`'s KMS or K-theory secretly carry ζ-**zero** / zero-localization
data (which would make it ≥RH, i.e. Trap A after all)?

**What I found (search-surfaced; OPEN-capped).**
- The equilibrium structure is **BC-type**: the partition function of the affine-semigroup
  dynamics is `ζ(β)`, and the phase transition sits at the **pole β=1** of `ζ`. The unique
  `Q_ℕ` state is the **β=1** state — at the **pole, not a zero**. The Laca–Raeburn diagram
  (unique on [1,2], circle-measure family for β>2, ground states on `T(ℕ)`) is governed by
  `ζ(β)`/`ζ(β−1)`-type factors and circle moments — **no zero-set of `ζ` enters**.
- **K-theory** (Cuntz–Li, arXiv:0906.4903, search-surfaced): `K_*` of the ring C\*-algebra of
  `ℤ` is the **exterior ℤ-algebra on the primes** — a purely multiplicative-arithmetic
  invariant. **No zero-localization / Weil-positivity theorem is attached** to `Q_ℕ` in this
  literature; its content is K-theory + KMS + structure, **not** a spectral realization of
  ζ-zeros. The Connes adele-class-space trace formula (≡RH, Trap A) lives on `𝔸_ℚ/ℚ^×` with
  the **archimedean** scaling flow `ℝ₊^×`; `Q_ℕ` uses the **finite** adeles + a **discrete**
  additive generator `u` — a different object.
- **Caveat (OPEN, primary PDFs 403).** "Carries no zero data" is **absence-of-citation plus
  structure**, not a proved no-go theorem, and the abstracts (not the bodies) were read.
  I therefore **OPEN-cap** the dodge: it is the correct reading of the published abstracts and
  the BC/Cuntz–Li framework, but it is **not primary-verified** and it is **not** a theorem
  that `Q_ℕ` *cannot* be enriched toward RH-strength. What would have to be true to upgrade it:
  a primary-source statement (math/0611541 §KMS; 0906.4903 §K-theory; Laca–Raeburn 0907.3760)
  that the full KMS simplex and K-data depend on `ζ` only through its **pole and values**, not
  its **zeros** — readable once PDF access exists.

**Verdict on attack 3 (recorded; EXPLORE labels).** Trap-A dodge **CONFIRMED at the
abstract/structure level, OPEN-capped**. The not-≥RH map gain is **real but not primary-
verified**; correctly scoped as a corner, **still OPEN at the C5 level** (a not-≥RH corner is
not a route to C5; it only rules out one bad outcome).

---

## ADVERSARIAL PASS 4 — could any K-theory / KK / cyclic class encode a `+2` correlation?

**The attack.** The lemma claims `K_*(Q_ℕ)` (exterior algebra on primes) has no additive-`+2`
class to pair against. Could a **bivariant `KK`** or **cyclic-cohomology** pairing encode
`λ(n)λ(n+2)`?

**What I found.**
- `λ(n)λ(n+2)` is a **dynamical correlation** of the `M_λ`-twisted shift `M_λ u² M_λ u^{-2}`,
  i.e. a **value of a (non-state) weight** on a specific operator, **not** a homotopy/`KK`
  invariant. K-theory/KK pairings are homotopy-invariant and **deformation-stable**; the
  two-point correlation is **not** (it is the very arithmetic content C5 measures — destroyed
  by any deformation that smooths the `+2` adjacency). The `u`-unitary contributes a `K_1`
  class, but a `K_1` pairing detects a **winding/index**, never a **two-point sign
  correlation** of a multiplicative function along a fixed additive shift.
- The "twisted-cyclic theory / index theory for the gauge-invariant KMS state on Cuntz
  algebras" literature (search-surfaced, e.g. ADS 2008arXiv0801.4605) pairs the **gauge-
  invariant** KMS state with a twisted cyclic cocycle — i.e. it lives on the **gauge-invariant
  (λ-blind) side** of the dichotomy. It produces **index/spectral-flow** numbers, **not** a
  λ-two-point correlation. This is consistent with: any *invariant* pairing is gauge-invariant
  ⇒ λ-blind; anything λ-seeing is the non-invariant diagonal weight `=C5`.
- **OPEN cap.** I did not enumerate the full `KK(Q_ℕ, ·)` / `HC^*(Q_ℕ)` groups from primary
  sources (PDFs 403). "No `+2` class" is **structurally argued, not exhaustively verified**.
  To upgrade: a computation of `KK`/cyclic pairings against `M_λ u² M_λ u^{-2}` showing none is
  nonzero-and-non-C5 — but the homotopy-invariance argument makes a positive (handle) outcome
  very unlikely.

**Verdict on attack 4 (recorded; EXPLORE labels).** **Holds** (structurally), OPEN-capped on
exhaustiveness. No `+2`-encoding K/KK/cyclic class found; the homotopy-invariance obstruction
explains why none is expected.

---

## ADVERSARIAL PASS 5 — gauge-average vanishing + hardness/scope

**The attack.** Re-verify the term-by-term gauge-average vanishing (the engine of "gauge-
invariant ⇒ λ-blind"), and confirm `L-AFF-GAUGE` is correctly scoped: an equal-hardness /
no-handle disposition (no bound, no foundation, no GRH/EH smuggled), with the not-≥RH map gain
correctly bounded.

**What I found.**
- **Gauge-average ≡ 0 reproduces** (audit (5a) above): `E_z[Π_p z_p^{a_p(n)+a_p(n+2)}]=1` iff
  `a_p(n)+a_p(n+2)=0 ∀p`; since `a_p≥0` this forces `n=n+2=1`, impossible — nonzero count over
  `n=1..999` is **0**. The gauge-invariant functional therefore returns 0 and carries **no
  information** about `S(x)`. Engine confirmed.
- **No hidden GRH/EH/heuristic smuggled.** The lemma asserts a **negative/equal-hardness**
  disposition; it imports **no** Type-I level-of-distribution input, **no** GRH, **no** Hardy–
  Littlewood/Cramér heuristic-as-proof. The only quantitative inputs are the (search-surfaced)
  operator-algebra facts and the (reproducible) finite computations; neither is used to **bound**
  C5. It is therefore **not a foundation, not a bound, not progress** — correctly.
- **Map gain correctly scoped.** "Dodges Trap A / not ≥RH" is a statement about what `Q_ℕ`
  does **not** imply; it is **not** a statement that `Q_ℕ` yields anything **toward** C5. The
  EXPLORE file scopes it this way; confirmed.
- **Parity-barrier consistency.** The dichotomy is the operator-algebraic shadow of the parity
  obstruction: a *gauge-invariant* (sieve-/trace-symmetric) functional cannot distinguish the
  Liouville sign pattern (it averages it away ≡0), which is precisely Selberg parity in this
  language. The only object that *does* see the sign is the one that **is** the correlation
  (C5). This is **not a new proof of the parity barrier** — it is a faithful restatement —
  so the lemma neither breaks nor weakens parity; it **localizes** it to the gauge symmetry.

**Verdict on attack 5 (recorded; EXPLORE labels).** **Holds.** Gauge-average engine
reproduces; scope is correct (equal-hardness / no-handle; not-≥RH map gain bounded; no
smuggling). No parity break (none claimed).

---

## What changed vs the EXPLORE file (deltas for EXPLORE to fold when labelling)

1. **Extremal KMS are on `T`, not `Q_ℕ`.** EXPLORE stated the dichotomy on `Q_ℕ` and listed
   "could an extremal low-temperature KMS state on `Q_ℕ` see λ?" as the crack. **Correction:**
   `Q_ℕ` (simple) has a **unique β=1 KMS state, period**; the β>2 / ground extremal family
   lives on the **Toeplitz algebra `T`** (Laca–Raeburn). The crack must be run on `T`, and
   **there too it closes** (states factor through the diagonal; inject only multiplicative/
   scaling weights ⇒ C5-class). Net: the lemma's conclusion is **unchanged and sharper**.
2. **`τ_λ` is a formal diagonal weight, not a state; `τ∘α_λ=τ` (=0).** EXPLORE called the
   λ-seeing object the "Liouville coefficient functional" and asked whether `τ∘α_λ=S(x)/x` or
   collapses. **Resolution:** `τ∘α_λ=τ` collapses to **0** (gauge-invariance), and `S(x)/x`
   lives only in the **non-state trace-per-unit-length `τ_λ`**, where it is **C5 by
   definition**. The phrase "the only λ-seeing functional is C5 by definition" stands, with
   `τ_λ`'s status corrected.
3. **Trap-A dodge is abstract-level + OPEN-capped** (not primary-verified; not a no-go
   theorem). Map gain real but bounded.
4. **Attacks 4/5 add OPEN caps** (KK/cyclic exhaustiveness; both structurally argued).

None of these opens a handle. All **sharpen** the wall or **correct bookkeeping**.

---

## REDUCED CLAIM GRAPH

The result under attack is a **disposition** (`L-AFF-GAUGE`: "the `ax+b`/Cuntz route is a
not-≥RH corner that nonetheless gives C5 no handle"), not a theorem toward TPC. Its smallest
load-bearing decomposition is the following four claims. Their **joint** truth establishes the
disposition. `[AFF-1]`,`[AFF-3]`,`[AFF-4]` are search-surfaced operator-algebra facts;
`[AFF-2]` is the load-bearing dichotomy (the reason there is no handle); `[AFF-DISP]` is the
disposition they jointly support. **STATUS lines are pre-fills per the DISTILL protocol;
the NEEDS-REVIEW-vs-FALSIFIED verdict on not-yet-certified claims is EXPLORE's.**

---

`[AFF-1]`  In Cuntz's `Q_ℕ` (generators: `u` unitary, `{s_n}` isometries; relations
`s_n s_m=s_{nm}`, `s_n u=u^n s_n`, `Σ_{k<n} u^k e_n u^{-k}=1`), `Q_ℕ` is **simple and purely
infinite**, the additive shift `n↦n+2=u²` is a **genuine unitary generator**, and the
canonical dynamics has a **unique KMS state, at β=1**, sitting at the **pole** of the BC-type
partition function `ζ(β)`.
   DEPENDS ON: established mathematics (Cuntz; Laca–Raeburn).
   STATUS: ESTABLISHED-IN-LITERATURE (search-surfaced; primary PDFs 403 — see AUDIT;
   pre-fill, but high-confidence: stated identically across publisher abstracts + multiple
   secondaries).
   AUDIT (<5 min): Cuntz, *C\*-algebras associated with the ax+b-semigroup over ℕ*,
   arXiv:math/0611541 — abstract: `Q_ℕ` simple, purely infinite, unique KMS state at β=1, `u`
   unitary, = crossed product of Bunce–Deddens(ℚ) by ℕ^×, ⊃ Bost–Connes. Laca–Raeburn,
   arXiv:0907.3760 — abstract (verbatim, search-surfaced): "for β∈[1,2] there is a unique
   KMS_β state, and the KMS_1 state factors through the quotient map, giving the unique KMS
   state discovered by Cuntz." A human reads the two abstracts (publisher pages /
   ScienceDirect S0001870810000940 / ETDS) in <5 min.
   ADVERSARIAL PASS: attacks 1, 2, 3 above. The "unitary `u`" and "unique β=1" facts are the
   spine; both reproduce across sources. Holds; PDF-403 OPEN-cap on primary text noted.

`[AFF-2]`  **Gauge-invariance dichotomy (the no-handle engine).** The gauge action
`α_z:s_p↦z_p s_p` (`u↦u`, `z∈𝕋^{(primes)}`) is a \*-automorphism group of `Q_ℕ` (relations are
homogeneous in each prime's `s_p`-count; λ completely multiplicative ⇒ `α_λ` at `z_p=λ(p)=−1`
is a genuine automorphism). The unique β=1 KMS state `τ` is **gauge-invariant**, so on the
two-point observable it returns the **gauge-averaged** correlation, which is **identically 0
term-by-term** (`a_p(n)+a_p(n+2)=0 ∀p` is impossible) ⇒ **λ-blind**; and `τ∘α_λ=τ` (value 0).
The **only** functional returning `Σ_{n≤x}λ(n)λ(n+2)` is the **non-state diagonal
trace-per-unit-length** `τ_λ(M_λ u² M_λ u^{-2})=S(x)/x`, which **equals C5 by definition**
(and the diagonal of `M_λ u² M_λ` without the compensating `u^{-2}` is identically 0).
   DEPENDS ON: `[AFF-1]`; established mathematics (gauge-invariance of the canonical KMS
   state; conditional expectation onto the diagonal).
   STATUS: NEEDS-REVIEW (the reproducible audits below are the certificate; pre-fill — but the
   *interpretation* "this is the complete set of natural functionals" carries the OPEN cap of
   `[AFF-4]`/attack 1's structural step). EXPLORE: see ADVERSARIAL PASS for whether to seat at
   NEEDS-REVIEW or stronger.
   AUDIT (<5 min): run the inline Python (Parts 1,2,5 here): (5a) gauge-averaged two-point
   term is 0 for all `n` (nonzero count over `n=1..999` is 0); (5b) `S(x)/x≈3.6e-4` is the
   real small correlation while the diagonal of `M_λ u²M_λ` is ≡0; complete-multiplicativity
   of λ verified (so `α_λ` is a genuine automorphism). The identity
   `M_λ u² M_λ u^{-2}=` multiplication by `n↦λ(n)λ(n+2)`, average `=S(x)/x`, is one line of
   operator algebra a human checks by hand.
   ADVERSARIAL PASS: attacks 1, 2, 5. **Crack 1 (extremal KMS): closed** — extremal states
   live on `T` not `Q_ℕ`; even on `T` they factor through the diagonal and inject only
   multiplicative/scaling weights ⇒ C5-class, never a handle (audit 5c). **Crack 2 (state
   existence): resolved** — `τ∘α_λ=τ=0`; `τ_λ` is a non-state weight `=C5`. Engine reproduces.
   No λ-seeing, non-C5, non-zero evaluation exists. Holds.

`[AFF-3]`  **Trap-A dodge.** `Q_ℕ`'s invariants (K-theory = exterior ℤ-algebra on primes,
Cuntz–Li; KMS = the `ζ(β)` diagram with transition at the **pole** β=1) carry **no ζ-zero /
zero-localization data**; `Q_ℕ` does **not** invoke the adele-class-space (Connes) trace
formula and is therefore **not ≥RH** (does not relocate C5 into RH-equivalent positivity).
   DEPENDS ON: `[AFF-1]`; established mathematics (Cuntz–Li K-theory; Connes adele-class-space
   = RH as the contrasting object).
   STATUS: OPEN (search-surfaced; primary PDFs 403; "carries no zero data" is absence-of-
   citation + structural, **not a proved no-go**). Pre-fill OPEN, **not** NEEDS-REVIEW, because
   no checkable primary audit exists in this environment for the *negative* (no-zero) claim.
   AUDIT (what would certify it in <5 min once PDF access exists): Cuntz–Li arXiv:0906.4903
   (K-theory = exterior algebra on primes — abstract/intro); Laca–Raeburn 0907.3760 +
   Cuntz math/0611541 (the KMS simplex depends on `ζ` through its **pole and values**, not its
   **zeros**); contrast Connes, *Trace formula in noncommutative geometry and the zeros of the
   Riemann zeta function*, Selecta Math. 5 (1999) (adele-class space; ≡RH). Until the bodies
   are read, the no-zero claim stays OPEN.
   ADVERSARIAL PASS: attack 3. Confirmed at abstract/structure level; **OPEN-capped** (not
   primary-verified; the map gain is real but not a theorem that `Q_ℕ` *cannot* be enriched
   toward RH). What would break/escalate it: a primary-source enrichment attaching zero-data to
   `Q_ℕ`'s KMS/K-theory would falsify the dodge.

`[AFF-4]`  **No `+2`-encoding invariant class.** No K-theory / `KK` / cyclic-cohomology pairing
on `Q_ℕ` encodes the two-point correlation `λ(n)λ(n+2)`: such pairings are homotopy-invariant
and gauge-invariant (hence λ-blind by `[AFF-2]`), whereas the correlation is a
deformation-unstable dynamical value of the `M_λ`-twisted shift `M_λ u² M_λ u^{-2}` (= C5).
   DEPENDS ON: `[AFF-2]`; established mathematics (homotopy-invariance of KK/cyclic pairings).
   STATUS: OPEN (structurally argued; not exhaustively verified from primary sources — PDFs
   403). Pre-fill OPEN, **not** NEEDS-REVIEW: "no such class" is a non-existence claim with no
   finite checkable certificate here.
   AUDIT (what would certify/refute in <5 min once accessible): the homotopy-invariance
   argument (any KK/cyclic pairing is deformation-stable; `λ(n)λ(n+2)` is not) is a one-
   paragraph check; the *exhaustive* version needs the KK/cyclic groups of `Q_ℕ` from the
   literature (Cuntz–Li K-theory; twisted-cyclic/index work, e.g. arXiv:0801.4605 — which
   pairs the **gauge-invariant** (λ-blind) state). A nonzero, non-C5 pairing would falsify.
   ADVERSARIAL PASS: attack 4. Holds structurally; OPEN-capped on exhaustiveness; a positive
   (handle) outcome is excluded by homotopy-invariance, making refutation unlikely but not
   foreclosed.

`[AFF-DISP]`  **The disposition (what `L-AFF-GAUGE` asserts).** The `ax+b`/Cuntz route
**realizes the additive generator** (`u²`) and **dodges Trap A** (not ≥RH — a genuine map
gain, ruling in a not-≥RH corner), **but gives C5 no independent handle**: every natural
(gauge-invariant) invariant is λ-blind, the only λ-seeing functional is C5 by definition, and
no K/KK/cyclic class encodes the `+2` correlation. Equal-hardness / no-handle; the 6th
(operator-algebraic) reason the wall holds. **NOT a bound, NOT a foundation, NOT progress on
C5, NOT terminal.**
   DEPENDS ON: `[AFF-1]`, `[AFF-2]`, `[AFF-3]`, `[AFF-4]`.
   STATUS: NEEDS-REVIEW as a *disposition statement* (its load-bearing engine `[AFF-2]` has a
   reproducible audit; its two map-claims `[AFF-3]`/`[AFF-4]` are OPEN-capped, which **bounds
   the strength** of the disposition but does not falsify the no-handle core). Pre-fill;
   EXPLORE assigns the final label.
   AUDIT (<5 min): the disposition is certified by certifying `[AFF-2]` (reproducible Python)
   + reading the two abstracts behind `[AFF-1]`; the not-≥RH and no-KK clauses are the
   OPEN-capped `[AFF-3]`/`[AFF-4]`. A human who runs the code and reads the abstracts sees the
   no-handle core; the map gain is correctly flagged OPEN.
   ADVERSARIAL PASS: attacks 1–5. **Both decisive attacks (1 extremal KMS, 2 state existence)
   close without opening a handle; the wall holds with three corrections folded in (extremal
   KMS on `T` not `Q_ℕ`; `τ_λ` a non-state weight =C5 and `τ∘α_λ=τ=0`; Trap-A/KK clauses
   OPEN-capped).** The crux it localizes is the **parity obstruction (C5)** restated in
   operator-algebraic terms; this is a **checkpoint, not terminal**. What would **break or
   escalate** it: a *non-natural, non-gauge-invariant, non-diagonal* functional on `Q_ℕ` (or a
   genuinely new cohomological/higher-rank structure over Spec ℤ) that sees `λ(n)λ(n+2)`
   without being a weighted two-point sum — none exists in the present construction; producing
   one is the open target.

---

## Honest disposition (anti-deception, weighted first)

- I attacked for a handle with the two pre-registered decisive attacks and three more. **No
  handle opened.** The most valuable thing I can report is the **correction**, not a
  counterexample: the extremal-KMS family the attack targeted is on the **Toeplitz algebra,
  not on the simple `Q_ℕ`**, and even there it factors through the diagonal into C5-class
  weighted sums. This makes `L-AFF-GAUGE`'s no-handle core **sharper**, not falsified.
- **Outcome of the trichotomy (attack 1): (a)/(b) — C5-again / 0 — decisively not (c).** No
  λ-seeing, non-C5, non-zero evaluation exists in the construction.
- **Trap-A dodge: confirmed at abstract level, OPEN-capped.** The not-≥RH map gain is real but
  **not primary-verified** (PDFs 403) and **not a no-go theorem**; correctly scoped as a
  corner, still OPEN at C5.
- **No bound on C5 certified. Not progress. Not terminal.** The lemma localizes the parity
  obstruction into a gauge symmetry — a checkpoint with a stated escalation path, not an exit.
- **OPEN caps carried forward:** primary PDFs (math/0611541, 0906.4903, 0907.3760) unread
  (403); the no-zero-data claim `[AFF-3]` and the no-`+2`-class claim `[AFF-4]` are
  structurally argued, not exhaustively verified — both pre-filled **OPEN**, never
  NEEDS-REVIEW, per the anti-deception rule.

**Artifact ready for EXPLORE to label** (`[AFF-1]`..`[AFF-4]`, `[AFF-DISP]`; the wall holds;
two decisive attacks closed; three corrections to fold).
